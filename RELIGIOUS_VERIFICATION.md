# سجل تحقق دفعة القسم الديني

## النطاق

أضيفت واجهة عربية RTL للقسم الديني داخل `/religious` تشمل متابعة الصلوات الخمس، التقدم في الورد اليومي، جلسات أذكار الصباح والمساء، وإعدادات المدينة وطريقة الحساب. يحفظ النظام حالة الإنجاز والإعدادات فقط، ولا يخزن نصوص القرآن أو الأذكار أو مصادر المواقيت الخارجية كنص موثوق.

## التغييرات التقنية

أضيف جدول `religious_settings` user-owned مع migration رقم `0004_lethal_shen.sql`. يتضمن الجدول المدينة، طريقة الحساب، سجلات الصلوات، تقدم الورد، وجلسات الذكر. أضيف `GET/PATCH /api/religious` مع session ownership وupsert آمن. توسع الـstore و`backend-sync` بالـhydration والمزامنة المتفائلة وlocalStorage fallback. عند إكمال الصلاة، يتحدث العنصر المرتبط في خطة اليوم.

## نتائج الاختبار

| الاختبار | النتيجة |
|---|---|
| TypeScript check | ناجح بلا أخطاء |
| Next.js production build | ناجح؛ تم توليد مسار `/religious` و`/api/religious` |
| Drizzle migration generation | ناجح؛ `0004_lethal_shen.sql` يضيف الجدول والقيد المتوقعين |
| RTL وDesign System | ناجح بصريًا؛ PageShell، Cairo، الكروت، والألوان الدلالية متسقة |
| التنقل | ناجح؛ رابط «الديني» ظاهر في TopNav |
| إكمال صلاة الظهر | ناجح؛ تغيرت النسبة من 20% إلى 40% وتحدثت الحالة محليًا |
| تقدم الورد | ناجح بعد إصلاح Provider dependencies؛ ظهرت القيمة المحفوظة `20/20` بعد إعادة التحميل |
| جلسة أذكار المساء | ناجحة؛ ظهرت كمكتملة فورًا بعد الضغط |
| `GET /api/religious` دون جلسة | `401 Unauthorized` |
| `PATCH /api/religious` دون جلسة | `401 Unauthorized` |
| `git diff --check` | ناجح |

## ملاحظات وحدود

لا توجد credentials فعلية لـNeon أو Better Auth في البيئة الحالية، لذلك تم اختبار fallback المحلي وحماية الـAPI دون جلسة. اختبار المزامنة الفعلية وownership بين مستخدمين يحتاج قاعدة بيانات وجلسة حقيقية. مواقيت الصلاة الحالية تجريبية إلى حين ربط مصدر موثوق والتحقق من API خارجي.
