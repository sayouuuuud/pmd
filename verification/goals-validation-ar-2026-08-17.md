# تقرير تحقق نموذج الأهداف — التحقق العربي

**التاريخ:** 2026-08-17
**وقت الجولة المقاس من ساعة النظام:** بدأت عند 13:38:09Z وانتهت بعد التنظيف عند 13:38:51Z تقريبًا.
**النطاق:** نموذج إنشاء هدف جديد في `components/goals/goals-workspace.tsx`.

## التغيير

أضيف تحقق محلي عربي لحقل عنوان الهدف، مع `noValidate` على النموذج ورسالة `اكتب اسم الهدف أولًا` داخل عنصر `role="alert"`. عند ظهور الخطأ، يحمل حقل العنوان `aria-invalid` و`aria-describedby`، ويُمسح الخطأ عند بدء الإدخال من جديد. لم يتغير مخزن البيانات أو مسار CRUD أو التصميم البصري.

## الاختبار المتصفحي

| الحالة | النتيجة |
|---|---|
| إرسال النموذج فارغًا | نجح؛ ظهرت الرسالة العربية `اكتب اسم الهدف أولًا` ولم يُنشأ سجل |
| إدخال `اختبار تحقق الهدف` بعد الخطأ | نجح؛ اختفى الخطأ واستمر النموذج في الحالة الطبيعية |
| حفظ الهدف الاختباري | نجح؛ ظهر الهدف ضمن الأهداف النشطة |
| أرشفة الهدف الاختباري من الزر المرئي الخاص به | نجح؛ اختفى من الأهداف النشطة وعادت الإحصاءات للحالة السابقة |
| إنشاء/تعديل بيانات مستخدم فعلية | لم يُنفذ؛ الجولة استخدمت سجلًا اختباريًا محليًا ونظفته بالكامل |

## النطاق والقيود

لم تُرسل بيانات اعتماد أو طلبات مصادقة أو تغييرات خارجية. لم يُجرَ أي تعديل مباشر على `localStorage` لتنظيف الهدف؛ تم استخدام زر الأرشفة المرئي المدعوم في الواجهة.

## الحالة

الدفعة جاهزة لتشغيل TypeScript وESLint و`git diff --check` وNext build، ثم إعادة فحوص ownership وresponsive وaccessibility قبل اعتمادها.

## بوابات الجودة

بدأ التشغيل عند `2026-08-17T13:39:45Z` وانتهى عند `2026-08-17T13:40:07Z` وفق ساعة النظام. نجح `tsc --noEmit`، ونجح ESLint، ونجح `git diff --check`، ونجح `next build` مع توليد 27 صفحة. بقي تحذير Next.js المعلوماتي بشأن تقادم convention الخاص بـ`middleware`، وهو غير حاجز ولم يتغير ضمن هذه الدفعة.

## فحوص الاعتماد الشاملة

أُعيد تشغيل الفحوص بعد تعديل نموذج الأهداف. فحص الملكية الساكن مرّ بنتيجة `ownership_audit=PASS` على 45 Route Handler، وفحص responsive مرّ بـ34 حالة ناجحة و0 فشل، وفحص accessibility مرّ بـ34 حالة و0 فشل. أعيدت ملفات screenshots وJSON المولدة العابرة إلى حالتها السابقة، ولم تُعتمد إلا نتائج الفحص الموثقة هنا.
