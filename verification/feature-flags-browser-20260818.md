
## 2026-08-18T02:03Z — flag مغلق

تم فتح `/account` على خادم الإنتاج المحلي بعد إعادة تشغيله من البناء الحالي دون متغيرات Feature Flags. حملت الصفحة كاملة مع RTL وبقية إعدادات الحساب والنسخ الاحتياطي. لم تظهر بطاقة «التحقق بخطوتين — تجريبي»، ولم تظهر أزرار إعداد أو تعطيل 2FA، بينما بقيت بقية عناصر الحساب متاحة. ظهرت محاولة تشغيل أولى بصفحة عطل مؤقت لأن عملية Next قديمة كانت ما تزال ممسكة بالمنفذ؛ بعد إيقاف العملية القديمة وإعادة التشغيل أصبح الاختبار PASS. لا توجد بيانات جلسة بعيدة في هذه البيئة.

## 2026-08-18T02:04Z — flag 2FA مفعّل صراحةً

تم بناء نسخة مؤقتة باستخدام `NEXT_PUBLIC_PMD_ENABLE_EXPERIMENTAL_2FA=1` وتشغيلها على `localhost:3006`. حملت `/account` بنجاح، وظهرت بطاقة «التحقق بخطوتين — تجريبي» ورسالة تسجيل الدخول المطلوبة، بينما بقيت بقية الميزات التجريبية غير مفعلة. الواجهة RTL سليمة. الاختبار يثبت أن 2FA لا يظهر في الحالة الافتراضية ويظهر فقط عند فتح flag صراحةً؛ لا يثبت جاهزية 2FA للإنتاج ولا تدفق login challenge أو recovery.
