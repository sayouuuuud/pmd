# أدلة المتصفح — أخطاء الملاحظات

**التاريخ:** 2026-08-17
**المسار:** `http://localhost:3004/notes`

## إنشاء ملاحظة فارغة

أُرسل نموذج الالتقاط السريع بعد تفريغ حقل عنوان الملاحظة. ظهرت الرسالة العربية: `اكتب عنوان الملاحظة أولًا.` دون إنشاء ملاحظة جديدة.

فحص DOM أثبت القيم التالية:

| العنصر | القيمة |
|---|---|
| `role` | `alert` |
| `aria-live` | `assertive` |
| `aria-atomic` | `true` |
| `aria-describedby` للحقل | `new-note-error` |
| `aria-invalid` للحقل | `true` |

النتيجة: **PASS** لمسار التحقق والارتباط الدلالي والـ live-region. دليل console محفوظ في `/home/ubuntu/console_outputs/exec_result_2026-08-17_18-44-06_218.txt`.

## النطاق المتبقي

لم يُختبر بعد نموذج تعديل الملاحظة؛ سيُفتح من بطاقة ملاحظة قائمة ويُرسل بعنوان فارغ، ثم تُفحص الرسالة `note-<id>-edit-error` وخصائصها والارتباط بالحقل قبل تشغيل بوابات الجودة.

## حدود الاختبار

الاختبار لم يغير بيانات قائمة؛ اقتصر على حالة تحقق فاشلة محلية، ولم يتضمن أي تعديل على API أو قاعدة البيانات أو المصادقة.

## تعديل ملاحظة بعنوان فارغ

فُتح نموذج تعديل الملاحظة الأولى، وفُرغ العنوان، ثم أُرسل النموذج. ظهرت الرسالة العربية: `اكتب عنوان الملاحظة أولًا.`

فحص DOM أثبت القيم التالية:

| العنصر | القيمة |
|---|---|
| معرّف الخطأ | `note-note-1786967049031-edit-error` |
| `role` | `alert` |
| `aria-live` | `assertive` |
| `aria-atomic` | `true` |
| `aria-describedby` للحقل | `note-note-1786967049031-edit-error` |
| `aria-invalid` للحقل | `true` |

النتيجة: **PASS** لمسار تعديل الملاحظة والـ live-region والربط الدلالي. دليل console محفوظ في `/home/ubuntu/console_outputs/exec_result_2026-08-17_18-44-46_496.txt`.

الاختبار لم يحفظ أي تعديل ولم يغير بيانات الملاحظة؛ اقتصر على حالة التحقق الفاشلة.
