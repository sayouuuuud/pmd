# سجل اختبارات التفاعل

**التاريخ:** 16 أغسطس 2026
**البيئة:** خادم التطوير المحلي على `http://localhost:3004`

## النتائج

- **إضافة مهمة:** أُضيفت مهمة «اختبار تدفق المهمة» وظهرت في قائمة المهام.
- **تسجيل مصروف:** سُجّل مصروف «اختبار تدفق المصروف» وظهر في آخر العمليات.
- **تقدم ديني:** زِيد عداد ذكر الصباح مرة واحدة، وظهر التحديث في شريط التقدم والعداد الإجمالي.
- **السبورة:** أُنشئت بطاقة «اختبار تحريك البطاقة» بالملاحظة «بطاقة تحقق تفاعلية». ظهرت في مساحة «إطلاق المنتج»، ثم تحركت إحداثياتها من `(403,470)` إلى `(555,581)` بعد محاكاة السحب، ما يؤكد عمل السحب والإفلات والحفظ المحلي.
- **التلاوة:** اختير القارئ «إبراهيم الأخضر» وسورة الفاتحة، ثم شُغلت التلاوة. ظهرت حالة «تعمل الآن» ومشغل الصوت وزر «حفظ الموضع الحالي».
- **الأرشيف:** فُتحت صفحة `/archive` وتحققت حالة الفراغ العربية ومرشحات الأقسام. دورة الأرشفة والاسترجاع لبطاقات السبورة موثقة تفصيليًا في `verification/board-archive.md`.

## ملاحظات التحقق

أُجريت الفحوصات بعد الاختبارات التفاعلية: نجح TypeScript، ونجح ESLint مع تحذيرين قائمين غير حاجزين في `app/api/finance/route.ts` و`app/layout.tsx`، ونجح بناء الإنتاج. لا تُعد رسائل Next.js Dev Tools الظاهرة في بيئة التطوير أخطاءً منطقية في التطبيق.

## البنود اللاحقة

تبقى تحسينات غير حاجزة خارج نطاق اختبار التفاعل الأساسي، أهمها سرعة تحميل المصحف والسبورة، تحكم سرعة التلاوة المخصص، حالات «أسمعه بعدين/تم الاستماع»، السبحة الإلكترونية المتقدمة، ومراجعة شاملة لكل حالات الفراغ والتحميل.

## النتيجة

اكتملت مسارات التفاعل الرئيسية المحددة في Milestone 6.2 بنجاح في المتصفح المحلي، مع توثيق البنود اللاحقة بدل اعتبارها مكتملة دون تحقق.

- **تحكم التلاوة المتقدم:** بعد تشغيل سورة الفاتحة، ظهرت أزرار الرجوع والتقديم 15 ثانية وقائمة السرعة. اختيرت سرعة 1.5×، وأكد فحص عنصر الصوت أن `playbackRate` أصبح `1.5` مع مدة 61 ثانية.
- **السبحة الإلكترونية:** اختير الهدف 33 فظهر العداد `28 / 33`، ثم استُخدم زر «تصفير» فصار `0 / 33`، وبعد «تسبيحة واحدة» صار `1 / 33` مع نسبة 3%.
- **قراءة المصحف:** ظهرت أزرار «صغير/متوسط/كبير» بعد تحميل سورة الفاتحة، وتم اختيار الحجم الكبير أثناء عرض الآيات دون أخطاء تشغيلية أو تغيير في مصدر النص.

- **قوائم السماع:** نجح تفعيل «أسمعه بعدين» للسورة الحالية؛ تغير الزر إلى «إزالة من أسمعه بعدين» وارتفع العداد إلى 1. ثم نجح تفعيل «تم الاستماع»؛ تغير الزر إلى «إلغاء تم الاستماع» وظهر العداد الإجمالي 1، مع بقاء حالة المصحف والنص دون إعادة تحميل.
- **حالة الفراغ في السبورة:** أُنشئت سبورة «اختبار حالة الفراغ» من الواجهة، وظهرت رسالة «السبورة فارغة» داخل الكارت المشترك مع وصف إرشادي عربي.
- **حالة فراغ الأرشيف:** فُتحت صفحة `/archive` مع صفر عناصر، فظهرت رسالة EmptyState العربية «لا توجد عناصر هنا» مع وصف الاستعادة، مع بقاء حقل البحث ومرشحات الأقسام ظاهرين.
- **شاشة الخطأ العامة:** راجعت `app/error.tsx` وأصلحت رابط العودة الذي كان يشير إلى `/dashboard` غير الموجود ليشير إلى `/`، مع نجاح typecheck وESLint والبناء.
- **بوابة الجودة:** أزيل تحذيرا ESLint المتبقيان بإزالة استيراد مالي غير مستخدم وتطبيق Cairo على `body`، ثم نجح TypeScript وESLint بلا تحذيرات والبناء الإنتاجي.
- **صفحة 404:** جرى اختبار مسار غير موجود في خادم التطوير؛ ظهرت صفحة عربية متوافقة مع التصميم مع زر يعمل للعودة إلى `/`.
- **اليوميات:** حملت مساحة اليوميات في المتصفح بعد توحيد حالة الدفتر الفارغ على `EmptyState`؛ بقيت التدوينة المحلية الحالية ورسم المزاج يعملان دون أخطاء.
- **تنظيف بيانات وهمية:** حُذفت مكوّنات الداشبورد القديمة و`lib/mock-data.ts` بعد إثبات عدم استخدامها؛ تحمّلت الصفحة الرئيسية بالبيانات الموحدة، وأظهر فحص وحدة التحكم اتصال HMR فقط دون أخطاء تطبيقية.
- **روابط السياق:** فتحت شارة المشروع «منصة التحكم الشخصي» المسار `/projects#project-1` ووصلت إلى بطاقة المشروع، ثم فتحت شارة الهدف «إطلاق النسخة الأولى من المنتج» المسار `/goals#goal-1` ووصلت إلى بطاقة الهدف ذات المعرّف المطابق.
- **قابلية الوصول للسياق:** أضيفت معرّفات DOM مستقرة لبطاقات المشاريع والأهداف مع الحفاظ على RTL والـsemantic tokens وعدم تغيير بنية العرض.
- **روابط سياق الملاحظات:** أضيفت روابط عميقة من شارات المهمة والمشروع والهدف داخل الملاحظات، وأضيفت anchors مستقرة لبطاقات المهام (`task-*`). فحص DOM أكد وجود anchors المهام وروابط المشروع والهدف الحالية؛ لا توجد ملاحظة مرتبطة بمهمة في بيانات الاختبار المحلية لفتحها مباشرة دون اختلاق بيانات.
- **التذكيرات المتكررة:** أُنشئ تذكير «اختبار التذكير المتكرر» بتكرار يومي، ثم أُكمل من الواجهة؛ بقي ضمن التذكيرات المفتوحة وتحول موعده من «اليوم، ٢٠:٠٠» إلى «غدًا»، مع بقاء عداد التذكيرات المتكررة محدثًا. فحص المتصفح: نجح.
- **تدقيق الخصوصية:** راجعت Route Handlers الشخصية؛ مسارات الحساب والمهام والملاحظات والعادات وخطة اليوم والمال والمشاريع والأهداف والدين المحفوظ والتذكيرات والمراجعة والأرشيف تستخدم `getCurrentUser` وعزل `user_id`. الاستثناءات غير المحمية هي القرآن والقراء ومواقيت الصلاة، وكلها تجلب بيانات خارجية عامة مع حدود مدخلات ومصدر معلن، ولا تقرأ حالة المستخدم. التدقيق الساكن: نجح؛ اختبار الجلسة الإنتاجية الفعلية ما يزال مرتبطًا بالـcredentials.
- **روابط سياق العادات:** فُتحت `/habits` بصريًا وتحقق ظهور نموذج إضافة عادة مع اختيار مهمة ومشروع وهدف والتكرار. أُنشئت عادة اختبارية «تمرين الصباح» مرتبطة بـ`task-3` و`project-3` و`goal-2`؛ ظهرت شارات روابط عميقة إلى `/tasks#task-task-3` و`/projects#project-3` و`/goals#goal-2`. كما أضيفت `EmptyState` عند عدم وجود عادات، مع بقاء localStorage fallback.
- **Onboarding وقيمة أول خطة يوم:** فُتحت `/onboarding` بصريًا؛ ظهرت ثلاث خطوات عربية واضحة (عن يومك، إيقاعك، هدفك) مع RTL وCairo. في جلسة localStorage نظيفة بقي «التالي» معطلًا حتى إدخال الاسم، ثم أمكن تجاوز المدينة والإيقاع الاختياريين والوصول إلى اختيار الاتجاه. صفحة النجاح أظهرت اسم المستخدم والملخص ورسالة «خطوتك التالية المقترحة» ورابط `/daily-plan`. بعد فتحه ظهر Timeline فعلي يضم المهام والعادات والصلاة والورد والراحة، مع الإكمال/التأجيل وروابط سياق المشروع والهدف. أُصلحت مزامنة القيم بعد hydration وأضيف `autoComplete="off"` لمنع browser autofill من عرض اسم قديم دون تحديث state. نجحت بوابة `tsc --noEmit` وESLint بلا تحذيرات و`next build`؛ تحذير تقادم convention الخاص بـmiddleware من Next.js غير حاجز.
- **مراجعة EmptyState — المهام وخطة اليوم:** بعد تفريغ الحالة المحلية تجريبيًا، ظهرت في `/tasks` حالة EmptyState المشتركة «ابدأ بقائمة مهامك» مع بقاء نموذج «مهمة جديدة» متاحًا. وفي `/daily-plan` ظهرت «خطة اليوم جاهزة للإضافة» مع رابط مباشر «أضف أول مهمة» إلى `/tasks`، وحُسب التقدم بأمان `0%` من `0` عناصر. بقي RTL وخط Cairo والكروت الدائرية وSemantic Tokens كما هي.
- **مراجعة EmptyState — الترفيه والمشاريع:** بعد تفريغ عناصر الترفيه والمشاريع تجريبيًا، ظهرت بطاقة «لا يوجد اقتراح بعد» في الترفيه باستخدام النمط المشترك مع بقاء حالات رفوف «عايز أتفرج/بتفرج/خلصت» واضحة. وفي `/projects` ظهرت EmptyState موحدة في أعمدة «قادم/جاري/مكتمل»، مع بقاء نموذج إضافة المشروع وخريطة الإحصاءات ظاهرين.
- **تدقيق LoadingState والحالات الشبكية:** فُحصت wrappers الكسولة لليوميات والترفيه والديني والسبورة، وكلها تستخدم `LoadingState` عربيًا مع `aria-busy="true"` و`role="status"` ونص مخفي للوصول. كما تمت مراجعة حالات المواقيت والقرآن في المساحة الدينية؛ التحميل والخطأ والنجاح رسائل مخصصة مرتبطة بطلبات خارجية، لذلك لم تُستبدل بـEmptyState العامة. أُغلق بند المراجعة الشاملة في `EXECUTION_PLAN.md`.
- **روابط سياق Weekly Review:** أضيفت بطاقة «ارجع إلى السياق» مشتقة من الحالة الحالية، تعرض روابط للمهمة المفتوحة والعادة التالية والمشروع النشط والهدف النشط دون تغيير عقد المراجعة أو إضافة migration. في الاختبار ظهرت الروابط الأربع داخل `/review`، والنقر على «المهمة المفتوحة» نقل إلى `/tasks#task-task-3` ووصل إلى بطاقة «تحضير عرض تقديمي للمشروع» ذات الـanchor المطابق. أضيف anchor مستقر لكل بطاقة عادة في `/habits` لدعم نفس التدفق.
اختبار رابط العادة من Weekly Review نجح؛ النقر على «العادة التالية» فتح `/habits#habit-4` ووضع بطاقة «قراءة كتاب» في موضعها داخل صفحة العادات. أظهر الاختبار أيضًا أن الروابط القائمة للمشاريع والأهداف ظلت ظاهرة داخل بطاقات العادات المرتبطة.

- **البحث الشامل والروابط العميقة:** فُتح البحث من الـShell، وبُحث عن «قراءة كتاب» فظهرت نتيجة مجمعة تحت العادات. النقر عليها نقل إلى `/habits#habit-4` ووضع بطاقة العادة المطابقة في موضعها. أضيفت anchors مستقرة أيضًا للملاحظات وخطة اليوم واليوميات والعمليات المالية والتذكيرات وبطاقات الترفيه، مع الحفاظ على query الشهر/التاريخ عند الحاجة. نجحت `tsc --noEmit` وESLint بلا تحذيرات و`next build` و`git diff --check`؛ تحذير تقادم convention الخاص بـmiddleware بقي معلوماتيًا وغير حاجز.
- **Anchors العناصر الزمنية:** فحص DOM في `/daily-plan` أكد وجود `plan-item-plan-1` حتى `plan-item-plan-6` على عناصر Timeline. ثم فُتح `/journal?date=2026-08-15#journal-journal-1` فاختير تاريخ 15 أغسطس وظهرت بطاقة «بداية هادئة» ذات المعرّف المطابق، ما يؤكد توافق query التاريخ مع hash التدوينة.

- **دفعة المالية والتذكيرات:** ظهرت لوحة الميزانية عند 21% استخدام دون تنبيه، ثم عُدّلت الميزانية محليًا إلى 2,000 جنيه فظهر تنبيه «تجاوزت سقف الميزانية» مع شريط أحمر ورسالة إرشادية، ثم أُعيدت القيمة الأصلية 12,000 جنيه وحُفظت بنجاح. ظهرت شارات «فتح السياق» على التذكيرات المرتبطة، واختُبر الرابط المرتبط بمهمة ووصل إلى `/tasks#task-task-3` مع بقاء البطاقة الصحيحة ظاهرة.

- **روابط سياق المالية:** ظهرت شارات المشروع والهدف على المصروف المتكرر والعملية الأخيرة المرتبطة، مع روابط عميقة إلى `/projects#project-1` و`/goals#goal-1`. النقر على رابط المشروع من `/money` وصل إلى بطاقة «منصة التحكم الشخصي» في `/projects#project-1`، مع بقاء الأرشفة والميزانية والتكرار سليمة. نجحت بوابة `tsc --noEmit` وESLint بلا تحذيرات و`next build` و`git diff --check`.
- **دفعة القسم الديني وPWA:** فُتح `/religious` بصريًا مع RTL، وظهرت حالات المواقيت والمصحف والتلاوة والسبحة والأذكار دون كسر الهوية الحالية. تغيير السورة إلى الكهف (`18`) أعاد تحميل عنوان السورة وعدد الآيات والنص من المصدر الخارجي بنجاح، وبقيت أزرار التشغيل وقوائم السماع وأحجام الخط وإعدادات المواقيت قابلة للوصول. وحدة التحكم خالية من أخطاء React أو hydration؛ ظهرت فقط رسائل React DevTools وHMR المعتادة. تغييرات PWA تشمل تجربة التثبيت/التحديث وأمر تطبيق تحديث Service Worker، مع إعادة محاولة واضحة عند فشل تحميل آيات المصحف. يبقى اختبار Service Worker الكامل معتمدًا على بيئة production/HTTPS.
- **Weekly Review — مؤشرات الدين والمال وروابط المساحات:** ظهرت بطاقات دخل/مصروفات الأسبوع، نسبة الصلوات، الورد، الحفظ، الترفيه المكتمل، وعدد السور المستمعة وقائمة السماع اللاحقة، مع تنسيق RTL سليم. ظهرت روابط المهمة والعادة والمشروع والهدف، إضافة إلى رابطين مباشرين للمساحتين الدينية والمالية. فتح رابط «المساحة الدينية» من Weekly Review المسار `/religious` بنجاح، وظهرت لوحة الصلوات والورد والمصحف دون أخطاء تشغيلية. نجحت بوابة `tsc --noEmit` وESLint و`next build` و`git diff --check`.

- **روابط سياق القسم الديني:** ظهرت روابط «فتح الصلاة في خطة اليوم»، «فتح في خطة اليوم»، و«فتح عادة القرآن» داخل `/religious`. فتح رابط الصلاة بنجاح إلى `/daily-plan#plan-3` ووصل إلى عنصر الصلاة داخل Timeline؛ تستخدم الروابط الأخرى `/daily-plan#plan-4` و`/habits#habit-1`. حافظت الروابط على RTL والهوية الحالية، وأكملت العلاقة بين القسم الديني وخطة اليوم والعادات.

- **مولّد التذكيرات الهادئ:** أضيفت اقتراحات مشتقة من عناصر خطة اليوم المفتوحة والعادات غير المكتملة والمصاريف المتكررة، مع منع التكرار عبر `sourceId`. في اختبار `/reminders` ظهر الوضع الهادئ بثلاثة اقتراحات، ثم أُضيفت الاقتراحات بنجاح كعناصر مرتبطة بالسياق. بعد ذلك أزيلت عناصر الاختبار من localStorage وعادت القائمة إلى أربعة تذكيرات أصلية. نجحت بوابة `tsc --noEmit` وESLint و`next build`؛ تحذير تقادم convention الخاص بـmiddleware بقي معلوماتيًا وغير حاجز.

## 2026-08-16 — Daily Plan source links

- فتحت `/daily-plan` بالحالة المحلية الحالية وتحققت بصريًا من ظهور روابط سياق عربية تحت عناصر المهمة والصلاة والورد والعادة.
- روابط المصادر الظاهرة: `/tasks#task-task-3`، `/habits#habit-1`، `/religious#prayer-tracker`، `/religious#quran-progress`، وروابط المشروع والهدف بصيغة anchors مستقرة.
- فتحت رابط `مهمة: تحضير عرض تقديمي للمشروع` من خطة اليوم؛ انتقل المتصفح إلى `/tasks#task-task-3` وظهر عنصر المهمة مع سياق المشروع والهدف.
- لم يظهر `NaN` أو خطأ hydration أو شاشة مكسورة أثناء الاختبار.
- ما يزال اختبار روابط الصلاة والورد والعادة من المتصفح مطلوبًا قبل إغلاق الدفعة.
- اختبار رابط العادة من خطة اليوم نجح؛ النقر على «عادة: رياضة» فتح `/habits#habit-2` ووضع بطاقة «رياضة» في موضعها، مع بقاء روابط المشروع والهدف داخل البطاقة ظاهرة.
- اختبار `/religious#prayer-tracker` نجح؛ تموضع المتصفح عند بطاقة «صلوات اليوم» وظهرت الصلوات الخمس ورسالة العودة إلى خطة اليوم.
- اختبار `/religious#quran-progress` نجح؛ تموضع المتصفح عند بطاقة «الورد اليومي» مع أزرار زيادة الدقائق وروابط خطة اليوم وعادة القرآن، وبقيت بيانات المصحف الخارجية محمّلة دون خطأ ظاهر.

- **بطاقات الدين في Dashboard:** أضيفت بطاقتا «الصلوات» و«ورد القرآن» إلى `/` بالهوية الحالية وRTL. ظهرت بطاقة الصلاة بنسبة `20%` (1 من 5) مع الصلاة القادمة، وبطاقة الورد بنسبة `40%` (8 من 20 دقيقة) مع المتبقي وشريط التقدم. النقر على صلاة الظهر حدّث العداد إلى 2 من 5 و40% ثم أُعيدت الحالة الأصلية. رابط الصلاة فتح `/religious#prayer-tracker`، ورابط الورد فتح `/religious#quran-progress` ووصل إلى البطاقتين الصحيحين. فحص console أظهر React DevTools وHMR فقط دون أخطاء تشغيل أو hydration. بوابة `git diff --check` وTypeScript وESLint و`next build` نجحت قبل الاختبار.

- **Dashboard — التاريخ والملخص المالي والروابط:** فُتحت `/` بعد نجاح بوابة الجودة. ظهر التاريخ الميلادي مع التاريخ الهجري بصيغة عربية (`الأحد، ١٦ أغسطس · ٣ ربيع الأول ١٤٤٨ هـ`). ظهرت بطاقة «الفلوس» بقيمة المصروف الشهري ونسبة استخدام الميزانية واتجاه المقارنة ورابط `/money`. ظهرت بطاقات الإحصاءات الأربع كروابط إلى `/tasks` و`/daily-plan` و`/habits`، مع بقاء RTL والكروت المستديرة والرموز الدلالية متسقة. لم يظهر `NaN` أو كسر بصري، وبقيت بطاقتا الصلوات وورد القرآن والاقتراحات ظاهرة. شارة Next.js DevTools ظهرت في بيئة التطوير فقط.
- **اختبار روابط Dashboard الجديدة:** النقر على «فتح الفلوس» نقل إلى `/money` وظهرت لوحة الميزانية والمصاريف دون كسر بصري. ثم النقر على بطاقة «إنجاز المهام» نقل إلى `/tasks` وظهرت قائمة المهام العربية مع الفلاتر ونموذج الإضافة كما هو متوقع. لم تُنشأ أو تُعدّل بيانات أثناء الاختبار.

### 2026-08-16 — Habit archive round-trip
- فتحت `/habits` وتحققت من ظهور زر `أرشفة` مستقل لكل بطاقة عادة مع استمرار زر الإكمال وروابط المشروع والهدف.
- أرشفت عادة `قراءة القرآن` مؤقتًا؛ انخفضت القائمة من 5 إلى 4 عادات وتحدثت النسبة من 60% إلى 50% دون كسر الواجهة.
- فتحت `/archive`؛ ظهر العنصر ضمن تصفية `العادات` مع العنوان والتاريخ وزر `استعادة`.
- استعدت العادة بنجاح؛ أصبحت صفحة الأرشيف فارغة وظهر إشعار `تمت استعادة «قراءة القرآن» إلى العادات.`، ثم عادت العادة إلى مساحة العادات.
- النتيجة: دورة الأرشفة والاسترجاع المحلية مكتملة، ولم يتم الاحتفاظ بأي تغيير تجريبي على بيانات الجلسة.

> ملاحظة: ظهرت شارة Next.js DevTools في جلسة التطوير، وهي أداة تطوير وليست خطأً في التطبيق.

### 2026-08-16 — Personal suggestions decisions
- ظهرت بطاقة اقتراح سياقية مع مصدر `ملخص العادات` وسبب عربي واضح لتوليدها.
- نجح زر `اعتماد` في تحويل البطاقة إلى `تم اعتماد الاقتراح` مع زر `تراجع`، ثم أعادها التراجع إلى الحالة القابلة للإجراء.
- فتح `تعديل` حقلًا معنونا؛ عُدّلت الصياغة إلى `أقرأ صفحتين من الكتاب قبل النوم`، ثم أظهر `حفظ التعديل` حالة `تم حفظ التعديل` مع إبقاء التراجع.
- نجح `رفض الاقتراح` في إخفاء البطاقة وإظهار الحالة الفارغة الهادئة، دون تعديل بيانات العادة أو المهام.
- لم يظهر كسر بصري أو خطأ hydration أو خطأ JavaScript أثناء التدفق.


- **شريحة الترفيه:** نجحت بوابة `git diff --check` وTypeScript وESLint والبناء قبل الاختبار. ظهرت مساحة `/entertainment` بالعربية وRTL مع البطاقات الإحصائية، الاقتراح القادم، إحصائيات الشهر، الفلاتر، رفوف الحالات، والقوائم السريعة. نُقلت `Severance` مؤقتًا من «عايز أتفرج» إلى «بتفرج» ثم أُعيدت إلى حالتها الأصلية. أُضيف عمل مؤقت من النموذج، وتحققت البطاقة الكاملة وبياناتها، ثم أُرشف وعادت القائمة إلى حالتها الطبيعية. اختُبر فلتر «مرشّح لحد» وفلتر «عايز أنزله» وظهرت النتائج وحالات الفراغ الصحيحة، ثم أُلغيت الفلاتر دون ترك بيانات اختبارية. لا أخطاء تطبيقية ظاهرة في وحدة التحكم أثناء الاختبار.

### Habit history, Streak, Heatmap, and weekly progress — 2026-08-16
- `http://localhost:3004/habits` فتح بصريًا بعد بوابة الجودة؛ ظهرت بطاقات العادات وشبكات آخر 35 يومًا ومؤشرا إنجاز اليوم والأسبوع دون كسر RTL.
- تم تبديل «قراءة كتاب» من غير مكتملة إلى مكتملة؛ ارتفع إنجاز اليوم من 60% إلى 80%، وإنجاز الأسبوع من 9% إلى 11%، وظهر يوم مكتمل في شبكة العادة، مع تحديث عداد الـStreak من 0 إلى 1.
- أعيدت «قراءة كتاب» إلى حالتها الأصلية؛ عادت النسب إلى 60% و9% والـStreak إلى 0، دون تغيير دائم في بيانات جلسة التطوير.
- النتيجة: نجح المسار المحلي في كتابة history بتاريخ القاهرة، وإعادة حساب الـStreak المتصل، وإظهار Heatmap ومؤشر أسبوعي قابلين للتفسير.

- **Prayer tracker وسجل الشهر:** فُتح `/religious#prayer-tracker` بعد نجاح TypeScript وESLint وBuild. ظهر RTL العربي، مؤشر اليوم، بطاقة الثبات، نسبة آخر 30 يومًا، وعدّاد السجلات، مع تقويم 30 يومًا يميز المكتمل والجزئي وغير المسجل. تبديل صلاة الظهر مؤقتًا رفع اليوم من 1/5 (20%) إلى 2/5 (40%) وغيّر النسبة الشهرية تفاعليًا، ما يؤكد عمل سجل الصلاة المحلي.
- **استعادة حالة prayer tracker:** أُعيد تبديل صلاة الظهر إلى حالتها الأصلية بعد الاختبار؛ عاد العرض إلى 2 من 5 (40%) وعاد التقويم/مؤشر آخر 30 يومًا إلى القيم السابقة، مع استمرار بطاقة الثبات والتقويم دون أخطاء واجهة.

## 2026-08-16 — حالات الصلاة التفصيلية

- **القسم الديني** (`/religious#prayer-tracker`): ظهرت قائمة حالة مستقلة لكل صلاة بخيارات «لم تُسجّل»، «في وقتها»، «جماعة»، «قضاء»، «فائتة».
- **التفاعل**: تم تغيير صلاة الفجر إلى «جماعة»؛ انعكست الحالة فورًا في بطاقة الصلاة مع بقاء العداد `2 من 5` و`40%` لأن الحالة تُعد مكتملة. أُعيدت بعدها إلى «في وقتها» حفاظًا على بيانات جلسة التطوير.
- **Dashboard** (`/`): ظهرت حالات الفجر والظهر كـ«في وقتها» مع أزرار الحالة، وبقيت الصلوات غير المسجلة معروضة كـ«لم تُسجّل».
- **Weekly Review** (`/review`): ظهرت بطاقة «الصلوات المكتملة: 2» ومقاييس الصلاة الأسبوعية دون أخطاء واجهة.
- **الملاحظات**: لم يظهر خطأ hydration أو خطأ تطبيق في المحتوى المستخرج. شارة Next DevTools أظهرت `0 1 Issue` المعلوماتية المعتادة.

**النتيجة:** اختبار المتصفح اليدوي لحالات الصلاة التفصيلية ناجح.

## اختبار أرشفة واستعادة العادات — 2026-08-16

- فتحت `/habits` وتحققت من ظهور العادات وزر الأرشفة لكل عنصر.
- أرشفت «قراءة القرآن»؛ اختفت من قائمة العادات وانخفض العدد من 5 إلى 4، مع بقاء بقية واجهة العادات مستقرة.
- فتحت `/archive`؛ ظهرت «قراءة القرآن» ضمن قسم العادات مع زر «استعادة».
- استعدت العنصر؛ اختفى من الأرشيف وظهرت رسالة نجاح «تمت استعادة قراءة القرآن إلى العادات.»، بما يثبت دورة الأرشفة والاستعادة المحلية.
- لا توجد أخطاء مرئية في الواجهة أثناء الدورة. بوابة TypeScript وESLint وBuild ناجحة قبل الاختبار.

النتيجة: بعد إصلاح Route العادات، أصبح الأرشيف يحفظ العادة نفسها بدل حذف habitLog الخاص باليوم، مع الحفاظ على سلوك حذف سجل الإنجاز اليومي عند استخدام toggle العادي.

- **تحليل أنواع حالات الصلاة:** فُتحت `/religious#prayer-tracker` وتأكد ظهور قائمة الحالات لكل صلاة: لم تُسجّل، في وقتها، جماعة، قضاء، فائتة. غُيّرت صلاة الفجر مؤقتًا إلى «فائتة»، فانعكس العداد من 2/5 إلى 1/5، وظهر في بطاقة الثبات: فائتة = 1، لم تُسجّل = 3، والأكثر فواتًا = الفجر · 1 مرات. أُعيدت إلى «في وقتها»، فعاد العداد إلى 2/5 وتحليل اليوم إلى في وقتها = 2، فائتة = 0، لم تُسجّل = 3. وحدة تحكم المتصفح خالية من أخطاء hydration أو runtime؛ الظاهر فقط React DevTools وHMR.

- **مطابقة Weekly Review مع سجل العادات:** فُتحت `/habits` وسُجل تبديل «قراءة القرآن» من مكتملة إلى غير مكتملة ثم إعادتها إلى مكتملة؛ انعكست القيم محليًا (`2/5` ثم `3/5`) وبقيت العادة ذات `1 يوم مكتمل`. بعد فتح `/review` ظهر «3 إنجازات عادات خلال 1 أيام هذا الأسبوع» ولقطة «إنجازات العادات: 3 · 1 أيام»، ما يؤكد استخدام `doneToday` لليوم الحالي و`history` لبقية الأسبوع. كما أضيف التاريخ المحلي صراحة إلى PATCH/DELETE لمزامنة `habitLog`. فحص وحدة التحكم أظهر HMR وReact DevTools فقط دون أخطاء hydration أو runtime.

- **بوابة الجودة لدفعة Weekly Review:** نجح `tsc --noEmit` وESLint بلا تحذيرات والبناء الإنتاجي. بقي تحذير Next.js المعلوماتي الخاص بتقادم convention الخاص بـmiddleware فقط.

## 2026-08-16 — حفظ سجل العادة داخل الأرشيف

- فُتحت مساحة العادات، وكانت «قراءة القرآن» مكتملة مع سجل يومي ظاهر.
- أُرشفت العادة؛ اختفت من قائمة العادات وانخفض عداد العادات النشطة دون ظهور خطأ في الواجهة.
- فُتح الأرشيف؛ ظهرت «قراءة القرآن» ضمن قسم العادات مع زر الاستعادة.
- أُعيدت العادة؛ ظهرت رسالة نجاح، ثم عادت إلى مساحة العادات مع التكرار والروابط وسجل اليوم الظاهر.
- فحص وحدة التحكم بعد الدورة: لا أخطاء hydration أو runtime؛ ظهرت فقط رسائل React DevTools وHMR المعتادة.
- التغيير الخلفي يقيّد قراءة `habitLog` بسجلات العادات المؤرشفة المملوكة للحساب، ويحفظ `history` و`doneToday` و`streak` في حمولة الأرشيف.

- **رابط سياق العادة من التذكيرات:** أُضيف تذكير محلي مؤقت بمصدر `habit-1`، ثم فُتح «فتح السياق» من `/reminders`. انتقل الرابط إلى `/habits#habit-1` وظهرت بطاقة «قراءة القرآن» في موضعها الصحيح؛ أزيل التذكير المؤقت بعد الاختبار، ولم تظهر أخطاء runtime أو hydration.

- **إصلاح روابط سياق التذكيرات:** عولجت البادئات المكررة للمهام والعمليات المالية؛ أصبحت الروابط `/tasks#task-3` و`/money#finance-1` مطابقة للـanchors الفعلية. أضيف دعم `/religious#prayer-tracker` و`/religious#quran-progress`. أُنشئت أربعة عناصر اختبارية مؤقتة، ظهرت الروابط الصحيحة، وفُتح رابط المالية بنجاح إلى `money#finance-1`. أزيلت العناصر الاختبارية بعد التحقق وعادت القائمة إلى حالتها الأصلية. وحدة التحكم خالية من أخطاء hydration/runtime؛ الظاهر فقط React DevTools وHMR.

### تذكيرات — حفظ تفضيل الوضع الهادئ (2026-08-16)
- فُتحت `/reminders` وتحقق ظهور زر `الوضع الهادئ مفعّل` وثلاثة اقتراحات كحد أقصى.
- غُيّر التفضيل إلى `عرض اقتراحات أكثر`، فظهرت الاقتراحات الإضافية المتاحة.
- أُعيد تحميل الصفحة، فاستُعيد التفضيل من localStorage وبقيت عبارة `عرض اقتراحات أكثر` ظاهرة.
- أُعيد التفضيل إلى الوضع الهادئ بعد الاختبار حتى تبقى جلسة التطوير على الإعداد الافتراضي المحافظ.
- وحدة تحكم المتصفح خالية من أخطاء hydration وruntime؛ ظهر فقط React DevTools وHMR.
- بوابة TypeScript وESLint وBuild و`git diff --check` نجحت قبل الاختبار.

- **حفظ تكرار العملية المالية داخل الأرشيف:** أُرشف مصروف «اشتراك أدوات العمل» الشهري من `/money`، فاختفى من المصاريف المتكررة والعمليات الحالية، ثم ظهر في `/archive` تحت قسم الفلوس. استُعيد العنصر بنجاح، وعند العودة إلى `/money` عاد إلى بطاقة المصاريف المتكررة مع الوسم «شهري» وموعده يوم 12 من كل شهر، ما يؤكد أن `recurrence` لم يُفقد من payload الأرشيف. وحدة تحكم المتصفح أظهرت React DevTools وHMR فقط، دون أخطاء hydration أو runtime.

### اختبار 2026-08-16 — تصدير بيانات الحساب كاملة
فُتحت `/account` في الوضع المحلي، ونُفّذ زر «تنزيل نسخة محلية» بنجاح وظهرت رسالة تأكيد التنزيل. فحصت وحدة التحكم ولم تظهر أخطاء hydration أو runtime. أثناء تدقيق Route `/api/account/export` تبيّن أن حمولة Neon كانت تستبعد سجلات اليوميات والترفيه رغم وجودهما في الحالة المحلية وعقد الاستعادة؛ أُضيف استعلام مملوك بـ`user_id` لكل من `journalEntry` و`entertainmentItem` وأُدرجا في `data` باسمَي `journal` و`entertainment` المتوافقين مع النسخة المحلية، مع قبول المستورد احتياطيًا للمفتاح القديم `journalEntries`. بقيت جلسة المتصفح غير مسجلة، لذلك لم يُنفّذ تصدير Neon الحقيقي، ولم تُدّعَ نتيجة إنتاجية له.

### إصلاح متابعة 2026-08-16 — توافق مفتاح اليوميات في النسخ
بعد مراجعة عقد `normalizeState` تبيّن أن مستورد النسخ يتطلب `journal`، لذلك وُحّد تصدير الحساب على المفتاح `journal` بدل `journalEntries`، وأضيف fallback يستوعب `journalEntries` القديمة. أُعيد تشغيل TypeScript وESLint وBuild و`git diff --check`، ثم أُعيد فتح `/account` وفحص وحدة التحكم؛ ظهرت أدوات النسخ دون أخطاء hydration/runtime.
- **التذكير الشهري المتكرر:** أُنشئ تذكير «اختبار تذكير شهري» من نموذج `/reminders` مع اختيار «شهريًا»، ثم ظهر في قائمة المفتوحة بالشارة نفسها. بعد الضغط على «تم» بقي التذكير مفتوحًا وتحوّل الموعد إلى «الشهر القادم». فحص وحدة التحكم بعد الدورة أظهر React DevTools وHMR فقط دون أخطاء تطبيقية. التفاصيل في `verification/reminder-recurrence-browser.md`.


- **حالات Kanban المشاريع:** فُتحت `/projects` وظهرت أعمدة «أفكار»، «جاري»، «متوقف»، و«مكتمل». تم الضغط على «إيقاف مؤقت» في بطاقة «منصة التحكم الشخصي»، فانتقلت فورًا إلى «متوقف» وتغير الإجراء التالي إلى «إنهاء المشروع»، مع بقاء تفاصيل المشروع والمهام المرتبطة ظاهرة. التحقق شمل localStorage fallback؛ حفظ Neon الفعلي مؤجل لغياب credentials.

- **خطة اليوم — التأجيل والإرجاع:** فُتحت `/daily-plan`، وأُجّل عنصر «تحضير عرض تقديمي للمشروع» بزر «تأجيل»، فتحول بصريًا إلى عنصر مؤجل وظهر زر «إرجاع إلى الخطة». بعد الضغط عليه عاد إلى الحالة النشطة واختفى زر الإرجاع، مع بقاء روابط المهمة والمشروع والهدف. فحص وحدة التحكم أظهر React DevTools وHMR فقط دون أخطاء تطبيقية. التفاصيل في `verification/daily-plan-snooze-browser.md`.

- **خطة اليوم — التخطي والإرجاع:** ظهر زر «تخطي» للعنصر النشط، فتم تخطي «تحضير عرض تقديمي للمشروع» فتحولت البطاقة إلى حالة متخطاة، وارتفع التقدم من 17% إلى 20% لأن المقام استبعد العنصر المتخطى. بعد الضغط على «إرجاع إلى الخطة» عادت البطاقة نشطة، وعاد التقدم إلى 17%، مع بقاء روابط المهمة والمشروع والهدف. وحدة التحكم أظهرت React DevTools وHMR فقط دون أخطاء تطبيقية. التفاصيل في `verification/daily-plan-skip-browser.md`.
- **خطة اليوم — التحرير المباشر:** فُتحت `/daily-plan`، واستُخدم زر «تعديل العنصر» لعنصر «تحضير عرض تقديمي للمشروع». غُيّر العنوان مؤقتًا إلى «تحضير عرض تقديمي للمشروع — تعديل» والوقت إلى `10:30`، فانعكس التعديل فورًا في Timeline وفي بطاقة التركيز الحالية. أُعيدت القيم إلى العنوان الأصلي والوقت `10:00` وحُفظت بنجاح. فحص وحدة التحكم أظهر React DevTools وHMR فقط دون أخطاء JavaScript أو مزامنة. التفاصيل في `verification/daily-plan-edit-browser.md`.

### 2026-08-16 — نقل عنصر خطة اليوم بين التواريخ

- **المسار:** `/daily-plan`
- **الدورة:** نقل «تحضير عرض تقديمي للمشروع» من 2026-08-16 إلى 2026-08-17، التحقق من ظهوره في التاريخ الهدف واختفائه من التاريخ الأصلي، ثم إعادته إلى 2026-08-16.
- **النتيجة:** PASS بعد تصحيح التعبير النمطي في `movePlanItem`؛ كانت المحاولة الأولى ترفض التاريخ بسبب escape مزدوج.
- **التحقق:** TypeScript وESLint و`git diff --check` و`next build` ناجحة، ولا أخطاء تطبيقية في وحدة التحكم بعد إعادة التحميل.
- **الأثر الجانبي:** لا توجد بيانات اختبارية متبقية؛ عادت الخطة إلى حالتها الأصلية.
- **التفاصيل:** `verification/daily-plan-move-browser.md`.

- **حفظ السنن والنوافل:** في `/religious` فُعّلت «صلاة الضحى»، ظهرت علامة الإنجاز، ثم بقيت الحالة بعد إعادة تحميل الصفحة. أصبحت الحالة محفوظة ضمن `religious.dhikr.sunnahChecks` عبر localStorage fallback. فحص console خالٍ من أخطاء تطبيقية؛ رسائل React DevTools وHMR فقط.

## 2026-08-16 — تخصيص هدف الورد اليومي

- فُتحت صفحة `/religious` واختير هدف الورد 30 دقيقة بدل 20.
- انعكس التغيير مباشرة من 8/20 إلى 8/30 مع نسبة 27%.
- أُعيد تحميل الصفحة، فبقي الهدف 30 دقيقة والتقدم 8/30 عبر localStorage fallback.
- فحص console: لا أخطاء JavaScript؛ ظهر اتصال HMR فقط.
- النتيجة: ناجح.

## 2026-08-16 — تحرير المهمة واستعادة البيانات
- **المسار:** `/tasks`
- **الدورة:** تعديل عنوان وموعد وتصنيف وأولوية `task-1` مؤقتًا، حفظ التغييرات والتحقق من انعكاسها في البطاقة، ثم إعادة كل القيم الأصلية وحفظ الاستعادة.
- **النتيجة:** PASS؛ ظهرت القيم المعدلة مباشرة، ثم عادت البطاقة إلى «مراجعة تقرير الشغل الأسبوعي»، «النهاردة»، «شغل»، و«عالية».
- **التحقق:** لا أخطاء تطبيقية في console؛ ظهرت رسائل React DevTools وHMR فقط. التفاصيل في `verification/tasks-edit-browser.md`.
- **النطاق:** تم الاختبار عبر localStorage fallback في غياب credentials؛ عقد `updateTask` الحالي جاهز للمزامنة البعيدة عند توفر قاعدة البيانات.

## 2026-08-16 — خطة حفظ القرآن
- **المسار:** `/religious`
- **الدورة:** ظهرت قائمة حالات السور السبعة داخل بطاقة «خطة الحفظ»، ثم غُيّرت حالة سورة البقرة إلى «حفظتها».
- **النتيجة:** PASS؛ انعكس التغيير مباشرة وارتفع العداد من 2/7 إلى 3/7 سور محفوظة، ثم بقي بعد إعادة تحميل الصفحة.
- **التحقق:** TypeScript وESLint و`git diff --check` و`next build` ناجحة، وفحص console أظهر React DevTools وHMR فقط دون أخطاء تطبيقية.
- **التفاصيل:** `verification/quran-memorization-browser.md`.
- **النطاق:** الاختبار عبر localStorage fallback؛ الحفظ البعيد يستخدم حقل JSONB الحالي دون migration جديدة عند توفر Neon.

## 2026-08-16 — أعمدة زمنية للمهام
- **المسار:** `/tasks`
- **الدورة:** ظهرت أعمدة «النهاردة»، «بكرة»، «الأسبوع ده»، و«بعدين»، ثم نُقلت «حجز موعد الدكتور» إلى «بكرة» بالنقر على زر النقل المباشر، فتغير العدّاد من 4/1 إلى 3/2، ثم أُعيدت إلى «النهاردة» فعادت العدادات الأصلية.
- **النتيجة:** PASS؛ تعمل أزرار النقل المباشر، ويظل السحب والإفلات مدعومًا كمسار إضافي.
- **التحقق:** TypeScript وESLint و`git diff --check` و`next build` ناجحة، وفحص console خالٍ من أخطاء تطبيقية.
- **التفاصيل:** `verification/tasks-time-buckets-browser.md`.
- **النطاق:** localStorage fallback؛ مزامنة النقل تستخدم `updateTask` الحالي عند توفر backend.

## 2026-08-16 — المهام المتكررة وقائمة أقل من دقيقتين
- في `/tasks` ظهرت خيارات التكرار «بدون»، «يومي»، و«أسبوعي» داخل نموذج إنشاء المهمة.
- أُنشئت مهمة اختبار بعنوان «اختبار مهمة يومية» مع تفعيل «يومي»، فظهرت في عمود «النهاردة» مع شارة التكرار العربية.
- ظهرت المهمة الأسبوعية الحالية في «الأسبوع ده» مع شارة «أسبوعية».
- ظهرت بطاقة «أقل من دقيقتين» للمهام المفتوحة منخفضة الأولوية مع زر إكمال مباشر.
- TypeScript وESLint و`git diff --check` و`next build` ناجحة، ولا توجد أخطاء تطبيقية ظاهرة في console.
- التفاصيل: `verification/tasks-recurrence-quick-list-browser.md`.

## 2026-08-16 — الآيات المفضلة والتدبر
- **المسار:** `/religious`
- **الدورة:** حُفظت الآية الأولى من سورة الفاتحة، ثم أُضيف تدبر شخصي «أستحضر رحمة الله وأثبت على طريق الاستقامة.»، وأُخرج التركيز من الحقل لتشغيل الحفظ عند `blur`.
- **النتيجة:** PASS؛ ظهر زر «محفوظة» وبطاقة «آياتي المفضلة» بعدد آية واحدة، ثم بقي المرجع ونص التدبر بعد إعادة تحميل الصفحة.
- **التحقق:** فحص console أظهر رسائل React DevTools وHMR فقط دون أخطاء تطبيقية. التفاصيل في `verification/quran-favorites-browser.md`.
- **النطاق:** التخزين عبر `quranProgress` وJSONB الحالي، مع localStorage fallback ودون migration جديدة.

## 2026-08-16 — مجموعات السبورة وتحويل الورقة
- **المسار:** `/board`
- **النتيجة:** PASS وظيفيًا؛ ظهرت مناطق «التقاط»، «هذا الأسبوع»، و«قرارات»، وأُسندت «فكرة للمنتج» إلى «التقاط» فتغيرت العدادات. حُولت الورقة مؤقتًا إلى مهمة فاختفت من السبورة وظهرت المهمة مع `sourceNoteId`، ثم أُزيلت بيانات الاختبار وأعيدت الورقة إلى لوحة البداية، وتأكدت الإعادة بعد reload.
- **التحقق:** TypeScript وESLint و`git diff --check` و`next build` ناجحة. لا توجد migration جديدة. ظهر تحذير hydration mismatch عام في جلسة التطوير دون أخطاء JavaScript مرتبطة بمنطق السبورة؛ التفاصيل في `verification/board-groups-browser.md`.


## 2026-08-16 — عداد الصلاة وحالات الشبكة

- **الصفحة:** `/religious`.
- **الاختبار:** ظهر عداد الصلاة القادمة وتناقص كل ثانية، ثم انتقل بعد آخر وقت إلى صلاة الفجر مع وسم «غدًا». أعاد Route المواقيت المحلي استجابة AlAdhan صالحة لمدينة القاهرة.
- **اختبار الاعتمادية:** حُوكي انقطاع الشبكة داخل جلسة المتصفح؛ بقيت آخر المواقيت ظاهرة، وظهرت رسالة الخطأ دون انهيار الصفحة أو فقدان البيانات المحلية.
- **النتيجة:** ناجح. فحص console لم يظهر أخطاء JavaScript أو hydration جديدة مرتبطة بالدفعة. التفاصيل في `verification/prayer-countdown-browser.md`.


## 2026-08-17 — تحرير الملاحظات inline وإصلاح hydration

فُتحت `/notes` واختُبر زر `تعديل الملاحظة` على الملاحظة الأولى. غُيّر العنوان والمحتوى والتصنيف إلى «تطوير»، ثم حُفظت القيم وانعكست مباشرة في البطاقة. فُتح تحرير الملاحظة الثانية ثم أُلغي، فعادت إلى وضع العرض دون تغيير. بعد إعادة تحميل الصفحة بقي التعديل محفوظًا عبر localStorage fallback. أثناء الاختبار الأول ظهر React hydration Issue لأن المتجر كان يقرأ localStorage أثناء render؛ عولجت التهيئة بتحميل defaults أثناء SSR وlocalStorage داخل effect، ثم اختفى Issue بعد إعادة الفتح. وحدة التحكم بعد الإصلاح أظهرت React DevTools وHMR فقط دون أخطاء JavaScript أو hydration أو runtime. نجحت بوابات TypeScript وESLint و`git diff --check` و`next build`. التفاصيل في `verification/notes-inline-edit-browser.md`.


## 2026-08-17 — عداد الصلاة في Dashboard

فُتحت الصفحة الرئيسية `/` وتحققت بصريًا من بطاقة «الصلوات» وظهور الصلاة القادمة وموعدها ووسم «غدًا» عند الانتقال لليوم التالي. قُرئت قيمة العداد من عنصر `aria-label`؛ انتقلت من `7س 36د 11ث` إلى `7س 36د 10ث` بعد 1.5 ثانية، ما يثبت التحديث كل ثانية. بقيت أزرار الصلوات وروابط متابعة الصلاة ظاهرة ضمن RTL، ولم تظهر أخطاء JavaScript أو hydration. نُقل منطق الحساب إلى utility مشتركة تستخدمها Dashboard و`/religious`. التفاصيل في `verification/dashboard-prayer-countdown-browser.md`.


- **فلاتر المهام:** في `/tasks` ظهر فلتر الأولوية وفلتر التصنيف إلى جانب فلاتر الحالة والبحث. اختيار «أولوية عالية» حصر النتائج في المهام عالية الأولوية، ثم اختيار «شغل» حصر التقاطع في المهمة المصنفة «شغل» فقط. زر «مسح الفلاتر» أعاد توزيع المهام الأصلي داخل الأعمدة الزمنية دون تعديل البيانات. ظهرت واجهة RTL والكروت والشارات وروابط السياق دون أخطاء runtime. التفاصيل في `verification/tasks-filters-browser.md`، ونجحت بوابات TypeScript وESLint و`git diff --check` و`next build`.


- **وسوم الملاحظات المتعددة:** أُنشئت ملاحظة اختبارية بثلاثة وسوم («شخصي»، «شغل»، «تطوير»)، وظهرت الوسوم منفصلة داخل البطاقة. فلتر «شغل» أبقى الملاحظة المطابقة، ثم أبقى البحث عن «اختبار الوسوم» النتيجة نفسها مع استمرار الفلتر. بعد إعادة التحميل عادت جلسة التطوير إلى بيانات backend الحالية، ولم تظهر أخطاء Runtime أو hydration. التوثيق التفصيلي في `verification/notes-tags-browser.md`. بوابات TypeScript وESLint و`git diff --check` و`next build` نجحت.

## 2026-08-17 — Quick Capture وتثبيت الملاحظات
- في `/notes` أُدخل عنوان عربي داخل حقل «التقاط سريع» وضُغط `Enter`، فتم إنشاء الملاحظة فورًا بالمحتوى الافتراضي عند ترك التفاصيل فارغة. أُضيف guard يعتمد `nativeEvent.isComposing` حتى لا ينهي Enter تركيب IME مبكرًا.
- تم تثبيت الملاحظة الجديدة، فتحول الزر إلى «إلغاء التثبيت» وانتقلت البطاقة إلى مقدمة النتائج. بقيت البطاقات الأخرى وواجهة RTL سليمة، ولم تظهر أخطاء JavaScript أو hydration.
- التفاصيل: `verification/notes-quick-capture-browser.md`.
- الحالة: ناجح. بوابات TypeScript وESLint و`git diff --check` و`next build` ناجحة.

## 2026-08-17 — فلترة ألوان السبورة
- فُتح `/board` واختُبر مرشح اللون الأزرق، فعرض الورقة الزرقاء فقط مع بقاء أدوات Zoom وPan ومجموعات السبورة متاحة.
- أُعيدت «كل الألوان» فعادت الأوراق الثلاث، ثم اختُبر فلتر «التقاط» وتقاطع «التقاط + أزرق» الذي عرض حالة عدم وجود نتائج دون تغيير البيانات.
- اختُبر التكبير إلى 110% ثم «إعادة ضبط» إلى 100%. Console احتوى على React DevTools وHMR فقط دون أخطاء JavaScript أو hydration.
- التفاصيل: `verification/board-color-filter-browser.md`.

## 2026-08-17 — الاستعادة الجماعية في الأرشيف
فُتحت `/archive`، وتم تحديد كل النتائج الظاهرة؛ ظهر العداد `(1 محدد)` وتفعّل زر «استعادة المحدد (1)». بعد التنفيذ اختفت البطاقة، أصبح العداد `0 عنصر مؤرشف`، وظهرت رسالة «تمت استعادة 1 عناصر من الأرشيف.» بقيت الفلاتر والبحث والاستعادة الفردية سليمة. التفاصيل في `verification/archive-bulk-restore-browser.md`. نجحت بوابات TypeScript وESLint و`git diff --check` و`next build`.


## 2026-08-17 — حجم ورقة السبورة
- فُتحت `/board` بصريًا؛ ظهرت خيارات «صغير/كبير» في نموذج إضافة Sticky Note وعلى كل بطاقة ورقة، مع استمرار RTL والكروت الدائرية وأدوات Zoom/Pan.
- أُنشئت ورقة «ورقة اختبار كبيرة» بعد اختيار «كبير»، وظهر الكارت بعرض واسع مقارنة بالأوراق الصغيرة.
- تم تبديل الحجم من بطاقة الورقة، ثم فحص `personal-command-center-board-v2` في localStorage؛ أكد الفحص حفظ `size: large`، بينما بقيت ورقة قديمة بلا حجم مطبّقًا عليها fallback «صغير».
- أُعيد تحميل `/board`، فبقيت الورقة الكبيرة بحجمها وخياراتها ظاهرة، دون خطأ JavaScript أو hydration ظاهر. لم تُترك تغييرات خارجية أو backend وراء الاختبار.


## 2026-08-17 — Badge أولوية المهام

- **المسار:** `/tasks`.
- **الدورة:** فُتحت صفحة المهام وتحققت بصريًا من ظهور الشارات العربية «عالية»، «متوسطة»، و«منخفضة» داخل بطاقات المهام، مع نقطة لون وحدود وخلفيات متوافقة مع Semantic Design System.
- **الفلاتر:** عند اختيار «أولوية عالية» ظهرت مهمتان وشارتان `أولوية عالية`؛ وعند اختيار «أولوية متوسطة» ظهرت ثلاث مهام وشارات `أولوية متوسطة`؛ وعند اختيار «أولوية منخفضة» ظهرت مهمتان وشارتان `أولوية منخفضة`.
- **فحص DOM:** أكدت وحدة التحكم وجود `aria-label` عربي لكل شارة، وتطبيق الكلاسات `bg-warning/15` للعالية، `bg-accent` للمتوسطة، و`bg-muted` للمنخفضة. لم تختفِ الشارة على العرض الحالي، وبقي RTL والأعمدة ونموذج إنشاء المهمة والفلاتر سليمة.
- **النتيجة:** PASS؛ لم تظهر أخطاء تطبيقية أو hydration أثناء الاختبار، ولم تُترك بيانات اختبارية جديدة.
- **التفاصيل:** `verification/tasks-priority-badge-browser.md`.

## 2026-08-17 — قرار Weekly Review قابل للتنفيذ
- فُتحت `/review` وأُدخل قرار عربي في حقل «ما هدف الأسبوع القادم؟»، ثم استُخدم زر «أضف كهمة للأسبوع» من بطاقة «قرار الأسبوع القادم».
- تم إنشاء مهمة بالفئة `مراجعة أسبوعية` والأولوية المتوسطة وموعد «الأسبوع ده»، وظهر رابطها ضمن «ارجع إلى السياق».
- حفظ الزر المراجعة كمسودة تلقائيًا قبل إنشاء المهمة، فبقي القرار محفوظًا بدل فقدانه عند مغادرة الصفحة.
- بعد إعادة التحميل بقي القرار في الحقل، وتحول الزر إلى «أضيفت إلى المهام» وأصبح disabled. فحص DOM وlocalStorage أكد وجود نسخة واحدة فقط من المهمة.
- لم تظهر أخطاء JavaScript أو hydration، وبقيت RTL والهوية البصرية الحالية سليمة. التفاصيل في `verification/weekly-review-actionable-goal-browser.md`.
- **النتيجة:** PASS. TypeScript وESLint و`git diff --check` نجحت؛ ما زال `next build` مطلوبًا قبل commit.

## 2026-08-17 — حفظ آخر آية مقروءة في المصحف
- فُتحت `/religious` وانتظر الاختبار حتى اكتمل تحميل سورة الفاتحة وظهرت أزرار «احفظ الموضع» بجوار الآيات.
- حُفظت الآية الثانية؛ تغيّر الزر إلى «آخر موضع»، وظهر في بطاقة التلاوة «موضع محفوظ: السورة 1 · الآية 2 · 0:00» مع إبقاء أزرار المفضلة والصوت سليمة.
- فحص DOM أكد `aria-pressed="true"` للآية الثانية و`false` لبقية الآيات، مع aria-label عربي واضح.
- بعد إعادة التحميل بقي الموضع محفوظًا في `personal-command-center-state-v2` وبقيت الآية الثانية محددة كآخر موضع.
- نُظفت قيمة الاختبار من localStorage فقط بعد التحقق، دون المساس بباقي الحالة. التفاصيل في `verification/quran-last-read-ayah-browser.md`.
- **النتيجة:** PASS للاختبار التفاعلي والمحلي؛ TypeScript وESLint و`git diff --check` نجحت قبل الفحص البصري.

## 2026-08-17 — استئناف القراءة من آخر موضع محفوظ
- **المسار:** `/religious`.
- **السيناريو:** بعد وجود موضع محفوظ في سورة الفاتحة، غُيّر محدد السورة مؤقتًا ثم تم الضغط على «أكمل من حيث توقفت».
- **التحقق:** عاد محدد السورة إلى القيمة `1`، وبقيت بطاقة «موضع محفوظ: السورة 1 · الآية 2 · 0:00» ظاهرة، واحتفظ زر الآية الثانية بنص «آخر موضع» و`aria-pressed="true"`. أُوقفت التلاوة بعد الاختبار.
- **النتيجة:** PASS بصريًا وتفاعليًا ومحليًا، مع نجاح TypeScript وESLint و`git diff --check` و`next build` (`BUILD_EXIT=0`). التفاصيل في `verification/quran-last-read-ayah-browser.md`.

## دفعة أنواع الورد اليومي — 2026-08-17

- **المسار:** `/religious`.
- **الاختبار:** تبديل نوع الورد إلى الصفحات ثم زيادة التقدم صفحة واحدة، وتغيير الهدف إلى 5 صفحات؛ ظهر الحساب `1 من 5 صفحة` بنسبة `20%` والمتبقي `4 صفحات`. جرى اختبار نصف الجزء والجزء الكامل، ثم أُعيد الوضع إلى الدقائق.
- **الاستمرارية:** بعد إعادة تحميل القسم الديني بقي `wirdMode: "minutes"` والهدف `10 دقائق` والتقدم `8 من 10 دقيقة`، مع احتفاظ الحالة المحلية بالتقدم الصفحي المنفصل.
- **التحقق:** لم تظهر أخطاء تطبيقية في وحدة التحكم، وبقيت واجهة الورد متوافقة مع RTL والهوية البصرية الحالية. لم تُترك بيانات اختبارية بعد الفحص.

## 2026-08-17 — جدول مراجعة خطة حفظ القرآن
- **المسار:** `/religious`.
- **السيناريو:** التحقق من ظهور جدول «السورة / المراجعة / الخطة» أسفل حالات السور، ثم تغيير سورة البقرة من «حفظتها» إلى «تحتاج مراجعة».
- **التحقق:** ظهر الجدول داخل بطاقة خطة الحفظ وباتجاه RTL؛ تحولت صف البقرة فورًا إلى «غدًا / مراجعة قريبة»، وانخفض عداد المحفوظ من 3 إلى 2، ثم أعيدت الحالة إلى «حفظتها» بعد الاختبار.
- **الاستمرارية:** لا يعتمد الجدول على نص القرآن أو migration جديدة؛ الحالة الحالية تُقرأ من مخزن القسم الديني وتبقى متوافقة مع localStorage والمزامنة الحالية.
- **النتيجة:** PASS بصريًا وتفاعليًا، دون أخطاء runtime أو hydration. التفاصيل في `verification/quran-review-schedule-browser.md`.

## 2026-08-17 — فلترة المهام حسب الحالة
- **المسار:** `/tasks`.
- **السيناريو:** استخدام فلتر «فلترة حسب الحالة» لاختيار «جارية»، ثم «لم تبدأ»، ثم «مكتملة»، وأخيرًا إعادة الاختيار إلى «كل الحالات».
- **التحقق:** عرض «جارية» مهمة «تحضير عرض تقديمي للمشروع» فقط؛ عرض «لم تبدأ» أربع مهام غير مكتملة؛ وعرض «مكتملة» المهمتين المنجزتين مع أزرار «إعادة فتح المهمة». عاد العرض الكامل بعد اختيار «كل الحالات».
- **النتيجة:** PASS بصريًا وتفاعليًا، مع بقاء فلاتر الأولوية والتصنيف وروابط المشروع والهدف سليمة، ودون تعديل بيانات المهام. التفاصيل في `verification/tasks-status-filter-browser.md`.


- **ترتيب خطة اليوم حسب الوقت والأولوية:** على `/daily-plan` تم اختيار التاريخ التجريبي `2026-08-16`. ظهرت رسالة عربية تشرح أن الترتيب حسب وقت العنصر ثم أولوية المهمة عند تساوي الوقت، مع عدم تغيير المواعيد تلقائيًا. ظهرت العناصر من 08:30 حتى 20:39 بالترتيب الزمني، وبقيت روابط السياق وأزرار الإكمال والتأجيل والتخطي والتعديل والنقل. نجحت TypeScript وESLint و`git diff --check` و`next build` قبل الاختبار، ولم تظهر أخطاء runtime أو hydration في الحالة المرئية. التفاصيل في `verification/daily-plan-order-browser.md`.

## 2026-08-17 — تصفح القرآن بالأجزاء
- **المسار:** `/religious`.
- **السيناريو:** تبديل طريقة تصفح المصحف إلى «جزء»، ثم التحقق من الجزء الأول، عدد الآيات، وعرض أسماء السور داخل قائمة آيات الجزء.
- **التحقق:** ظهر محدد الأجزاء 1–30، وجُلب «الجزء 1 · 148 آية» من AlQuran Cloud، مع عرض الآيات العابرة للسور واسم السورة لكل مجموعة. اختفى «احفظ الموضع» في نمط الجزء لحماية مرجع الموضع السورِي، بينما بقيت المفضلة مرتبطة بالسورة والآية.
- **التوافق الرجعي:** بقي «أكمل من حيث توقفت» يعيد نمط السورة، وبقيت أدوات القارئ والتلاوة وحجم الخط والواجهة RTL كما هي.
- **النتيجة:** PASS بصريًا وتفاعليًا ومحليًا، مع نجاح TypeScript وESLint و`git diff --check` و`next build`. التفاصيل في `verification/quran-juz-browser.md`.

- **Onboarding — تهيئة عادة وورد من البداية:** فُتحت `/onboarding` وتحققت مؤشرات المراحل الأربع: «عن يومك»، «إيقاعك»، «هدفك»، «بدايتك». أُدخل اسم تجريبي، ثم اختيرت عادة «قراءة يومية» ونمط الورد «نصف جزء»؛ حدّثت الواجهة الهدف تلقائيًا إلى «نصف جزء · 10 صفحات». الضغط على «ابدأ مساحتي» أظهر شاشة «تم حفظ إعداداتك» مع ملخص المدينة وبداية اليوم والاتجاه والعادة والورد وروابط خطة اليوم ولوحة التحكم. لم تظهر أخطاء runtime أو hydration. الدليل التفصيلي: `verification/onboarding-habits-wird-browser.md`.


## فهرس المصحف الكامل والنص العثماني

1. افتح `/religious` وانتظر اكتمال حالة فهرس السور؛ يجب ظهور رسالة نجاح الفهرس من المصدر الموثوق بدل القائمة المختصرة.
2. افتح قائمة «السورة» وتحقق من وجود السور 1–114 بالعربية، ثم اختر السورة 114؛ يجب أن يتحول العنوان إلى «سُورَةُ النَّاسِ · 6 آية» وتظهر الآيات الست.
3. تحقق بصريًا من ظهور النص القرآني بالتشكيل العثماني، ومن بقاء نمط «جزء» والقارئ والتلاوة والمفضلة وحفظ موضع القراءة متوافقة.
4. في حالة تعذر Route الفهرس أو المصدر الخارجي، يجب أن تبقى القائمة الاحتياطية المحدودة ظاهرة مع رسالة عربية واضحة، دون crash أو تخزين نص القرآن محليًا.
5. النتيجة: نجح الاختبار على `http://localhost:3004/religious`، بما في ذلك الفهرس الكامل، اختيار السورة 114، وعرض نص سورة الناس بالتشكيل.

## 2026-08-17 — ترحيل ألوان أوراق السبورة إلى Semantic Tokens
- **المسار:** `/board`.
- **السيناريو:** فتح لوحة البداية، التحقق من ظهور الأوراق الأصفر والأزرق والأخضر، ثم اختيار فلتر «أصفر» وإعادة الفلتر إلى «كل الألوان».
- **التحقق:** ظهرت أربع أوراق مع بقاء النصوص والأزرار ومحددات الحجم والمجموعة مقروءة. عرض فلتر «أصفر 2» ورقتي «ورقة اختبار كبيرة» و«فكرة للمنتج» فقط، ثم عاد العرض الكامل بعد «كل الألوان».
- **التوافق:** لم تتغير بنية كروت السبورة أو أحجام الأوراق أو أزرار التحويل والأرشفة؛ التغيير محصور في استبدال قيم الألوان المباشرة بتوكنز دلالية معرفة في `app/globals.css`.
- **النتيجة:** PASS بصريًا وتفاعليًا، مع نجاح TypeScript وESLint و`git diff --check` و`next build`. التفاصيل في `verification/board-semantic-colors-browser.md`.

## 2026-08-17 — ألوان تقويم الصلاة الدلالية

- **المسار:** `/religious`
- **الهدف:** التحقق من ترحيل حالة اليوم الجزئية من ألوان Tailwind مباشرة إلى `bg-partial` و`border-partial-foreground/35` دون كسر التقويم أو الحالات الأخرى.
- **الخطوات:** فتح القسم الديني، تغيير صلاة الظهر مؤقتًا إلى «في وقتها» لإنشاء حالة جزئية، فحص خلايا التقويم وComputed Styles، ثم إعادة الصلاة إلى «لم تُسجّل» لتنظيف الأثر.
- **النتيجة:** ظهرت خلايا الأيام الجزئية بالصنف الدلالي واللون المحسوب `rgb(255, 243, 196)`، وبقيت الحالات المكتملة وغير المسجلة متوافقة بصريًا، ولم تظهر أخطاء runtime أو hydration.
- **التحقق:** TypeScript وESLint و`git diff --check` و`next build` ناجحة، مع التحذير المعلوماتي المعتاد الخاص بـmiddleware convention.
- **الحالة:** ناجح؛ لا توجد بيانات تجريبية متروكة في جلسة المتصفح.

## StatCard المشترك في Dashboard — 2026-08-16

فُتحت الصفحة الرئيسية `/` وتحققت بصريًا من ظهور كروت «إنجاز المهام»، «خطة اليوم»، «العادات»، و«أعلى Streak» ضمن شبكة RTL. احتفظت الكروت بالزوايا المستديرة، خلفية `bg-card`، شارة التفاصيل، وحالات hover/focus المتوافقة مع Semantic Tokens الحالية. نُقر على كارت «إنجاز المهام» فانتقلت الصفحة بنجاح إلى `/tasks`، وظهرت صفحة المهام وفلاترها دون أخطاء runtime أو hydration. لم تُعدّل بيانات المستخدم أثناء الاختبار.

## Design System Reference + Shared Dialog — 2026-08-16

فُتحت `http://localhost:3004/design-system` وتحققت من ظهور StatCard وContentCard وButton variants والشارات وحقول الإدخال وEmptyState وLoadingState ضمن تخطيط عربي RTL. ظهرت البطاقات الأربع بروابطها ومؤشرات الألوان الدلالية كما هو متوقع. فُتحت نافذة Dialog من زر المعاينة، وتحققت من overlay والعنوان والأزرار، ثم أُغلقت عبر `Escape` بنجاح دون أخطاء runtime أو hydration. أُعيدت الصفحة إلى حالتها الأصلية، ونتيجة التحقق التفصيلية محفوظة في `verification/design-system-browser.md`.

## Quick Add بعد إعادة استخدام Dialog المشترك — 2026-08-16
- **المسار:** `/`.
- **السيناريو:** فتح «إضافة سريعة» من الشريط العلوي، التحقق من overlay والعنوان والوصف وتبويبات مهمة/ملاحظة/مصروف/فيلم وحقل الإدخال واختيار المشروع، ثم إغلاق النافذة عبر `Escape`.
- **النتيجة:** ظهر Dialog المشترك بصريًا داخل تخطيط RTL، وأُغلقت النافذة عبر `Escape` وعادت الصفحة الرئيسية دون أخطاء runtime أو hydration.
- **سلامة البيانات:** لم يتم الضغط على الحفظ أو المراجعة ولم تتغير أي مهمة أو ملاحظة أو حركة مالية أو عنصر ترفيه.
- **التحقق:** TypeScript وESLint و`git diff --check` و`next build` ناجحة. التفاصيل في `verification/quick-add-dialog-browser.md`.

---

## ملاحظات تنفيذية
- Dialog المشترك يتولى Escape والإغلاق بالنقر على الـoverlay وزر الإغلاق.
- Quick Add يحتفظ بدالته الخاصة `closeQuickAdd` لمسح مسودة الإدخال عند الإغلاق، بينما يدير Dialog حالة العرض فقط.
- لم تُضاف أي أسرار أو بيانات اعتماد إلى المستودع.

**الحالة: PASS.**

_تم التسجيل في 2026-08-16 وفق تاريخ جلسة المتصفح المحلي._

## Cairo وMetadata العربية في Foundation — 2026-08-16
فُتحت الصفحة الرئيسية على `http://localhost:3004/` وتحققت بصريًا من بقاء تخطيط Dashboard والكروت والتنقل العلوي RTL دون تغيير. أظهر DOM العنوان «المساحة الشخصية | مركز القيادة»، ووصف الصفحة العربي، و`lang="ar"` و`dir="rtl"`، كما أظهرت CSS المحسوبة استخدام Cairo وخلفية `bg-background` المطابقة للتوكن. لم تُجرَ أي تغييرات على بيانات المستخدم، ولم تظهر أخطاء runtime أو hydration. التفاصيل في `verification/foundation-font-metadata-browser.md`.

**الحالة: PASS.**

## تنظيف الألوان المباشرة في Design System — 2026-08-16
فُتحت صفحة `/design-system` وتحققت بصريًا من بقاء رأس الصفحة الداكن والنصوص الثانوية والمكونات المشتركة متوافقة مع الهوية. فُتحت نافذة Dialog التجريبية ثم أُغلقت عبر `Escape`، وعاد الـoverlay إلى حالته الأصلية. أظهر المسح الساكن عدم وجود أنماط `bg-white` أو `text-white` أو `border-white` أو ألوان Tailwind مباشرة شائعة في كود التطبيق. التفاصيل في `verification/design-system-semantic-color-cleanup-browser.md`.

**الحالة: PASS.**

## نافذة التذكيرات — Dialog المشترك — 2026-08-16
فُتحت `/reminders` بصريًا، ثم فُتحت نافذة «تذكير جديد» وتحققت الحقول العربية للعنوان والنوع والموعد والتكرار وزرا الإلغاء والحفظ. أُغلقت النافذة عبر `Escape` دون إرسال النموذج أو تغيير عدادات التذكيرات، وعاد الـoverlay إلى حالته الأصلية. التفاصيل في `verification/reminders-dialog-browser.md`.

**الحالة: PASS.**

## البحث الشامل — Dialog المشترك — 2026-08-16
فُتحت الصفحة الرئيسية ثم نافذة البحث الشامل من التنقل العلوي. أُدخلت كلمة «مهمة»، فظهرت نتائج من «المهام» و«خطة اليوم» داخل النافذة، ثم أُغلقت عبر `Escape` فعادت الصفحة الأصلية واختفى الاستعلام دون تنفيذ تنقل أو تعديل بيانات. التفاصيل في `verification/global-search-dialog-browser.md`.

**الحالة: PASS.**

## مكونات الحقول والشارات المشتركة — 2026-08-16
فُتحت `/design-system`، وأُدخل «نص تجريبي» في Input، وتغيّر Select إلى «هذا الأسبوع»، وظهرت الشارات الدلالية. فُتح Dialog المرجعي وأُغلق عبر `Escape` دون أخطاء أو تغييرات بيانات. التفاصيل في `verification/shared-fields-badge-browser.md`.

**الحالة: PASS.**

## توكنات ألوان المراجعة الأسبوعية — 2026-08-16
فُتحت `/review` وتحققت بطاقات المؤشرات الأربع بصريًا داخل RTL بعد نقل اللونين البرتقالي والبنفسجي إلى توكنات `insight` الدلالية. راجعت console ولم تظهر أخطاء runtime أو hydration. التفاصيل في `verification/weekly-review-semantic-colors-browser.md`.

**الحالة: PASS.**

## اعتماد الحقول المشتركة في المساحات — 2026-08-16
فُتحت `/religious` وتحققت حقول قائمة التلاوة وتدبر الآيات والمدينة وطريقة الحساب بعد اعتماد `Input` و`Select` و`Textarea` المشتركة، مع اختيار «مخصص» الحالي دون تغيير بيانات مقصود. ثم فُتحت `/review` وتحققت حقول المراجعة الثلاثة بعد اعتماد `Textarea` المشترك. لم تظهر أخطاء runtime أو hydration في console. التفاصيل في `verification/shared-fields-adoption-browser.md`.

**الحالة: PASS.**

## ألوان مخطط المال الدلالية — 2026-08-16
فُتحت `/money` وتحققت بصريًا من donut توزيع المصروفات ووسيلة التصنيفات والشرائط بعد نقل الألوان إلى توكنات `chart` الدلالية. بقي RTL والبيانات والنسب كما هي، ولم تظهر أخطاء runtime أو hydration في console. التفاصيل في `verification/money-chart-semantic-colors-browser.md`.

**الحالة: PASS.**

## نموذج المصادقة بالمكونات المشتركة — 2026-08-16
فُتحت `/login` وتحققت حقول البريد وكلمة المرور وزر الدخول بعد اعتماد `Input` و`Button` المشتركين. بدّلت إلى إنشاء الحساب فظهر حقل الاسم وتغير الزر دون إرسال النموذج أو استخدام بيانات شخصية. التفاصيل في `verification/auth-form-shared-controls-browser.md`.

**الحالة: PASS.**

## البحث الشامل مع Input المشترك — 2026-08-16
فُتح البحث الشامل من الصفحة الرئيسية، وأُدخلت كلمة «مهمة» فظهرت نتيجتان في «المهام» و«خطة اليوم». أُغلق Dialog عبر Escape فعادت الصفحة دون تنقل أو استعلام عالق. التفاصيل في `verification/global-search-input-adoption-browser.md`.

**الحالة: PASS.**

## الحساب مع مكونات الحقول المشتركة — 2026-08-16
فُتحت `/account`، وظهر اعتماد `Input` و`Textarea` و`Button` المشتركين في تفضيلات المساحة. أُدخل «اسم تجريبي» مؤقتًا في حقل الاسم ثم أُعيدت القيمة الأصلية دون الضغط على حفظ، ولم تُنفذ أي عملية بيانات حساسة. التفاصيل في `verification/account-shared-controls-browser.md`.

**الحالة: PASS.**

## الملاحظات مع مكونات الحقول المشتركة — 2026-08-16
فُتحت `/notes`، وأُدخل «مشروع» في البحث فظهرت الملاحظة المطابقة، ثم اختير وسم «تطوير» من Select وظهر «مسح الفلتر» مع بقاء النتيجة الصحيحة. لم تُنشأ ملاحظة ولم تُعدّل بيانات. التفاصيل في `verification/notes-shared-controls-browser.md`.

**الحالة: PASS.**
## Checkbox المشترك في الأرشيف ومرجع النظام — 2026-08-17
فُتحت `/archive` وتحقق ظهور Checkbox التحديد الجماعي مع تعطيله عند خلو النتائج، ثم فُتحت `/design-system` وتحقق ظهور Checkbox «تفعيل التذكير» وتبديل حالته دون تعديل بيانات. أكد فحص DOM وجود `data-slot="checkbox"` والكلاسات الدلالية وحالة `disabled` الصحيحة في الأرشيف. التفاصيل في `verification/shared-checkbox-archive-browser.md`.
**الحالة: PASS.**


- **مساحة الترفيه ومكونات الحقول المشتركة:** فُتحت `/entertainment` بصريًا، وأُدخلت `دراما` في البحث ثم اختير تصنيف `دراما` من Select المشترك، فتحدثت النتائج والعدادات دون تعديل بيانات. فُتح نموذج إضافة فيلم أو مسلسل، فظهرت Input وSelect وTextarea وCheckbox المشتركة، وفُعّل Checkbox الترشيح ثم أُلغي النموذج دون حفظ. console خالٍ من أخطاء runtime أو hydration؛ ظهرت فقط رسائل Fast Refresh وHMR الاعتيادية. التوثيق التفصيلي في `verification/entertainment-shared-controls-browser.md`.


- **مساحة الأهداف ومكونات الحقول المشتركة:** فُتحت `/goals` بصريًا، وظهرت الحقول المشتركة في نموذج «هدف جديد». فُتحت تفاصيل هدف قائم، ثم وضع التعديل، وأُدخل نص عربي مؤقت في Textarea وأُعيدت القيمة الأصلية دون حفظ. ظهر حقل إضافة المهمة وButton المشترك، وبقي RTL والتخطيط كما هما ولم تُنشأ بيانات اختبارية. التوثيق التفصيلي في `verification/goals-shared-controls-browser.md`.


## مساحة العادات — اعتماد الحقول المشتركة

- **المسار:** `/habits`
- **السيناريو:** فتح نموذج «عادة جديدة»، إدخال اسم عربي مؤقت، تغيير Select المشروع إلى «روتين الحركة والنوم» ثم إعادته إلى «بدون مشروع»، وإفراغ الاسم قبل الإغلاق.
- **النتيجة:** نجح العرض والتفاعل في RTL، وبقيت القيم محلية دون حفظ أو إنشاء عادة جديدة.
- **التحقق الإضافي:** أُغلق النموذج بزر الإغلاق، ثم عادت قائمة العادات إلى حالتها الأصلية.
- **المرجع:** `verification/habits-shared-controls-browser.md`

## القسم الديني — اعتماد Select وInput المشتركة
- **المسار:** `/religious`
- **السيناريو:** تغيير هدف السبحة مؤقتًا إلى `100` ثم إعادته إلى `33`، وإدخال دعاء عربي مؤقت في «دعاء جديد» ثم إفراغه دون حفظ.
- **النتيجة:** نجح العرض والتفاعل في RTL؛ أكد فحص DOM وجود `data-slot="select"` و`data-slot="input"` والكلاسات الدلالية، وعاد الهدف إلى `33` والحقل إلى قيمة فارغة. لم يظهر خطأ Next ظاهرًا ولم تُنشأ بيانات تجريبية.
- **المرجع:** `verification/religious-shared-controls-adoption-browser.md`
**الحالة: PASS.**

## أزرار مساحة الترفيه مع Button المشترك — 2026-08-16
فُتحت `/entertainment`، وفُعّل فلتر «مرشّح لحد» ثم أُعيد إلى حالته الأصلية. فُتح نموذج «إضافة عمل» ثم أُلغي دون حفظ، وبقي التخطيط العربي وRTL سليمين. لم تظهر أخطاء runtime أو hydration في console. التفاصيل في `verification/entertainment-shared-buttons-browser.md`.
**الحالة: PASS.**

## خطة اليوم — Input وButton المشتركة — 2026-08-17
فُتحت `/daily-plan`، وعُرض يوم `2026-08-16` مع سبعة عناصر في Timeline. ظهر حقل التاريخ وأزرار التأجيل والتخطي والتعديل والنقل من المكونات المشتركة. فُتح نموذج تعديل «تحضير عرض تقديمي للمشروع»، أُدخل عنوان عربي مؤقت ثم أُعيدت القيمة الأصلية وأُلغي النموذج دون حفظ. فُتح نموذج النقل، واختُبر حقل التاريخ مؤقتًا ثم أُلغي النقل دون حفظ. بقيت الصفحة عربية وباتجاه RTL ولم تتغير البيانات الدائمة.
التفاصيل في `verification/daily-plan-shared-controls-browser.md`.
**الحالة: PASS بصريًا وتفاعليًا، بانتظار بوابات الجودة.**

## مساحة المهام — اعتماد Input وSelect وButton المشتركة — 2026-08-17

اختُبرت `/tasks` بصريًا وتفاعليًا بعد ترحيل حقل البحث، فلاتر الحالة والأولوية والتصنيف، نموذج إضافة المهمة، أزرار الأولوية والتكرار، وأزرار صفوف المهام إلى المكونات المشتركة. أُدخل نص بحث مؤقت واختير فلتر «لم تبدأ» ثم أُعيدت القيم الافتراضية، كما اختُبر عنوان مهمة مؤقت واختيار «عالية» و«أسبوعي» دون إرسال النموذج. تم تحويل أزرار الإكمال والتوسيع والتحرير والأرشفة والخطوات الفرعية إلى `Button` المشترك، مع بقاء الأزرار العامة خارج مساحة المهام كما هي. لم تُحفظ أي بيانات اختبارية، ولم تظهر أخطاء runtime أو hydration، ونجحت TypeScript وESLint و`git diff --check` حتى هذه النقطة.

## اليوميات — Input وTextarea وButton المشتركة — 2026-08-17
تم فتح `/journal` والتحقق بصريًا من RTL والحقول المشتركة: التاريخ، عنوان اليوم، مساحة النص، خيارات المزاج، الحفظ والأرشفة ودفتر الأيام. أكد فحص DOM وجود `2` من `Input` و`1` من `Textarea` و`15` من `Button` المشتركة، مع خلو `journal-workspace.tsx` من عناصر التحكم الخام. اختُبر عنوان ونص مؤقتان واختيار المزاج «سعيد» ثم أُعيدت القيم إلى الفراغ دون حفظ. اختُبر التنقل إلى 2026-08-16 عبر زر اليوم ثم العودة إلى 2026-08-17 دون حفظ. ظهر عطل تحميل مؤقت عند إدخال التاريخ مباشرة عبر المتصفح، لكن إعادة فتح الصفحة أعادتها سليمة، ولم يظهر في Console بعدها سوى رسائل React DevTools وHMR المعتادة. التفاصيل في `verification/journal-shared-controls-browser.md`.

## المشاريع والأهداف — المكونات المشتركة — 2026-08-17

- فُتحت `/projects` محليًا على `localhost:3004` وتحقّق ظهور لوحة المشاريع، نموذج المشروع، بطاقات المشاريع وتفاصيل المشروع باتجاه RTL.
- أكد فحص DOM في المشاريع وجود `Input` و`Textarea` و`Select` و`Button` المشتركة في الحقول والأزرار الداخلية، بينما الأزرار الخام المتبقية تخص شريط التنقل العام خارج النطاق.
- فُتحت بطاقة «تحضير إطلاق تجريبي»، واختُبر حقل إضافة مهمة بقيمة مؤقتة ثم أُفرغ دون إرسال النموذج؛ لم تُضف مهمة ولم يحدث تغيير دائم.
- فُتحت `/goals` وتحقّق ظهور بطاقات الأهداف ونموذج الهدف الجديد وأزرار التفاصيل والأرشفة وتغيير الحالة باتجاه RTL.
- أكد فحص DOM في الأهداف اعتماد الحقول والأزرار الداخلية على المكونات المشتركة، مع بقاء الأزرار الخام العامة خارج مساحة الأهداف.
- لم تُرسل نماذج ولم تُنفّذ تغييرات دائمة. اجتازت الدفعة TypeScript وESLint و`git diff --check` مبدئيًا، وتبقى بوابة `next build` النهائية قبل الدفع.

## onboarding والسبورة — المكونات المشتركة — 2026-08-17

**onboarding:** فُتحت `/onboarding` وتحققت الخطوة الأولى وحقول الاسم والمدينة وزر «التالي» باستخدام `Input` و`Button` المشتركة. تم التنقل إلى إيقاع اليوم والاتجاه والعادات، واختُبر اختيار اتجاه مؤقت دون إكمال الإعداد. ظهرت Selects نوع الورد وهدفه كمكونات مشتركة، وكان DOM خاليًا من الأزرار الخام داخل onboarding، مع اتجاه RTL وConsole نظيف. لم يُضغط «ابدأ مساحتي» ولم تُحفظ بيانات.

**السبورة:** فُتحت `/board` وتحققت أداة اختيار السبورة وفلاتر التجميع والألوان وأزرار التكبير وبطاقات Sticky Note. فُتح نموذج الإضافة، وأُدخل عنوان مؤقت ثم أُفرغ دون الضغط على «إضافة». ظهرت حقول النموذج ومحدداته من `Input` و`Select` و`Button` المشتركة، ولم توجد عناصر HTML خام داخل ملف السبورة. الأزرار الخام الأربعة التي ظهرت في فحص الصفحة تخص شريط التخطيط العام خارج نطاق الدفعة. الصفحة RTL وConsole نظيف، ولم تُحفظ تغييرات دائمة.

## المال وQuick Add — المكونات المشتركة — 2026-08-17

- فُتحت `/money` وتحقق العرض العربي RTL لميزانية الشهر، نموذج العملية المالية، المصروفات المتكررة وآخر العمليات.
- تحققت عناصر `Input` و`Select` و`Button` المشتركة عبر `data-slot`، مع اختبار حقل الميزانية وحقل المبلغ والاختيارات دون حفظ.
- فُتح Quick Add من TopNav، وأُدخل عنوان مؤقت، ثم تم التبديل إلى نوع «ملاحظة» للتحقق من `Textarea` المشترك، وأُغلق الحوار عبر «إلغاء» دون مراجعة أو حفظ.
- لم تُجرَ أي تغييرات دائمة في بيانات المال أو Quick Add.

## التذكيرات — المكونات المشتركة — 2026-08-17

- فُتحت `/reminders` وتحققت بصريًا من قائمة التذكيرات ومنطقة الاقتراحات وبقاء RTL.
- فُتح نموذج «تذكير جديد»؛ ظهر Input العنوان والموعد وSelect النوع والتكرار وأزرار الإلغاء والحفظ بالمكونات المشتركة.
- أُدخل عنوان مؤقت، واختير النوع «عادة» والتكرار «يوميًا»، ثم أُلغي النموذج دون إرسال أو حفظ بيانات.
- عادت الصفحة إلى حالتها الأصلية، ولم يظهر خطأ Console أو تذكير جديد.
- بوابات الجودة: TypeScript وESLint و`git diff --check` نجحت، وبناء الإنتاج نجح بعد تحديث التوثيق.

## الأرشيف والبحث الشامل — 2026-08-17

فُتحت `/archive` وتحقق ظهور حقل البحث وفلاتر الأقسام وأزرارها كمكونات مشتركة داخل RTL. أُدخلت عبارة بحث مؤقتة ثم أُعيد الحقل فارغًا دون حفظ. فُتح البحث الشامل من TopNav، وأُدخلت كلمة «مهمة» فظهرت نتائج المهام وخطة اليوم داخل أزرار مشتركة، ثم أُغلق الحوار عبر زر الإغلاق دون فتح نتيجة أو تغيير دائم. لم تظهر أخطاء تشغيل أثناء الفحص.

## الصفحة الرئيسية — Button وInput المشتركة — 2026-08-17

- فُتحت الصفحة الرئيسية على `http://localhost:3004/` وتحقق ظهور الواجهة العربية باتجاه RTL مع أزرار المهام والصلوات والاقتراحات.
- أكدت مراجعة DOM أن عناصر التفاعل داخل الصفحة تستخدم Button المشتركة، وأن حقل تعديل الاقتراح يستخدم Input المشتركة عند فتحه.
- اختُبر زر «اختبار مهمة يومية» بالنقر ثم النقر مرة أخرى؛ تغيّرت مؤشرات الخطة محليًا من `2/7` إلى `3/7` ثم عادت إلى `2/7` دون إبقاء أثر تجريبي.
- لم تُرسل نماذج أو تُحفظ اقتراحات أو تغييرات دائمة، وبقيت الصفحة تعمل عبر الحالة المحلية المعتادة.
- TypeScript وESLint و`git diff --check` نجحت على دفعة الصفحة الرئيسية؛ سيُعاد تشغيل `next build` مع البوابات النهائية بعد تحديث التوثيق.

## الحساب والمصادقة والعادات — المكونات المشتركة — 2026-08-17

فُتحت مساحة الحساب وتحقق DOM من أزرار النسخ الاحتياطي وحذف الحساب وحقل رفع الملف باستخدام المكونات المشتركة، مع Console نظيف ودون تنفيذ عملية حساسة. فُتحت مساحة العادات، ثم فُتح نموذج عادة جديدة وأُدخل اسم مؤقت واختير تكرار أسبوعي، وبعد ذلك أُغلق النموذج دون حفظ فعادت الواجهة إلى حالتها الأصلية. فُتحت صفحة المصادقة الفعلية `/login` بعد التحقق من أن `/auth` غير موجود؛ نجح تبديل النموذج إلى إنشاء الحساب وظهور حقل الاسم ثم إعادته إلى تسجيل الدخول، دون إدخال بيانات اعتماد أو إرسال النموذج.


## دفعة المكونات المشتركة — مساحة الملاحظات — 2026-08-17
- **النطاق:** ترحيل أزرار تبديل الشبكة والقائمة، مسح الفلتر، وسوم الالتقاط والتحرير، التثبيت، تحرير الملاحظة، والأرشفة في `components/notes/notes-workspace.tsx` إلى `Button` المشتركة، مع الحفاظ على الكلاسات والـhandlers والحالة الحالية.
- **التحقق المتصفحي:** فُتحت `/notes` على `localhost:3004` وتأكد RTL بصريًا، وأظهر DOM عدم وجود أزرار خام داخل مساحة الملاحظات (`rawButtonsInNotes: 0`). اختُبر التبديل إلى عرض القائمة ثم فلترة «تطوير» ومسح الفلتر، وفُتح محرر أول ملاحظة ثم أُلغي دون حفظ.
- **الحفاظ على السلوك:** بقيت خمس ملاحظات دون تغيير، ولم تُنفذ عملية حفظ أو أرشفة أو تعديل دائم. لم تُضف migrations أو secrets.
- **التوثيق:** `verification/notes-shared-controls-browser.md`.
- **بوابات الجودة:** نجحت TypeScript وESLint و`git diff --check` و`next build`. ظهر فقط تحذير Next.js المعلوماتي الخاص بتقادم convention الخاص بـmiddleware.


- **دفعة المساحة الدينية — shared controls:** على `http://localhost:3004/religious` تم التأكد بصريًا وتفاعليًا من اتجاه RTL وظهور بطاقات الصلوات والورد والحفظ والسنن والنوافل والمصحف دون أخطاء. أكد فحص DOM عدم وجود عناصر خام داخل الصفحة، ووجود `data-slot="button"` و`data-slot="select"` على عناصر التحكم المرحّلة. اختُبر زر صلاة الظهر بتبديله مؤقتًا ثم إعادته إلى «لم تُسجّل»، واختُبر Select حالة حفظ سورة الفاتحة باختيار «تحتاج مراجعة» ثم إعادته إلى «بحفظها». نجحت TypeScript وESLint و`git diff --check` و`next build`، مع تحذير middleware المعلوماتي المعتاد.


- **دفعة المراجعة الأسبوعية — shared controls:** فُتحت `/review` على `localhost:3004` وظهر RTL والكروت العربية والسياق المرتبط دون أخطاء. أكد فحص DOM عدم وجود عناصر تحكم خام، وظهور أزرار الدفعة ضمن `data-slot="button"` وحقول المراجعة ضمن `data-slot="textarea"`. أُدخل قرار مؤقت، فتغيرت الحالة إلى «هناك نص غير محفوظ» وتم اختبار «حفظ كمسودة»؛ ثم استُعيدت نسخة localStorage الأصلية وأُعيد تحميل الصفحة دون بقاء أثر الاختبار أو إنشاء مهمة. نجحت TypeScript وESLint و`git diff --check` و`next build`، مع تحذير middleware المعلوماتي المعتاد.


- **المراجعة الساكنة النهائية وعناصر الصفحات العامة:** فُتحت `/offline` بصريًا، وظهر زر «إعادة المحاولة» العربي ضمن Button المشتركة؛ أكد فحص DOM `rawButtons: 0` و`sharedButtons: 1`. راجعت صفحة الخطأ العامة ساكنًا بعد ترحيل زر `reset` إلى Button. أعاد التدقيق الشامل في `app` و`components` صفر عناصر تحكم خام خارج `components/ui`، ثم نجحت TypeScript وESLint و`git diff --check` و`next build`.

## دفعة تحسين الـShell — قائمة التنقل السريع — 2026-08-17

فُتحت الصفحة الرئيسية على `localhost:3004` واختُبر زر «القائمة» في `TopNav`؛ ظهر Dialog «تنقل سريع» وعرض روابط الأقسام العربية كاملة. اختُبر رابط «مساحة العمل» من داخل Dialog، فتم الانتقال بنجاح إلى `/workspace` وإغلاق القائمة دون تغيير بيانات. أكد فحص DOM في Workspace عدم وجود عناصر تحكم خام، مع استخدام `data-slot` المشتركة في 8 أزرار و4 حقول إدخال وSelect واحد وTextarea واحد. نجحت TypeScript وESLint و`git diff --check` و`next build`، مع تحذير Next.js المعلوماتي المعتاد الخاص بتقادم convention الخاص بـmiddleware.
