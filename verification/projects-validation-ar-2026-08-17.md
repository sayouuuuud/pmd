
## اختبار المتصفح — 2026-08-17

- فُتح المسار `http://localhost:3004/projects` على الخادم المحلي.
- أُرسل نموذج «مشروع جديد» فارغًا؛ ظهرت الرسالة العربية `اكتب اسم المشروع أولًا` inline، ولم تُنشأ سجلات جديدة.
- أُدخل العنوان `مشروع اختبار التحقق`؛ اختفت الرسالة أثناء الكتابة.
- أُرسل النموذج بعد إدخال العنوان؛ ظهر المشروع في عمود «أفكار»، مع القيم الافتراضية `بدون هدف` و`بدون موعد`.
- أُرشف المشروع التجريبي بعد التحقق، وعادت إحصائية «كل المشاريع» إلى 3.
- الملاحظة: الاختبار يثبت مسار الرفض الفارغ، استعادة الحالة أثناء الكتابة، الإنشاء، والتنظيف. لم تُختبر عمليات قاعدة بيانات خارج fallback الواجهة في هذه الجولة.

المرجع: `projects-workspace.tsx`، commit الدفعة بعد اجتياز TypeScript وESLint و`git diff --check` و`next build`.
