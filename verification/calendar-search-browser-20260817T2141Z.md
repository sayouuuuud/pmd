# Calendar/Search browser verification — 2026-08-17

- افتُحت `/calendar` على `localhost:3004` في RTL وبالوضع الداكن دون أخطاء runtime.
- ظهرت شبكة أغسطس 2026 وبطاقة «أحداث 18 أغسطس» ومصادر التقويم، مع عدم وجود قصّ بصري في viewport المستخدم.
- فُتح «البحث الشامل» من صفحة التقويم، وظهر النص التعريفي المحدث الذي يذكر التقويم والأرشيف.
- استُخدم الاستعلام «تقويم»؛ ظهرت مجموعة «التقويم» بنتائج مرتبطة بالتذكيرات وخطة اليوم، بما فيها مواعيد اليوم والغد وعناصر خطة بأوقات ISO.
- نتيجة البحث أظهرت عناوين عربية وsubtitles واضحة، والأزرار قابلة للتفاعل، مع المحافظة على semantic tokens وRTL.
- Console بعد التفاعل: لا أخطاء JavaScript أو hydration؛ ظهرت فقط رسائل React DevTools وHMR المعتادة في بيئة التطوير.
- ملاحظة نطاق: روابط `/calendar?date=...` و`/archive?q=...` تمثل deep-link intent، بينما تمرير query إلى صفحات التقويم/الأرشيف يحتاج قراءة params إضافية في دفعة لاحقة إذا أُريد فتح العنصر المحدد تلقائيًا.

**الحالة:** PASS للبحث والفهرسة البصرية؛ deep-link query forwarding مؤجل كتحسين مستقل.

## Production deep-link rerun

في نسخة الإنتاج بعد Webpack build، فُتح `/calendar?date=2026-08-16` فانتقل التقويم إلى أغسطس 2026 وحدد «أحداث ١٦ أغسطس» مع سبعة عناصر، بما يثبت قراءة المعلمة date. وفُتح `/archive?q=اختبار` فامتلأ حقل «البحث داخل الأرشيف» بقيمة «اختبار» وظهرت العناصر المطابقة. لم يظهر أي إخراج في console في صفحة الأرشيف.

**الحالة النهائية:** PASS للـdeep-link date وq في بيئة الإنتاج المحلية.


## إعادة فحص بعد إصلاح hydration — 2026-08-17T21:57Z

- `/money`: فتحت نسخة الإنتاج بنجاح، ظهرت لوحة الميزانية والعمليات وحقل التاريخ بقيمة 2026-08-17 دون اختلاف مرئي أو خطأ JavaScript في console.
- `/calendar?date=2026-08-16`: فُتح أغسطس 2026، وتحدد يوم 16 أغسطس، وظهرت 7 عناصر مرتبطة بخطة اليوم؛ لا أخطاء console.
- `/archive?q=اختبار`: انتقلت قيمة `q` إلى حقل البحث وظهرت العناصر المؤرشفة؛ لا أخطاء مرئية أثناء التحميل.
- سبب الإصلاح: إزالة قيم `new Date()` و`cairoToday()` من التهيئة الأولى في Money وCalendar وتأجيلها إلى effect، لتقليل احتمال React hydration mismatch قرب تغيّر اليوم أو اختلاف المنطقة الزمنية.
- بوابات typecheck وlint وWebpack أعيد تشغيلها بعد الإصلاح: PASS. تحذير ESLint الوحيد سابق ومعلوماتي في `workspace-workspace.tsx` حول dependency `loadData`.
- المصدر: `http://localhost:3004/money`, `http://localhost:3004/calendar?date=2026-08-16`, `http://localhost:3004/archive?q=%D8%A7%D8%AE%D8%AA%D8%A8%D8%A7%D8%B1`.

الحالة: PASS للـdeep-link والتحميل البصري في هذه الجولة؛ يلزم تشغيل بوابات ownership/responsive/accessibility بعد الإصلاح قبل الاعتماد.

## فحص خادم التطوير — 2026-08-17T22:03Z

- فُتحت `/money` بعد تشغيل خادم تطوير نظيف على `localhost:3004`.
- صفحة Money حمّلت واجهتها العربية والبيانات المحلية دون أخطاء runtime ظاهرة.
- Console احتوى على رسائل React DevTools وHMR فقط، ولم يظهر React hydration error #418 في الفحص اليدوي.
- تبقى نتيجة accessibility الآلية السابقة الخاصة بـMoney غير محسومة حتى تُعاد البوابة على خادم إنتاج متطابق مع آخر build؛ لذلك لا تُعامل هذه الملاحظة وحدها كإغلاق نهائي للخلل.


## إعادة اختبار الإنتاج بعد إصلاح Invalid Date — 2026-08-17 22:12 UTC

- أُعيد بناء Webpack وتشغيل process جديد فعليًا على `localhost:3004` بعد إيقاف process قديم كان يحتفظ بالـbundle السابق.
- فتح `/calendar?date=2026-08-16` بنجاح؛ ظهرت واجهة التقويم، شهر أغسطس 2026، وعنوان التفاصيل «أحداث ١٦ أغسطس» مع عناصر خطة اليوم المرتبطة.
- لم تعد شاشة «حصل عطل مؤقت» تظهر.
- console المتصفح: لا توجد مخرجات أو أخطاء، بما فيها React #418 و`RangeError: Invalid time value`.
- السبب المكتشف والإصلاح: `selectedDate` كان يبدأ بقيمة فارغة، ثم كان `dateFromKey('')` يُمرر إلى `Intl.DateTimeFormat`; أصبح يبدأ بقيمة ثابتة صالحة، وأصبح `today` مؤجلًا إلى `useEffect`، مع تحقق صارم من صلاحية `date=YYYY-MM-DD`.

النتيجة: PASS للتقويم الإنتاجي وdeep-link التاريخي بعد إعادة التشغيل الفعلية.


## إعادة فحص Money الإنتاجي — 2026-08-17 22:12 UTC

فُتحت `/money` على process الإنتاج الجديد، وظهرت لوحة الميزانية والإيرادات والمصروفات والمقارنة الشهرية وقيمة تاريخ العملية `2026-08-17` في الواجهة العربية RTL. لم يظهر أي إخراج في console؛ النتيجة PASS مبدئيًا لإصلاح hydration في Money.


## إعادة فحص Dashboard الإنتاجي — 2026-08-17 22:12 UTC

فُتحت `/` في نسخة الإنتاج الجديدة، وظهرت ملخصات اليوم وخطة اليوم والمهام والعادات والفلوس والصلوات والورد مع التواريخ العربية. لم يظهر أي إخراج في console؛ النتيجة PASS مبدئيًا لإصلاح التاريخ في Dashboard.


## إعادة فحص Religious الإنتاجي — 2026-08-17 22:13 UTC

فُتحت `/religious` في نسخة الإنتاج الجديدة، وظهرت متابعة الصلاة والورد وخطة الحفظ والمصحف والمواقيت العربية بصورة كاملة. لم يظهر أي إخراج في console؛ النتيجة PASS مبدئيًا لإصلاح `hydratedDateKey` وعدم ظهور React #418 في هذه الجولة.

## إعادة تحقق production بعد إصلاحات Projects/Money — 2026-08-17T22:29Z

- `/calendar?date=2026-08-16`: HTTP 200، الواجهة تعرض «أحداث ١٦ أغسطس» وسبعة عناصر مرتبطة، وظهر التاريخ المحدد داخل شبكة أغسطس ٢٠٢٦.
- Console بعد تحميل التقويم: لا توجد مخرجات أو أخطاء React/RangeError.
- `/archive?q=مشروع`: HTTP 200، حقل «البحث داخل الأرشيف» مملوء تلقائيًا بـ«مشروع»، وظهرت نتيجة «مشروع اختبار التحقق» مع خيار الاستعادة.

تُستكمل قراءة console واختبارات البحث الشامل بعد هذا السجل.

- Console بعد `/archive?q=مشروع`: لا توجد مخرجات أو أخطاء.
- فُتحت نافذة البحث الشامل من الأرشيف بنجاح؛ ظهر الوصف الذي يتضمن «التقويم» و«الأرشيف»، وحقل البحث العام أصبح متاحًا فوق الصفحة.

## إعادة اختبار البحث الشامل production — 2026-08-17T22:29Z

فُتحت نافذة البحث الشامل من الأرشيف، ثم أُدخلت كلمة «تقويم». ظهر قسم «التقويم» مع 13 نتيجة تشمل عناصر خطة اليوم، التذكيرات، والأحداث اليدوية، مع عناوين وأوقات عربية واضحة. لم يُسجل console أي خطأ بعد الإدخال.

النتيجة: PASS للبحث الشامل وقسم التقويم في نسخة الإنتاج.
