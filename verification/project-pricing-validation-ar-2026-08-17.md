# تحقق عربي لنموذج دفعات المشاريع — 2026-08-17

## النطاق

أُغلقت فجوة UX في نماذج دفعات المشاريع داخل `components/projects/projects-workspace.tsx`. أصبح نموذج إنشاء الدفعة ومحرر تعديل الدفعة يستخدمان `noValidate` ورسائل عربية inline، مع `aria-invalid` و`aria-describedby` ومسح الخطأ أثناء الكتابة. تم فصل حالة خطأ الإنشاء عن حالة خطأ التعديل حتى لا تظهر رسالة أحد النموذجين داخل الآخر.

## اختبار المتصفح

اختُبرت الصفحة المحلية `http://localhost:3004/projects` على مشروع موجود أصلًا، دون إنشاء مشروع جديد أو تغيير سجلات المستخدم الأصلية.

| المسار | الإجراء | النتيجة |
|---|---|---|
| إنشاء دفعة — العنوان | إرسال النموذج فارغًا | ظهر رفض عربي inline لبقاء النموذج مفتوحًا بدل الفشل الصامت |
| إنشاء دفعة — العنوان | إدخال `دفعة اختبار التحقق` | اختفت رسالة العنوان أثناء الكتابة |
| إنشاء دفعة — المبلغ | إرسال عنوان صحيح مع مبلغ فارغ | ظهر رفض عربي inline للمبلغ |
| إنشاء دفعة — المبلغ | إدخال `1000` | اختفت رسالة المبلغ أثناء الكتابة |
| إنشاء دفعة — النجاح | حفظ العنوان والمبلغ الصحيحين | ظهرت دفعة الاختبار بقيمة ١٬٠٠٠ جنيه |
| تنظيف إنشاء الدفعة | اختيار «إلغاء» للدفعة الاختبارية | اختفت الدفعة الاختبارية وعادت بيانات الدفعات الأصلية |
| تعديل دفعة — العنوان | فتح محرر دفعة الاختبار ومسح العنوان ثم الحفظ | ظهر رفض عربي خاص بالمحرر وبقي المحرر مفتوحًا |
| تعديل دفعة — العنوان | إعادة `دفعة اختبار التحقق` | اختفت رسالة العنوان وأُغلق المسار دون تغيير فعلي |
| تعديل دفعة — المبلغ | فتح المحرر ومسح المبلغ ثم الحفظ | ظهر رفض عربي `اكتب مبلغًا أكبر من صفر.` داخل محرر التعديل |
| تعديل دفعة — المبلغ | إعادة `1000` والحفظ | اختفت الرسالة وأُغلق المحرر، وبقيت قيمة الدفعة الأصلية |

## بوابات الجودة

السجل الخام: `verification/project-pricing-validation-quality-20260817T142931Z.log`.

| البوابة | النتيجة |
|---|---|
| TypeScript (`tsc --noEmit`) | PASS — status 0 |
| ESLint | PASS — status 0 |
| `git diff --check` | PASS ضمن سجل التشغيل |
| `next build` | PASS — Next.js 16.3.0، compilation وTypeScript و27 صفحة ثابتة |

تحذير Next.js الخاص بتسمية `middleware` إلى `proxy` معلوماتي فقط ولم يمنع البناء.

## حدود الاختبار

استخدم الاختبار مشروعًا ودفعة موجودين أصلًا لتقليل أثر البيانات. أُنشئت دفعة اختبارية مؤقتة ثم أُلغيت من الواجهة، وأُعيدت قيمة محرر الدفعة إلى `1000` قبل الإغلاق. لم تُستخدم قاعدة البيانات أو أسرار أو مسار ترحيل Drizzle.

## الحالة

الدفعة قابلة للاعتماد بعد إعادة تشغيل ownership وresponsive وaccessibility على النسخة النهائية من المكوّن، ثم تحديث سجل الاختبارات والخطة ومصفوفة التدقيق واعتماد التعديل في commit مستقل.

وقت إنشاء التقرير: 2026-08-17 UTC.

## إعادة التدقيق بعد التعديل

السجل الخام: `verification/project-pricing-audits-20260817T143233Z.log`.

| الفحص | النتيجة المقاسة |
|---|---|
| Ownership | PASS — 45 Route Handler، 41 مع جلسة، 41 مع ملكية ظاهرة، ولا توجد مسارات ناقصة |
| Responsive | PASS — 34/34 حالة، `failures: []` |
| Accessibility | PASS — 34/34 حالة، `failures: 0` |

بدأت الجولة عند `2026-08-17T14:32:33Z` وانتهت عند `2026-08-17T14:33:52Z`. أُرجعت artifacts الخاصة بالاستجابة بعد الفحص، وأزيل `tsconfig.tsbuildinfo` المؤقت.

## اعتماد الدفعة

تُعد فجوة تحقق دفعات المشاريع مغلقة: الإنشاء والتعديل يعرضان رفضًا عربيًا واضحًا للعنوان والمبلغ، ويزيلان الخطأ عند التصحيح، مع نجاح البوابات الأساسية وبوابات الملكية والاستجابة وإتاحة الوصول. الخطوة التالية هي تحديث السجلات المركزية ثم اعتماد الكود والتوثيق في commit مستقل.
