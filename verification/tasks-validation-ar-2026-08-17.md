# تحقق عربي لنماذج المهام — 2026-08-17

## النطاق

غطّت هذه الدفعة نموذج تعديل المهمة ونموذج إضافة الخطوة الفرعية. أضيف رفض عربي مرئي، و`noValidate`، وسمات `aria-invalid` و`aria-describedby`، ومسح الخطأ أثناء الكتابة، مع الحفاظ على منطق المهام والهوية البصرية الحالية.

## اختبار المتصفح

- فُتح نموذج تعديل المهمة للمهمة الأصلية `اختبار مهمة يومية`.
- فُرّغ عنوان المهمة ثم شُغّل submit مباشر للنموذج بعد تعذر الاعتماد على فهرسة الزر المتغيرة.
- ظهر التنبيه العربي `اكتب عنوان المهمة والموعد والتصنيف أولًا.` داخل النموذج عبر `role="alert"` ومعرّف `task-edit-error`.
- ظهرت `aria-invalid="true"` و`aria-describedby="task-edit-error"` على حقول العنوان والموعد والتصنيف، وبقيت المهمة الأصلية دون حفظ أو تغيير.
- أُعيد إدخال عنوان المهمة الأصلي؛ أكد فحص DOM اختفاء `role="alert"` وجميع حالات `aria-invalid` بعد الكتابة.
- أُرسل نموذج الخطوة الفرعية فارغًا؛ ظهر النص العربي `اكتب الخطوة الفرعية أولًا.` داخل النموذج.
- أُدخلت القيمة `تجهيز ملخص اليوم`؛ اختفى التنبيه أثناء الكتابة.
- أُضيفت الخطوة بنجاح، وظهر عداد المهمة `0/1 خطوات` مع زرَي إنجاز وحذف للخطوة المؤقتة.
- حُذفت الخطوة الفرعية التجريبية؛ عاد النموذج إلى حالة `أضف أول خطوة صغيرة لتقسيم المهمة.` وعاد عداد المنصة إلى `خطوات فرعية0`.

## بوابات الجودة

سجل البوابات: `verification/tasks-validation-quality-20260817T152030Z.log`.

- TypeScript: PASS.
- ESLint: PASS.
- Diff check: PASS.
- Next production build: PASS؛ اكتمل تشغيل TypeScript داخل البناء دون أخطاء حاجزة.

## فحوص التدقيق المستقلة

سجل التدقيق الخام: `verification/tasks-validation-audits-20260817T152404Z.log`.

- Ownership: PASS؛ `route_count=45`، و`routes_with_session=41`، و`routes_with_visible_ownership=41`، ولا توجد مسارات مفقودة.
- Responsive: PASS؛ `total=34`، و`failures=[]`.
- Accessibility: PASS؛ `total=34`، و`failures=0`، و`failureSummary=[]`.

## الحالة

دفعة تحقق نماذج المهام مكتملة وظيفيًا، واختبار المتصفح والتنظيف والبوابات موثقة، وجاهزة للاعتماد في commit مستقل.
