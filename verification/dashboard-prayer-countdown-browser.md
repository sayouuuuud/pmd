# اختبار عداد الصلاة في Dashboard

## النطاق

- **المسار:** `/`
- **التاريخ:** 2026-08-17
- **البيئة:** `http://localhost:3004` مع واجهة RTL وlocalStorage fallback.

## النتائج

ظهرت بطاقة «الصلوات» داخل Dashboard بنفس تخطيط الكروت الحالي، مع اسم الصلاة القادمة وموعدها وشارة الوقت المتبقي. في جلسة الاختبار كانت الصلاة القادمة «الفجر · غدًا · 04:49»، وظهر العداد بصيغة عربية للساعات والدقائق والثواني.

قُرئت قيمة العداد من عنصر `aria-label` المخصص، ثم انتُظرت 1.5 ثانية. انتقلت القيمة من `7س 36د 11ث` إلى `7س 36د 10ث`، ما يثبت أن Dashboard تحدث العداد كل ثانية. بقيت أزرار الصلوات الخمسة وروابط متابعة الصلاة ظاهرة، ولم يتغير التخطيط أو اتجاه RTL.

تم استخراج العداد عبر DOM من الصفحة بعد البناء التطويري المحلي. لم تُسجل أخطاء JavaScript أو hydration أثناء العرض.

## التغييرات التقنية

نُقل منطق `getNextPrayerCountdown` و`formatPrayerCountdown` إلى `lib/prayer-countdown.ts`، وأصبح مكوّنا `/religious` وDashboard يستخدمان نفس utility. أضيفت ساعة محلية داخل Dashboard تبدأ بعد hydration وتُحدّث كل ثانية، مع fallback يعرض الصلاة pending وموعدها إلى أن تصبح قيمة الساعة جاهزة.

## النتيجة

**PASS.** العداد ظاهر في Dashboard ويحدث كل ثانية، ويدعم وسم «غدًا» للحالة التي تنتقل إلى أول صلاة في اليوم التالي.
