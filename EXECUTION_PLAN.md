# خطة التنفيذ الشاملة — Personal Command Center

> **تعليمات لوكيل التنفيذ (Agent):** هذا الملف هو المرجع الوحيد لخطة العمل. اقرأه بالكامل قبل البدء في أي مرحلة. نفّذ المراحل بالترتيب، ولا تنتقل لمرحلة قبل إتمام كل الـ Milestones الخاصة بالمرحلة الحالية.

---

## 0. قواعد عامة وإلزامية (اقرأها أولاً)

### 0.1 قاعدة التصميم — الأهم على الإطلاق

- **المستخدم سيرفع ملف تصميم جاهز (Design Reference).** هذا الملف هو مصدر الحقيقة الوحيد للشكل والستايل.
- **ممنوع اختراع تصميم جديد.** لا Sidebar جديد، لا Layout جديد، لا ألوان جديدة من عندك.
- المطلوب منك:
  1. استخرج الـ Design System من ملف التصميم المرفوع: الألوان، الخطوط، المسافات، الـ border-radius، شكل الكروت، شكل الأزرار، شكل الـ Inputs، طريقة عرض الإحصائيات.
  2. حوّل هذا الـ Design System إلى Tokens في `globals.css` (بنظام Tailwind المتبع في المشروع).
  3. **حافظ على شكل الـ Dashboard الموجود في التصميم كما هو تماماً** — فقط استبدل الداتا/المحتوى بالبيانات المطلوبة في هذه الخطة (مهام، صلوات، عادات، مصاريف... إلخ).
  4. كل الصفحات والأقسام الجديدة التي ستبنيها يجب أن تلتزم بنفس الـ Design System المستخرج — نفس الكروت، نفس الهوامش، نفس الخطوط، نفس أسلوب العرض. اللوحة كلها يجب أن تبدو كأنها قطعة واحدة.
  5. طريقة التنقل بين الأقسام (Navigation) تُبنى بنفس أسلوب التنقل الموجود في التصميم المرفوع — لا تفرض نمطاً من عندك.

### 0.2 قواعد اللغة والاتجاه

- الواجهة كلها **بالعربية** مع دعم كامل للـ **RTL** (`dir="rtl"` على مستوى الـ html).
- التواريخ تُعرض ميلادي + هجري حيث يكون ذلك منطقياً (خاصة القسم الديني).
- الأرقام والإحصائيات بصيغة مقروءة وواضحة.

### 0.3 استراتيجية البناء

- **Frontend أولاً بالكامل (المراحل 1 → 4)** باستخدام داتا تجريبية (Mock Data) منظمة في ملفات منفصلة (مثل `lib/mock-data/`) بحيث يسهل استبدالها لاحقاً.
- **Backend والداتابيز بعد ذلك (المرحلة 5)**: قاعدة بيانات Neon + مصادقة Better Auth (إيميل + باسورد فقط).
- الداتا التجريبية يجب أن تكون **واقعية وبالعربية** (أسماء مهام حقيقية، ملاحظات منطقية...) وليس "Lorem ipsum".
- كل قسم يُبنى كمكونات منفصلة (Components) — ممنوع ملفات page.tsx عملاقة.
- الجهاز المستهدف حالياً: **اللابتوب / الشاشات الكبيرة** — لكن اجعل الأساس متجاوباً قدر الإمكان بدون التضحية بتجربة الديسكتوب.

### 0.4 الأقسام العشرة النهائية

1. Dashboard الرئيسية
2. المهام (Tasks)
3. الملاحظات (Notes)
4. السبورة / مساحة العمل (Canvas Board)
5. القسم الديني (صلوات + مصحف + مكتبة صوتية + أذكار)
6. العادات + اليوميات (Habits + Journal)
7. المشاريع + الأهداف (Projects + Goals)
8. الفلوس (Finance)
9. الترفيه (Entertainment)
10. Weekly Review + البحث الشامل + Quick Add

### 0.5 — رؤية المنتج والحلقة الأساسية

المنتج ليس مجرد مجموعة أقسام منفصلة؛ هو **نظام تشغيل شخصي عربي** يساعد المستخدم على ترتيب يومه وتحسين حياته بدون تشتيت. القيمة الأساسية يجب أن تكون حلقة متكررة وواضحة:

> كل صباح أعرف ماذا أفعل، وخلال اليوم ألتقط أي فكرة أو مهمة بسرعة، وكل مساء أراجع ما حدث، وكل أسبوع أقرر ما الذي سأحسّنه.

لذلك يجب أن تكون هناك تجربة مركزية باسم **خطة اليوم** تجمع المهام، العادات، الصلاة القادمة، الورد، المواعيد، وفترات الراحة في Timeline واحد. الأقسام المختلفة تظل موجودة للتفاصيل، لكن خطة اليوم هي نقطة الدخول الأساسية للاستخدام اليومي.

### 0.6 — العلاقات بين البيانات

يجب تصميم الواجهات والداتا بحيث تكون العناصر مترابطة وليست جزرًا منفصلة:

- الهدف يرتبط بمشروع، والمشروع يرتبط بمهام وملاحظات.
- العادة يمكن أن تنشئ مهمة متكررة أو تظهر داخل خطة اليوم.
- الصلاة والورد يظهران داخل خطة اليوم بدون تحويل القسم الديني إلى جزء منفصل عن حياة المستخدم.
- المصروف يمكن ربطه بمشروع أو تصنيف أو هدف مالي.
- يمكن تحويل سطر من ملاحظة إلى مهمة مع الاحتفاظ بالرابط بينهما.
- Weekly Review تقرأ العلاقات والنتائج من كل الأقسام وتقترح قرارات للأسبوع القادم.

### 0.7 — أولويات الإطلاق ونطاق النسخ

لتجنب تحول المشروع إلى عشرة تطبيقات داخل تطبيق واحد، يُنفّذ على نسخ واضحة:

| النسخة | النطاق الأساسي |
|---|---|
| MVP | Dashboard، المهام، الملاحظات، العادات، خطة اليوم، Quick Add، Weekly Review، تسجيل الدخول، وقاعدة البيانات الأساسية |
| V1 | الصلوات، الورد، المشاريع، الأهداف، المصاريف، البحث الشامل، والتذكيرات |
| V2 | Canvas Board المتقدم، المكتبة الصوتية، المساعد الذكي، التحليلات المتقدمة، وOffline/PWA الكامل |

الأولوية داخل كل قسم هي: الوظيفة الأساسية والعرض، ثم الإحصائيات والرسوم، ثم الكماليات والفلاتر والعلامات الخاصة. يمكن تأجيل Canvas Board والمكتبة الصوتية إذا أثّرا على إطلاق الـMVP، مع الحفاظ على تصميم البيانات القابل لإضافتهما لاحقاً.

### 0.8 — سجل التنفيذ الحالي

> آخر دفعتين أنشأتا أول Vertical Slice يومي قابل للاستخدام ثم أضافتا أساس Backend/Auth قابلًا للتشغيل عند توفير credentials، مع الحفاظ على هوية التصميم الحالية. هذه الحالة لا تعني اكتمال الـMVP أو اتصال الإنتاج.

| النطاق | الحالة | ما تم تنفيذه |
|---|---|---|
| Foundation | مكتمل جزئيًا | Provider عام، Content Card، Empty State، RTL/Cairo، Navigation محدث، ومسارات MVP الأساسية |
| Dashboard | مكتمل جزئيًا | Dashboard جديدة تتمحور حول خطة اليوم، مع إكمال المهام، ملخص العادات، آخر الملاحظات، واقتراح قابل للتعديل |
| Daily Plan | مكتمل جزئيًا | Timeline موحد للمهام والعادات والصلاة والورد والراحة، مع إكمال وتأجيل العناصر، وروابط مصدرية عميقة للمهمة والعادة والصلاة والورد والمشروع والهدف، وAPI محمي وremote sync مع fallback محلي |
| Tasks | مكتمل جزئيًا | إضافة، إكمال، أرشفة، بحث، فلاتر أساسية، أولويات وتصنيفات وتواريخ Mock |
| Notes | مكتمل جزئيًا | Quick Capture، بحث، Pin، أرشفة، Grid/List، وتصنيفات أساسية |
| Habits | مكتمل جزئيًا | إكمال العادة، Streak، نسبة إنجاز اليوم، ربط العرض بخطة اليوم، اختيار روابط المهمة/المشروع/الهدف، شارات سياق عميقة، وAPI محمي لتسجيل الإنجاز وحفظ الروابط حسب الملكية |
| Weekly Review | مكتمل جزئيًا | ملخص قرارات قابل للتصرف مبني على المهام والعادات والملاحظات، مع مؤشرات أسبوعية مؤرخة للعادات والصلاة والمال والورد وروابط سياق قابلة للتنفيذ |
| Backend/Auth | مكتمل جزئيًا | Schema Drizzle، اتصال Neon lazy، Better Auth بإيميل/باسورد، Route Handler للمصادقة، شاشة Login/Signup، migrations، APIs محمية لـTasks/Notes/Habits/Daily Plan/Profile، middleware وsession-aware navigation؛ الاتصال الفعلي الكامل ينتظر credentials |
| Onboarding | مكتمل جزئيًا | تدفق الاسم والمدينة وبداية اليوم ووقت العمل والهدف مع حفظ محلي وصفحة نجاح عربية؛ أضيف اختيار حتى 3 عادات بداية ونوع الورد اليومي (دقائق/صفحات/نصف جزء/جزء كامل) مع إنشاء العادات وحفظ هدف الورد، ومراجعة زمنية مكتملة مع مزامنة hydration ومنع autofill المضلل؛ التخصيص الأعمق والأهداف المخصصة لكل نمط ورد لاحقًا |
| Account / Profile | مكتمل جزئيًا | جدول `user_profile` وmigration، Profile API محمي بملكية المستخدم، hydration وoptimistic persistence، صفحة `/account` عربية، ورابط حسابي في TopNav؛ التحقق الفعلي بجلسة وقاعدة بيانات ينتظر credentials |
| Projects / Goals | مكتمل جزئيًا | Kanban عربي للمشاريع، بطاقات أهداف مع التقدم، علاقة `goal → project → task` عبر `goal_id` و`project_id`، store محلي مع remote hydration، APIs محمية، وmigration `0002_zippy_pestilence.sql`؛ drag-and-drop والتحرير التفصيلي لاحقًا |
| Finance | مكتمل جزئيًا | Workspace مالية عربية، ميزانية شهرية، دخل ومصروفات، تحليل تصنيفات، روابط اختيارية بالمشاريع والأهداف، localStorage fallback، APIs محمية، وmigration `0003_tough_raza.sql`؛ الاتصال الفعلي بقاعدة البيانات ينتظر credentials |
| Religious | مكتمل جزئيًا | متابعة الصلوات والورد والأذكار وإعدادات المدينة، سجل صلاة محلي مع Streak وتقويم آخر 30 يومًا، حالات تفصيلية للصلاة (في وقتها/جماعة/قضاء/فائتة) مع احتساب الحالات المكتملة، دمج الصلاة والورد مع خطة اليوم، localStorage fallback، API محمي، وmigration `0004_lethal_shen.sql`؛ تحليل الفوات والمزامنة الإنتاجية الكاملة ينتظران استكمال النطاق ومصدرًا موثوقًا وcredentials |

---

## المرحلة 1: الأساس والتصميم (Foundation)

### Milestone 1.1 — نظام التصميم (Design System)

**الهدف:** ترجمة ملف التصميم المرفوع إلى نظام Tokens ومكونات موحدة.

المطلوب بالتفصيل:
- [x] استخراج لوحة الألوان من التصميم المرفوع وتعريفها كـ semantic tokens في `globals.css` (`--background`, `--foreground`, `--card`, `--primary`, `--accent`, `--muted`, `--destructive`... إلخ). **لا تستخدم ألواناً مباشرة (bg-white / text-black) في أي مكان في الكود.** أُنجز ترحيل ألوان أوراق السبورة وتوكنز نصوصها وحدودها، وحالة اليوم الجزئية في تقويم الصلاة إلى `partial`، كما استُبدلت آخر استخدامات الألوان المباشرة في صفحة Design System؛ أظهر المسح الساكن عدم وجود أنماط ألوان مباشرة شائعة في كود التطبيق.
- [x] أُنشئ `components/ui/dialog.tsx` كمكوّن موحد، ثم رُحّلت نافذة Quick Add في `components/layout/top-nav.tsx` لاستخدامه مع الحفاظ على تبويبات وحقول وسلوك مسح المسودة.
- [x] استخراج الخطوط المستخدمة في التصميم وإعدادها عبر `next/font` في `layout.tsx`: تم ضبط Cairo بمتغير `--font-cairo` مع `display: swap` وربطه بتوكن `font-sans`.
- [ ] ضبط `--radius` والمسافات لتطابق التصميم؛ قيمة `--radius` الحالية ومضاعفات Tailwind موحدة، وتبقى مراجعة المقاييس مقابل الصفحات الفعلية.
- [x] إضافة كلاس لون الخلفية `bg-background` على وسم `<html>` في `layout.tsx`.
- [ ] بناء المكونات المشتركة الملتزمة بالتصميم والتي ستُستخدم في كل الأقسام:
  - كارت إحصائية (Stat Card) بنفس شكل كروت الـ Dashboard في التصميم
  - كارت محتوى عام (Content Card)
  - [x] Modal / Dialog موحد للإضافة والتعديل، مع ترحيل Quick Add ونافذة «تذكير جديد» إليه؛ تبقى النوافذ الفعلية الأخرى للترحيل التدريجي.
  - [x] Inputs / Select / Textarea / Checkbox موحدة
  - Badge للأولويات والحالات
  - Empty State موحد (عندما يكون القسم فارغاً)
- [x] تحديث `metadata` في `layout.tsx` بعنوان ووصف واسم تطبيق وكلمات مفتاحية عربية متسقة مع `public/manifest.json`.

**معيار الإتمام:** صفحة واحدة تعرض كل المكونات المشتركة جنباً إلى جنب وتطابق التصميم المرفوع بصرياً.

### Milestone 1.2 — الهيكل العام والتنقل (Shell & Navigation)

المطلوب بالتفصيل:
- [ ] بناء الـ Layout العام للتطبيق **بنفس بنية التنقل الموجودة في ملف التصميم المرفوع** (أياً كانت: قائمة علوية، تبويبات، أيقونات...). لا تفترض شكلاً معيناً.
- [ ] إنشاء Route لكل قسم من الأقسام العشرة مع صفحة Placeholder أنيقة (عنوان القسم + Empty State) بحيث يعمل التنقل كاملاً من اليوم الأول.
- [x] زرار **Quick Add** ظاهر في الهيكل العام ويعمل من Dashboard والصفحات التي تستخدم الـShell، مع إضافة مهمة أو ملاحظة محليًا؛ دعم المصروف والفيلم والإدخال الطبيعي مؤجل إلى Milestone 4.5.
- [ ] خانة **البحث الشامل** ظاهرة في الهيكل العام (شكلية في هذه المرحلة — التفعيل في Milestone 4.5).

**معيار الإتمام:** التنقل بين الأقسام العشرة يعمل، والهيكل مطابق للتصميم.

### Milestone 1.3 — Dashboard الرئيسية (نسخة أولى)

**الهدف:** الحفاظ على شكل الـ Dashboard الموجود في التصميم المرفوع كما هو، مع استبدال محتواه بالبيانات التالية (Mock Data):

- [x] تحية حسب الوقت (صباح الخير / مساء الخير) + اسم المستخدم + التاريخ الميلادي والهجري؛ ظهر التاريخان بصياغة عربية في Dashboard.
- [x] كارت **مهام النهاردة**: عدد المهام المنجزة/الكلية + أول 3-4 مهام قادمة مع إمكانية التعليم عليها، مع حالة فارغة إرشادية للمستخدم الجديد.
- [x] كارت **الصلوات**: حالة الخمس صلوات اليوم + الصلاة القادمة والوقت، مع تعليم الصلاة مباشرة من Dashboard ورابط متابعة الصلاة.
- [x] كارت **العادات**: نسبة إنجاز عادات اليوم + الـ Streak الحالي، مع حماية الحساب من `NaN` عند غياب البيانات.
- [x] كارت **الفلوس**: إجمالي مصروفات الشهر مقارنة بالميزانية والاتجاه مقابل الشهر السابق، مع رابط مباشر لمساحة الفلوس.
- [x] كارت **آخر الملاحظات**: آخر 3 ملاحظات مضافة، مع عرض الملاحظات غير المثبتة عند غياب الملاحظات المثبتة.
- [x] كارت **ورد القرآن**: نسبة التقدم في الورد اليومي مع المتبقي ورابط مباشر لمتابعته.
- [x] بطاقات الإحصاءات والملخصات الرئيسية تنقل إلى القسم الخاص بها عبر روابط مستقرة، مع الحفاظ على الهوية البصرية وRTL.

**معيار الإتمام:** الـ Dashboard مطابقة للتصميم المرفوع بنسبة عالية جداً، والداتا التجريبية واقعية؛ تم التحقق بصريًا وتفاعليًا وتوثيق النتائج في `verification/interaction-smoke-tests.md`.

### Milestone 1.4 — Onboarding وخطة اليوم المركزية

- [x] بناء Onboarding مختصر يحدد اسم المستخدم، المدينة أو الموقع، بداية اليوم، أوقات العمل، الأهداف الحالية، العادات، والورد اليومي. **منجز:** الاسم، المدينة، بداية اليوم، وقت العمل، والهدف الحالي مع حفظ محلي، إضافة إلى اختيار حتى 3 عادات بداية ونوع الورد اليومي (دقائق/صفحات/نصف جزء/جزء كامل) مع إنشاء العادات وحفظ هدف الورد؛ التخصيص المتقدم والأهداف المخصصة لكل نمط ورد لاحقًا.
- [x] إنشاء صفحة **خطة اليوم** كـTimeline موحد يجمع المهام والعادات والصلاة القادمة والورد وفترات الراحة.
- [x] ترتيب عناصر اليوم حسب الوقت والأولوية، مع إظهار سبب ترتيب الاقتراحات وعدم فرضها على المستخدم.
- [x] السماح بتعديل خطة اليوم يدويًا، تأجيل عنصر، تخطيه، إرجاعه إلى الخطة، ونقله إلى يوم آخر.
- [x] جعل خطة اليوم نقطة دخول واضحة من الـDashboard مع الحفاظ على الأقسام التفصيلية.

**معيار الإتمام:** يستطيع مستخدم جديد إعداد تفضيلاته، ثم يرى خطة يوم واقعية قابلة للتعديل تجمع أكثر من قسم في شاشة واحدة.

---

## المرحلة 2: الإنتاجية الأساسية (Core Productivity — Frontend)

### Milestone 2.1 — المهام (Tasks)

المطلوب بالتفصيل:
- [x] CRUD كامل بواجهة: إضافة / تعديل / أرشفة / إكمال مهمة (على Mock Data بحالة Client State).
- [x] خصائص المهمة: العنوان، الوصف (اختياري)، الأولوية (عالية/متوسطة/منخفضة)، تاريخ الاستحقاق، التصنيف.
- [x] **مهام فرعية (Subtasks):** كل مهمة يمكن تقسيمها لمهام فرعية مع شريط نسبة إنجاز يتحدث تلقائياً.
- [x] **أعمدة زمنية:** النهاردة / بكرة / الأسبوع ده / بعدين — مع سحب وإفلات (Drag & Drop) للمهام بين الأعمدة.
- [x] **مهام متكررة:** يومي / أسبوعي — تظهر تلقائياً في يومها مع أيقونة تكرار.
- [x] **لستة "أقل من دقيقتين":** قسم جانبي مصغر للمهام السريعة.
- [x] Badge ملون للأولوية بنفس ألوان الـ Design System.
- [x] فلترة حسب: الأولوية / التصنيف / الحالة.

### Milestone 2.2 — الملاحظات (Notes)

المطلوب بالتفصيل:
- [x] CRUD كامل: إنشاء / تحرير / أرشفة ملاحظة.
- [x] خصائص الملاحظة: العنوان، المحتوى، التصنيف، Tags متعددة، تاريخ الإنشاء والتعديل.
- [x] **Quick Capture:** خانة دائمة أعلى الصفحة — تكتب وتضغط Enter وتُحفظ فوراً كملاحظة سريعة، مع احترام قاعدة CJK IME عند Enter.
- [x] **تثبيت (Pin):** الملاحظات المثبتة تظهر أولاً في منطقة مميزة.
- [x] بحث فوري داخل الملاحظات (العنوان + المحتوى + الـ Tags).
- [x] تبديل بين عرض **Grid** وعرض **List**.
- [x] فلترة بالتصنيف والـ Tags.

### Milestone 2.3 — السبورة (Canvas Board)

المطلوب بالتفصيل:
- [x] مساحة عمل حرة (Canvas) تشبه السبورة، تلتزم بألوان الـ Design System.
- [x] **Sticky Notes:** إضافة ورقة بنقرة، تحريرها مباشرة، وسحبها لأي مكان على السبورة (موقع كل ورقة `x, y` محفوظ في الحالة).
- [x] **ألوان للورق:** 4-5 ألوان بمعاني (ضمن الـ palette العام) مع فلترة بالألوان.
- [x] **حجمان للورق:** صغير وكبير.
- [x] **Zoom & Pan:** تكبير وتصغير وتحريك السبورة بالماوس/العجلة.
- [x] **سبورات متعددة:** إنشاء أكثر من سبورة بأسماء مختلفة والتنقل بينها.
- [x] **مناطق تجميع (Groups):** عنوان لمنطقة على السبورة يمكن وضع الورق تحته.
- [x] **تحويل بضغطة:** أي ورقة تتحول لمهمة (تظهر في قسم المهام) أو لملاحظة كاملة.

---

## المرحلة 3: القسم الديني (Frontend)

> ملاحظة: تم الاستغناء عن قسم المحاضرات بقرار من المستخدم. المكتبة الصوتية = تلاوات قرآن فقط.

### Milestone 3.1 — تتبع الصلوات (Prayer Tracker)

المطلوب بالتفصيل:
- [x] عرض الخمس صلوات لليوم الحالي، وكل صلاة لها حالة تُختار: **لم تُسجّل / في وقتها / جماعة / قضاء / فائتة**، مع احتساب «في وقتها» و«جماعة» و«قضاء» كحالات مكتملة في المؤشرات.
- [x] **مواقيت الصلاة:** جلبها من API مجاني (مثل Aladhan API) حسب مدينة المستخدم، مع عدّاد تنازلي للصلاة القادمة.
- [x] **Streak:** عدد الأيام المتتالية المسجلة التي اكتملت فيها الصلوات، مع عرض آخر سجل واضح داخل بطاقة الثبات.
- [x] **إحصائية شهرية:** مؤشر محفوظ مشتق من آخر 30 يومًا وتقويم يميز الأيام المكتملة/الجزئية/غير المسجلة، مع تحليل محفوظ لأنواع الحالات وتحديد الصلاة الأكثر تسجيلًا كفائتة، وتوافق مع السجلات القديمة التي لا تحتوي العدادات الجديدة.
- [x] **السنن والنوافل:** خانات اختيارية منفصلة (قيام / ضحى / وتر / سنن رواتب).
- [x] عرض تقويمي لآخر 30 يومًا يوضح الأيام الكاملة والجزئية وغير المسجلة، مع الحفاظ على fallback المحلي عند غياب قاعدة البيانات.

### Milestone 3.2 — المصحف (Quran)

المطلوب بالتفصيل:
- [x] جلب نص القرآن كاملاً بالتشكيل من API موثوق (AlQuran Cloud) عند الطلب عبر Route محلي مع فهرس السور الكامل، ودون تخزين النص القرآني في قاعدة بيانات المستخدم أو كتابته يدويًا — **ممنوع كتابة نص قرآني يدوياً من الذاكرة إطلاقاً.**
- [x] تصفح بالسور والأجزاء، مع عرض قراءة مريح؛ جلب السور والأجزاء وعرض الآيات والتحكم في حجم الخط (صغير/متوسط/كبير) وحفظ الآيات المفضلة مكتمل. النص والفهرس يُجلبان عند الطلب من المصدر الخارجي عبر Route محلي مع fallback محدود عند تعذر الشبكة.
- [x] **آخر موضع:** علامة تلقائية عند آخر ما وصل له المستخدم، مع زر "أكمل من حيث توقفت".
- [x] **الورد اليومي:** المستخدم يحدد ورده (دقائق / صفحات / نصف جزء / جزء)، والنظام يعرض تقدم اليوم + نسبة التقدم من الهدف المختار مع حفظ محلي ومزامنة خلفية.
- [x] **خطة الحفظ:** قائمة سور بحالات (بحفظ / حفظت / تحتاج مراجعة) + جدول مراجعة مقترح مشتق من الحالة الحالية مع آجال تنظيمية واضحة.
- [x] **الآيات المفضلة:** حفظ آية في قائمة مفضلة مع خانة "تدبر" لكتابة الخواطر.

### Milestone 3.3 — المكتبة الصوتية + الأذكار

المطلوب بالتفصيل:
- [x] **مشغل تلاوات:** اختيار القارئ ثم السورة من مصدر تلاوات جاهز، مع عنصر `<audio>` وتحكم التشغيل/الإيقاف، وتقديم/رجوع 15 ثانية، وسرعات 0.75× و1× و1.25× و1.5×؛ تم التحقق من تطبيق `playbackRate=1.5` في المتصفح.
- [x] **حفظ مكان الوقوف** في التلاوة والعودة إليه؛ أُضيفت استعادة الموضع وزر الحفظ، وتم التحقق من ظهور المشغل وحالة التشغيل.
- [x] **Playlists وقوائم السماع:** إنشاء قوائم تشغيل خاصة وإضافة/إزالة السور، وقائمة «أسمعه بعدين» وعلامة «تم الاستماع» مع حفظ محلي ومزامنة آمنة؛ تم اختبار التبديل والعدادات في المتصفح.
- [x] **أذكار الصباح والمساء:** قائمة الأذكار مع عدّاد مستقل لكل ذكر، وشريط تقدم لكل جلسة وشريط تقدم إجمالي مع حفظ محلي ومزامنة آمنة.
- [x] **السبحة الإلكترونية:** عدّاد كبير بضغطة واحدة مع اختيار الهدف (33/100/1000) وإعادة التصفير؛ تم اختبار الهدف 33 والتصفير والزيادة في المتصفح.
- [x] **الأدعية المحفوظة:** إضافة أدعية شخصية، عرضها داخل القسم الديني، وحذفها عند الحاجة.

---

## المرحلة 4: التطوير الذاتي والحياة (Frontend)

### Milestone 4.1 — العادات + اليوميات

- [x] **Habit Tracker:** إضافة عادات (اسم + أيقونة + تكرار يومي/أسبوعي) والتعليم عليها يومياً، مع روابط سياق اختيارية للمهمة/المشروع/الهدف وإجراء أرشفة واضح.
- [x] **Streak** لكل عادة + شبكة إنجاز شهرية (Heatmap-style grid) لكل عادة؛ يعتمد الحساب المحلي على `history` مؤرخ بتاريخ القاهرة ويُحدّث عند التعليم والإلغاء.
- [x] نسبة إنجاز اليوم والأسبوع؛ النسب مشتقة من سجل الإنجاز الفعلي وتبقى صالحة عند غياب العادات دون `NaN`.
- [x] **Journal:** تدوينة يومية (نص حر) + اختيار المزاج من مقياس (ممتاز → سيئ) + عرض تقويمي للتدوينات السابقة + رسم بسيط لتقلب المزاج خلال الشهر.

### Milestone 4.2 — المشاريع + الأهداف

- [x] **المشاريع:** Kanban Board بأعمدة (أفكار / شغال / متوقف / خلص) مع سحب وإفلات. كل مشروع كارت فيه: الاسم، الوصف، نسبة الإنجاز، عدد المهام المرتبطة؛ وأصبحت حالة «متوقف» مدعومة محليًا وبعيدًا مع fallback آمن.
- [x] صفحة تفاصيل المشروع: مهامه الخاصة + ملاحظاته المرتبطة.
- [x] **الأهداف:** هدف طويل المدى (عنوان + وصف + تاريخ مستهدف) يرتبط بمهام صغيرة تحته، مع Progress Bar يُحسب تلقائياً من المهام المكتملة.

### Milestone 4.3 — الفلوس (Finance)

- [x] تسجيل مصروف: المبلغ، التصنيف (أكل / مواصلات / ترفيه / فواتير / أخرى...)، التاريخ، ملاحظة.
- [x] تسجيل دخل (اختياري) لعرض الصافي.
- [x] **مصاريف متكررة:** (إيجار / اشتراكات) تُسجَّل تلقائياً كل شهر.
- [x] **رسوم بيانية** (باستخدام مكونات الـ Charts المتبعة في المشروع): Pie/Donut للتوزيع حسب التصنيف + Bar لمقارنة الشهور.
- [x] **الميزانية الشهرية:** سقف إجمالي و/أو سقف لكل تصنيف + مؤشر تقدم + تنبيه بصري عند تجاوز 50% و80% و100%.
- [x] مقارنة "الشهر ده × الشهر اللي فات" برقم واتجاه واضح، مع الحفاظ على الشهر المحدد في التحليل.

### Milestone 4.4 — الترفيه (Entertainment)

- [x] قوائم أفلام/مسلسلات بثلاث حالات: **عايز أتفرج / بتفرج / خلصت** مع نقل سهل بين الحالات؛ تم اختبار نقل `Severance` بين الحالات وإعادته.
- [x] خصائص العنصر: الاسم، النوع (فيلم/مسلسل)، التصنيف، سنة الإصدار، ملاحظة؛ تم اختبار إنشاء عنصر مؤقت وعرض بياناته ثم أرشفته.
- [x] **بعد الإنهاء:** تقييم بالنجوم (1-5) + سطر انطباع شخصي؛ ظهرت عناصر التقييم والانطباع في بطاقة عمل مكتمل.
- [x] **لستة "اقترحه لحد":** علامة خاصة للأعمال التي يريد ترشيحها؛ ظهرت القائمة السريعة والفلتر المطابق.
- [x] لستة تحميل: "عايز أنزله" كحالة أو علامة منفصلة؛ ظهرت العلامة والقائمة السريعة والفلتر المطابق.

### Milestone 4.5 — Weekly Review + Quick Add + البحث الشامل

- [~] **Weekly Review:** صفحة تلخص الأسبوع تلقائياً من الأقسام المتاحة: عدد المهام المنجزة، الالتزام بالعادات، المؤشرات الدينية (الصلوات والورد والحفظ والاستماع)، وإجمالي المصاريف/الدخل، مع ثلاث خانات كتابة: (إيه اللي مشي كويس / إيه اللي وقفني / هدف الأسبوع الجاي). **منجز جزئيًا:** المؤشرات الموحدة والقرارات القابلة للتصرف وروابط السياق للمهمة والعادة والمشروع والهدف وروابط مباشرة للدين والمال؛ أصبحت إنجازات العادات محسوبة من نطاق الأسبوع باستخدام `doneToday` لليوم الحالي و`history` المؤرخ لبقية الأيام، مع تمرير التاريخ المحلي صراحة إلى مزامنة `habitLog`. تبقى مراجعة الإنتاج بالـcredentials وبعض العلاقات الإنتاجية المفتوحة.
- [x] **Quick Add الحقيقي:** الزرار الموجود من المرحلة 1 يفتح Modal يضيف مهمة أو ملاحظة أو مصروفًا أو فيلمًا/مسلسلًا من أي صفحة دون مغادرتها، مع parser عربي طبيعي، معاينة قبل الحفظ، وربط اختياري بالمشروع والهدف؛ اختبارات المرحلة موثقة في `docs/quick-add-phase-test-results.md`.
- [x] **البحث الشامل:** خانة البحث تبحث في الأقسام الموحدة وتعرض النتائج مجمعة حسب القسم مع انتقال مباشر إلى العنصر عبر anchors مستقرة؛ تشمل الروابط العميقة المهام والملاحظات والمشاريع والأهداف والعادات والمال وخطة اليوم والتذكيرات والترفيه واليوميات، مع الحفاظ على query التاريخ/الشهر عند الحاجة.
- [~] **الأرشيف:** صفحة أرشيف مشتركة قابلة للاسترجاع وتغطية مكتملة حاليًا للعادات والمهام والملاحظات والسبورة وبعض الدومينات؛ ما تزال تغطية كل العناصر المكتملة/المنتهية في جميع الأقسام ومراجعة العقود الإنتاجية مفتوحة. إصلاح العادات يحافظ على سجل الإنجاز اليومي عند أرشفة العادة ويعيد التكرار والروابط عند الاستعادة.
- [~] **اقتراحات شخصية قابلة للرفض:** يوجد حاليًا اقتراح هادئ مشتق من خطة اليوم أو العادات أو المصروفات المتكررة، مع سبب ومصدر سياق وزر رفض وزر «اتخاذ خطوة» لا ينفذ قرارًا حساسًا تلقائيًا؛ تبقى تغطية نقل المهام المتأخرة واختيار وقت أفضل والتنبيهات المالية الموسعة.
- [x] دعم إدخال عربي سريع بصياغات طبيعية قدر الإمكان، مثل: "ضيف مهمة بكرة الساعة ٨" أو "سجل مصروف ١٢٠ مواصلات"، مع شاشة تأكيد قبل الحفظ.
- [~] عرض روابط العناصر المرتبطة داخل كل قسم: تم تطبيق روابط المشروع والهدف داخل المهام وخطة اليوم والملاحظات والعادات والمالية وWeekly Review، مع anchors مستقرة لمعظم البطاقات ونتائج البحث، وأضيفت روابط مباشرة من Weekly Review إلى المساحتين الدينية والمالية وروابط من الورد والصلاة إلى خطة اليوم والعادة المرتبطة؛ تبقى مراجعة بعض العلاقات الإنتاجية المفتوحة.

---

## المرحلة 5: الباك إند والداتابيز (Backend)

> **قاعدة إلزامية:** قبل كتابة أي كود يخص Neon أو Better Auth، يجب استدعاء GetOrRequestIntegration للتحقق من الاتصال، وقراءة skill الخاص بـ `neon-on-vercel` واتباعه حرفياً (Drizzle ORM + Better Auth).

### Milestone 5.1 — قاعدة البيانات والمصادقة

- [~] ربط تكامل **Neon** بالمشروع: اتصال Drizzle lazy وملف إعداد ومigration جاهزون، بينما اتصال Neon الفعلي ينتظر `DATABASE_URL`.
- [~] التحقق من وجود `BETTER_AUTH_SECRET`: الإعداد يرفض التشغيل الآمن عند غيابه، لكن secret الإنتاجي لم يُضف إلى البيئة المحلية.
- [x] إعداد **Better Auth** بإيميل + باسورد **فقط** (بدون OAuth أو أي طريقة أخرى).
- [x] صفحات تسجيل الدخول / إنشاء حساب بنفس الـ Design System.
- [~] حماية كل صفحات اللوحة — middleware وsession-aware navigation مضافان مع إبقاء fallback المحلي عند غياب credentials؛ ما يزال تفعيل الحماية الإنتاجية الكاملة ينتظر إعداد السر وقاعدة البيانات.
- [~] تصميم Schema أولي بـ Drizzle للأقسام الأساسية الحالية: Auth، tasks، subtasks، notes، habits، daily plan، weekly reviews؛ بقية جداول الدين والمال والمشاريع والترفيه ستضاف مع مراحلها.
  - `tasks`, `subtasks`
  - `notes`, `note_tags`
  - `boards`, `sticky_notes`, `board_groups`
  - `prayer_logs`, `prayer_settings`
  - `quran_progress`, `memorization_plan`, `favorite_ayahs`
  - `playlists`, `playlist_items`, `listening_progress`, `saved_duas`, `dhikr_sessions`
  - `habits`, `habit_logs`
  - `journal_entries`
  - `projects`, `goals`
  - `expenses`, `incomes`, `budgets`, `recurring_expenses`
  - `entertainment_items`
  - `weekly_reviews`
- [~] **كل جدول فيه `user_id` وكل استعلام مفلتر بـ `user_id` الخاص بالجلسة** — مطبق في Tasks وNotes وHabits وDaily Plan الحالية، ويجب استكماله مع بقية الدومينات.
- [x] استعلامات Parameterized فقط — لا يوجد SQL مبني بالنصوص في دفعة Backend الحالية.

### Milestone 5.2 — ربط الإنتاجية بالداتابيز

- [~] استبدال Mock Data في المهام والملاحظات والعادات وخطة اليوم بعمليات حقيقية عبر Route Handlers للقراءة والإنشاء والتحديث والأرشفة/التسجيل؛ السبورة لم تبدأ بعد، والاتصال الفعلي ينتظر credentials.
- [~] الواجهة تستخدم remote sync وOptimistic Updates للمهام والملاحظات والعادات وخطة اليوم مع localStorage fallback؛ إدخال SWR الرسمي مؤجل حتى تثبيت جلسة Auth وcredentials في البيئة.

### Milestone 5.3 — ربط القسم الديني

- [x] حفظ حقيقي لـ: حالات الصلوات، الورد وموضع القراءة، خطة الحفظ، الـ Playlists ومكان الوقوف، قوائم السماع، عدادات الأذكار، الأدعية، عبر `religious_settings` وAPI مفلتر بالجلسة.
- [x] نص القرآن والتلاوات والمواقيت تبقى من الـ APIs الخارجية — **الداتابيز تخزن تقدم المستخدم فقط** ولا تخزن المحتوى الخارجي نفسه.

### Milestone 5.4 — ربط باقي الأقسام + الداشبورد

- [~] ربط العادات واليوميات والمشاريع والأهداف والفلوس والترفيه عبر Route Handlers وremote sync وlocalStorage fallback؛ التفعيل الإنتاجي الكامل يتطلب credentials قاعدة البيانات والجلسة.
- [~] الـ **Dashboard** والـ **Weekly Review** والـ **البحث الشامل** وخطة اليوم تقرأ من الحالة الموحدة مع hydration من Route Handlers عند توفر الجلسة؛ أضيفت إلى Weekly Review مؤشرات مالية ودينية مشتقة وروابط للمساحات، وأضيفت واختُبرت روابط المصادر العميقة من خطة اليوم إلى المهام والعادات والصلاة والورد، وما يزال التحقق الإنتاجي الكامل مربوطًا بإعداد credentials.
- [~] تفعيل العلاقات بين الجداول والعناصر: حقول الربط وعزل المستخدم موجودة، وروابط السياق العميقة تعمل في المهام وخطة اليوم والملاحظات والعادات والمالية، وأضيفت روابط مشتقة في Weekly Review إلى المهمة والعادة والمشروع والهدف وروابط مباشرة إلى الدين والمال، مع اختبار anchors المصادر من خطة اليوم إلى المساحات المتخصصة؛ تبقى migration الفهارس المركبة ومراجعة بعض علاقات المجالات الإنتاجية مؤجلة حتى تثبيت تاريخ migrations بشكل آمن.
- [x] حذف كل ملفات الـ Mock Data القديمة بعد التأكد من عدم استخدامها؛ تحمّلت الصفحة الرئيسية من الحالة الموحدة بعد الحذف.

### Milestone 5.5 — التذكيرات والخصوصية والنسخ الاحتياطي

- [~] بناء نظام تذكيرات مرن للمهام والعادات والصلوات والورد والمصاريف المتكررة، مع إعدادات تمنع الإزعاج الزائد؛ التذكيرات اليومية/الأسبوعية/الشهرية تعيد الجدولة تلقائيًا عند الإكمال، وأضيف مولّد اقتراحات هادئ مشتق من خطة اليوم والعادات والمصاريف المتكررة مع منع التكرار، كما أصبح تفضيل الوضع الهادئ محفوظًا محليًا ويُستعاد بعد إعادة التحميل. يبقى التوليد الإنتاجي الكامل حسب كل مجال وإعدادات منع الإزعاج المتقدمة المرتبطة بالحساب مفتوحًا.
- [~] لا توجد مشاركة عامة مفعّلة افتراضيًا؛ تدقيق Route Handlers أكد أن مسارات البيانات الشخصية تستخدم `getCurrentUser`، وأن استثناءات القرآن والقراء والمواقيت تعرض بيانات خارجية عامة فقط. يبقى التحقق الإنتاجي الكامل مرتبطًا بإعداد credentials واختبار جلسة فعلية.
- [x] إضافة إمكانية تصدير بيانات المستخدم بصيغ JSON وCSV من مساحة الحساب، مع تصدير بعيد مشروط بجلسة المستخدم.
- [x] إضافة حذف الحساب والبيانات بشكل واضح مع تأكيد صريح قبل التنفيذ، ومسار DELETE مفلتر بالمستخدم الحالي.
- [x] توفير نسخة احتياطية واستعادة محلية من JSON، مع تصدير بعيد مشروط بالجلسة وعدم إرسال البيانات إلى خدمة خارجية أو نموذج ذكاء اصطناعي.

---

## المرحلة 6: التلميع والإطلاق (Polish)

### Milestone 6.1 — الأرشيف والتفاصيل الأخيرة
- [x] مراجعة أن كل "حذف" مهم هو أرشفة، وأن الأرشيف قابل للاسترجاع؛ الاستثناءات المتبقية تخص عناصر تابعة/جدولة/ارتباطات لا تمثل المصدر الأصلي، وهي موثقة في `verification/archive-audit.md`. اكتملت تغطية واجهة العادات بإجراء أرشفة مستقل لكل بطاقة، واختُبرت دورة الإخفاء من `/habits` ثم الظهور في `/archive` والاسترجاع بنجاح. كما يحفظ مجمّع الأرشيف الآن `habitLog` المؤرخ للعادات المؤرشفة المملوكة، فتعود العادة المستعادة بتاريخها بدل حمولة افتراضية فارغة.
- [x] مراجعة كل الـ Empty States والـ Loading States (Skeletons بنفس الـ Design System) — اكتملت المراجعة الشاملة: وُحّدت الحالات المتبقية في المهام وخطة اليوم واقتراح الترفيه وتفاصيل المشاريع، بينما بقيت الحالات الدينية الشبكية مخصصة لحالات التحميل/الخطأ ومزوّدة برسائل عربية، وجميع wrappers الكسولة تستخدم LoadingState مع `aria-busy` و`role="status"`.
- [x] مراجعة رسائل الأخطاء بالعربية بصياغة واضحة في المصادقة والمسارات الأساسية.

### Milestone 6.2 — الأداء والجودة
- [x] مراجعة سرعة التحميل وتقسيم الكود: اليوميات والترفيه والديني والسبورة تستخدم wrappers مستقلة مع `next/dynamic` و`LoadingState` عربي، مع إبقاء النصوص الخارجية الدينية خارج التخزين المحلي.
- [x] التحقق في المتصفح الفعلي من كل المسارات الرئيسية: إضافة مهمة، تعليم صلاة/تقدم ديني، تسجيل مصروف، إنشاء وسحب بطاقة على السبورة، واختيار قارئ وتشغيل تلاوة.
- [x] إضافة Security Headers الأساسية في `next.config`.

### Milestone 6.3 — أساس الموبايل
- [x] تمرير سريع على كل الصفحات للتأكد أنها لا تنكسر على الشاشات الصغيرة (الأولوية تظل للديسكتوب، لكن بدون كوارث على الموبايل).
- [x] تجهيز التطبيق ليعمل كـPWA أو Offline جزئي، مع صفحة `/offline` وService Worker وأيقونات PWA؛ أضيفت تجربة تثبيت/تحديث وإعادة محاولة واضحة للمحتوى الديني عند فشل الشبكة، بينما يبقى اختبار Service Worker الكامل معتمدًا على production/HTTPS.

### Milestone 6.4 — Definition of Excellent Product

> دفعة Weekly Review الحالية حسّنت مطابقة المقاييس مع مصادرها: إنجازات العادات تُقاس من سجل الأيام ضمن الأسبوع، واليوم الحالي يملك fallback صريحًا إلى `doneToday` بعد hydration، كما أن طلبات تسجيل/إلغاء الإنجاز ترسل `localDate` نفسه إلى الخادم.

- [x] يستطيع المستخدم فهم قيمة التطبيق وإنشاء أول خطة يوم خلال دقائق من التسجيل؛ onboarding من ثلاث خطوات يشرح القيمة، يتيح تجاوز الحقول الاختيارية، ويصل من صفحة النجاح مباشرة إلى Timeline خطة اليوم، مع اختبار متصفح موثق.
- [x] يستطيع التقاط مهمة أو ملاحظة أو مصروف من أي مكان بدون فقدان السياق.
- [~] تظهر العلاقات بين الهدف والمشروع والمهمة والعادة والمراجعة بوضوح؛ تعمل روابط المشروع والهدف في المهام وخطة اليوم والملاحظات والعادات والمالية وWeekly Review مع anchors مستقرة، وتعرض Weekly Review الآن مؤشرات الدين والمال وروابط المساحتين، كما تربط الصلاة والورد والعادة القرآنية بخطة اليوم والعادة المرتبطة، واختُبرت روابط المصادر العميقة في المتصفح؛ تبقى مراجعة بعض علاقات المجالات الإنتاجية وmigration الفهارس المركبة مفتوحة.
- [x] خطة اليوم قابلة للتعديل ولا تتخذ قرارات حساسة نيابة عن المستخدم.
- [x] كل البيانات الشخصية والدينية والمالية قابلة للتصدير والحذف، ولا تُشارك افتراضياً؛ توجد أدوات التصدير والحذف والنسخ الاحتياطي/الاستعادة في الحساب. شمل Route تصدير الحساب الآن اليوميات والترفيه أيضًا مع فلترة `user_id`، بينما يبقى اختبار تصدير Neon الحقيقي معتمدًا على جلسة تسجيل دخول.
- [x] التطبيق يظل مفيداً حتى لو استخدم المستخدم قسمين أو ثلاثة فقط، ولا يفرض عليه تعبئة كل الأقسام؛ اختُبرت حالة مستخدم جديد بحالة محلية فارغة في Dashboard وظهرت الإرشادات العربية لخطة اليوم والمهام والعادات والملاحظات، مع بقاء النماذج والتنقل متاحين وعدم ظهور `NaN` أو خطأ hydration، كما تم توثيق تدفق onboarding إلى أول خطة يوم.

---

## ملاحق

### سجل دفعة 2026-08-16 — حالات الصلاة التفصيلية

أضيفت حالات `on-time` و`congregation` و`qada` و`missed` إلى الحالة الموحدة، مع إبقاء `pending` و`done` للتوافق، وتحديث المزامنة والـAPI وواجهة القسم الديني وDashboard وWeekly Review. نجحت فحوص TypeScript وESLint وBuild، ثم نجح اختبار المتصفح اليدوي على `/religious#prayer-tracker` و`/` و`/review`: ظهرت القوائم الخمس، تغيّرت صلاة الفجر إلى «جماعة» وانعكست فورًا مع بقاء العداد، ثم أُعيدت إلى «في وقتها». سُجلت النتائج في `verification/interaction-smoke-tests.md`.

### سجل دفعة 2026-08-16 — تحليل حالات الصلاة وإصلاح أرشفة العادات

أضيفت عدادات `statusCounts` وأسماء `missedByPrayer` إلى سجل الصلاة، مع تطبيع آمن في `backend-sync` وتنقية محدودة في Route الديني وتوافق مع السجلات القديمة. تعرض المساحة الدينية تحليل آخر 30 يومًا لأنواع الحالات وتحدد الصلاة الأكثر فواتًا، وتم اختبار تحويل الفجر مؤقتًا إلى «فائتة» ثم إعادته إلى «في وقتها» مع انعكاس العداد والتحليل. كما أُصلح مسار أرشفة العادات ليستخدم `action=archive` بدل حذف سجل إنجاز اليوم، وأصبح مجمّع الأرشيف يضم `habitLog` التاريخي للعادات المؤرشفة مع فلترة الملكية وحدّ قراءة صريح، واكتملت دورة الإخفاء من `/habits` ثم الظهور في `/archive` والاستعادة والعودة إلى البطاقة الأصلية مع بقاء سجل اليوم. نجحت TypeScript وESLint وBuild و`git diff --check`، وسُجلت نتائج المتصفح في `verification/interaction-smoke-tests.md` و`verification/archive-audit.md`.

### أ. مصادر الـ APIs الخارجية (القسم الديني)
| الغرض | المصدر المقترح | ملاحظات |
|---|---|---|
| نص القرآن | AlQuran Cloud API أو Quran.com API | نص مشكول كامل — يُجلب ولا يُكتب يدوياً أبداً |
| مواقيت الصلاة | Aladhan API | حسب المدينة/الإحداثيات + التاريخ الهجري |
| التلاوات الصوتية | mp3quran.net أو everyayah.com | روابط mp3 مباشرة حسب القارئ والسورة |

> قبل الاستخدام تحقق من أن الـ API يعمل فعلاً ومن شكل الاستجابة الحالي — لا تعتمد على الذاكرة.

### ب. ترتيب الأولوية إذا ضاق الوقت داخل أي مرحلة
1. الوظيفة الأساسية للقسم (CRUD والعرض)
2. الإحصائيات والرسوم
3. الكماليات (فلاتر إضافية، تحويلات، علامات خاصة)

### ج. تعريف "تمّ" (Definition of Done) لأي Milestone
- الواجهة مطابقة للـ Design System المستخرج من ملف التصميم.
- كل النصوص بالعربية والاتجاه RTL سليم.
- لا أخطاء في الكونسول.
- تم التحقق من الوظيفة في المتصفح فعلياً وليس فقط "الكود يبني بنجاح".

### سجل دفعة 2026-08-16 — مطابقة Weekly Review ومزامنة سجل العادات

وُحّدت قراءة إنجازات العادات في Weekly Review مع منطق مساحة العادات: يُستخدم `doneToday` لليوم الحالي بعد hydration، وتُستخدم `history` المؤرخة لبقية أيام نطاق المراجعة. كما أصبحت طلبات تسجيل وإلغاء إنجاز العادة تمرر `localDate` صراحة إلى Route العادات، لتقليل تعارضات PATCH/DELETE وضمان حفظ `habitLog` لليوم الصحيح. في المتصفح، سُجلت «قراءة القرآن» ثم أُلغيت وأعيد تسجيلها؛ ظهرت في `/review` قيمة «3 إنجازات عادات خلال 1 أيام هذا الأسبوع» ولقطة «3 · 1 أيام». وحدة التحكم خالية من أخطاء hydration/runtime، ونجحت TypeScript وESLint وBuild. تبقى مراجعة الإنتاج بالـcredentials وبعض تغطية العلاقات والأرشيف العام مفتوحة كما هي معلّمة أعلاه.

### سجل دفعة 2026-08-16 — إصلاح رابط سياق العادة من التذكيرات
اكتُشف أن `sourceHref` كان ينتج `/habits#habit-habit-1` للتذكيرات المرتبطة بعادة، بينما anchors بطاقات العادات هي `habit-*` مباشرة. تم إصلاح التحويل إلى `/habits#${sourceId}` دون تغيير روابط المهام أو خطة اليوم أو المالية. أُضيف تذكير محلي مؤقت بمصدر `habit-1`، ونُقر رابط «فتح السياق» من `/reminders`، فوصل إلى `/habits#habit-1` وظهرت بطاقة «قراءة القرآن» في موضعها الصحيح. أزيلت بيانات الاختبار بعد التحقق، وتبقى التغطية الإنتاجية المرتبطة بالـcredentials مفتوحة كما هي معلّمة في الخطة.

### سجل دفعة 2026-08-16 — إصلاح روابط سياق التذكيرات للمهام والمالية والدين

صُححت دالة `sourceHref` لتطابق anchors الفعلية: أصبحت مصادر المهام والمالية ذات البادئة تذهب مباشرة إلى `/tasks#task-*` و`/money#finance-*` دون تكرار البادئة، وأضيف دعم للمصدرين الدينيين `prayer-tracker` و`quran-progress`. أُنشئت عناصر اختبارية مؤقتة للمصادر الأربعة، ظهرت الروابط الصحيحة، وفُتح رابط المالية بنجاح إلى anchor العملية. أزيلت بيانات الاختبار بعد التحقق، وخَلَت وحدة التحكم من أخطاء hydration/runtime. أُبقيت البنود الإنتاجية المعتمدة على credentials مفتوحة كما هي معلّمة في الخطة.

### سجل دفعة 2026-08-16 — حفظ تفضيل الوضع الهادئ للتذكيرات
أصبح تفضيل الوضع الهادئ في `/reminders` محفوظًا في localStorage بالمفتاح `personal-command-center-reminder-preferences-v1`، مع قراءة آمنة بعد hydration والتحقق من نوع القيمة ومسح الحمولة التالفة. اختُبر تبديل الوضع إلى «عرض اقتراحات أكثر»، وإعادة تحميل الصفحة، ثم استعادة التفضيل، وأُعيد الوضع الهادئ إلى الحالة الافتراضية بعد الاختبار. نجحت TypeScript وESLint وBuild و`git diff --check`، ووحدة تحكم المتصفح خالية من أخطاء hydration/runtime. ما يزال حفظ التفضيل على مستوى الحساب وإعدادات الإشعارات الإنتاجية المتقدمة مفتوحًا بسبب اعتماده على credentials ونطاق Backend إضافي.

### سجل دفعة 2026-08-16 — حفظ تكرار العمليات المالية داخل الأرشيف

أُصلحت حمولة مجمّع الأرشيف لتحتفظ بحقل `recurrence` للعمليات المالية المؤرشفة، بحيث لا تتحول المصروفات الشهرية أو الأسبوعية إلى عمليات غير متكررة بعد الاستعادة. أُرشف «اشتراك أدوات العمل» الشهري من `/money` ثم استُعيد من `/archive`، وعاد إلى بطاقة المصاريف المتكررة مع الوسم «شهري» وموعده يوم 12 من كل شهر. نجحت دورة الأرشفة والاستعادة، وخَلَت وحدة تحكم المتصفح من أخطاء hydration/runtime. تبقى تغطية علاقات الأرشيف الأخرى والمراجعة الإنتاجية المعتمدة على credentials مفتوحة كما هي معلّمة في الخطة.

### سجل دفعة 2026-08-16 — شمول اليوميات والترفيه في تصدير الحساب
اكتُشف أن Route `/api/account/export` كان يصدّر المهام والملاحظات والعادات والمالية والتذكيرات وغيرها، لكنه يستبعد `journalEntry` و`entertainmentItem` رغم وجودهما في الحالة المحلية وعقد الاستعادة. أُضيف استعلام كل جدول بفلترة `user_id`، وأُدرجت النتائج في الحمولة باسمَي `journalEntries` و`entertainment` دون تغيير النسخة المحلية أو عقد الاستيراد. اختُبرت صفحة `/account` وزر تنزيل النسخة المحلية وظهرت رسالة نجاح، وخَلَت وحدة التحكم من أخطاء hydration/runtime. تعذّر اختبار Route Neon الحقيقي لأن جلسة المتصفح كانت في الوضع المحلي، ولذلك بقي هذا الجزء موثقًا دون ادعاء تحقق إنتاجي. نجحت TypeScript وESLint وBuild و`git diff --check`.

### سجل دفعة 2026-08-16 — توحيد تكرار التذكيرات وإعادة الجدولة الشهرية

وُحّدت قيم تكرار التذكيرات القياسية في أداة `lib/reminder-utils.ts`، فأصبحت aliases القديمة مثل `شهري` و`أسبوعي` و`يومي` تُطبّع إلى `شهريًا` و`أسبوعيًا` و`يوميًا`، مع الحفاظ على القيم المخصصة غير المعروفة للتوافق. استُخدم التطبيع في `initialReminders` و`addReminder` و`toggleReminder`، وفي hydration وطلبات الإنشاء والتحديث البعيدة داخل `backend-sync`، وكذلك في POST وPATCH لمسارات `/api/reminders` مع استمرار فلترة الملكية عبر الجلسة.

اختُبرت دورة كاملة في المتصفح على `/reminders`: أُنشئ «اختبار تذكير شهري» باختيار «شهريًا»، ثم أُكمل وبقي في قائمة التذكيرات المفتوحة وتحول موعده إلى «الشهر القادم». أظهر فحص وحدة التحكم رسائل React DevTools وHMR فقط دون أخطاء تطبيقية. نجحت TypeScript وESLint و`git diff --check` و`next build`، وسُجلت النتيجة في `verification/reminder-recurrence-browser.md` و`verification/interaction-smoke-tests.md`. بقي اختبار Route Neon بجلسة حقيقية خارج المتناول في بيئة localStorage لغياب credentials، دون ادعاء تحقق إنتاجي.

### سجل دفعة 2026-08-16 — استرجاع عناصر خطة اليوم المؤجلة

أُضيف `restorePlanItem` إلى الحالة الموحدة، وأصبح Timeline `/daily-plan` يعرض زر «إرجاع إلى الخطة» للعناصر المؤجلة ويعيدها إلى الحالة `pending` مع تحديث المزامنة البعيدة عند توفرها. اختُبرت الدورة على عنصر مرتبط بمهمة ومشروع وهدف: تأجيل ثم إرجاع، مع بقاء روابط السياق والاقتراح الحالي سليمة. نجحت TypeScript وESLint و`git diff --check` و`next build`، وخَلَت وحدة تحكم المتصفح من أخطاء تطبيقية. التفاصيل محفوظة في `verification/daily-plan-snooze-browser.md` و`verification/interaction-smoke-tests.md`. ما يزال التخطي والنقل إلى يوم آخر وحفظ Neon الفعلي مفتوحًا.

### سجل دفعة 2026-08-16 — تخطي عناصر خطة اليوم واسترجاعها

أُضيفت حالة `skipped` إلى عقد `PlanItem`، وتوسعت الحالة الموحدة والمزامنة البعيدة وRoute `/api/daily-plan/[id]` لقبولها مع استمرار حماية الملكية. تعرض Timeline الآن زر «تخطي» للعناصر النشطة، وتعرض «إرجاع إلى الخطة» للعناصر المتخطاة أو المؤجلة. حُسب التقدم من العناصر غير المتخطاة حتى لا يعاقب المستخدم على تخطٍ مقصود، وبقيت روابط السياق للمهمة والمشروع والهدف سليمة. اختُبرت الدورة في المتصفح، ونجحت TypeScript وESLint و`git diff --check` و`next build`، وخَلَت وحدة التحكم من أخطاء تطبيقية. التفاصيل في `verification/daily-plan-skip-browser.md` و`verification/interaction-smoke-tests.md`. ما يزال النقل إلى يوم آخر والاختبار الفعلي على Neon مفتوحين.

### سجل دفعة 2026-08-16 — تحرير عناصر خطة اليوم وتوحيد حالة skipped
أُضيف إجراء `updatePlanItem` إلى الحالة الموحدة، وأصبح Timeline `/daily-plan` يعرض زر «تعديل العنصر» للعناصر النشطة مع نموذج RTL لتحرير العنوان والوقت وحفظهما عبر المزامنة البعيدة عند توفرها، مع استمرار localStorage fallback. كما وُحّد Route إنشاء عناصر خطة اليوم ليقبل `skipped` مثل Route التحديث، حتى لا تختلف عقود POST وPATCH.
اختُبرت دورة كاملة في المتصفح على عنصر «تحضير عرض تقديمي للمشروع»: فُتح النموذج، عُدّل العنوان والوقت إلى قيم تجريبية وانعكسا فورًا في Timeline وبطاقة التركيز، ثم أُعيدت القيم الأصلية وحُفظت. فحص وحدة التحكم أظهر React DevTools وHMR فقط دون أخطاء JavaScript أو مزامنة. نجحت TypeScript وESLint و`git diff --check` و`next build`، وسُجلت النتيجة في `verification/daily-plan-edit-browser.md` و`verification/interaction-smoke-tests.md`. ما يزال النقل إلى يوم آخر والاختبار الفعلي على Neon مفتوحين.

### سجل دفعة 2026-08-16 — نقل عناصر خطة اليوم بين التواريخ
أُضيف `localDate` إلى عقد `PlanItem` ومحوّل المزامنة البعيدة، وأصبح Route تحديث خطة اليوم يقبل تاريخًا بصيغة `YYYY-MM-DD` مع استمرار فلترة الملكية. أضيف إجراء `movePlanItem` إلى الحالة الموحدة وواجهة نقل داخل Timeline، مع إبقاء التغيير متفائلًا وداعمًا لـlocalStorage fallback.
اختُبرت الدورة في المتصفح بنقل «تحضير عرض تقديمي للمشروع» من 16 أغسطس إلى 17 أغسطس؛ اختفى من اليوم الأصلي وظهر في التاريخ الهدف بعد تصحيح guard كان يحتوي escape مزدوجًا. أُعيد العنصر إلى 16 أغسطس ونُظفت بيانات الاختبار. نجحت TypeScript وESLint و`git diff --check` و`next build`، ولم تظهر أخطاء تطبيقية في وحدة التحكم. التفاصيل محفوظة في `verification/daily-plan-move-browser.md` و`verification/interaction-smoke-tests.md`.
يبقى قيد واجهة `min={viewDate}` مقصودًا في هذه الدفعة لتوجيه النقل إلى الأيام المستقبلية؛ دعم النقل إلى تاريخ سابق يحتاج قرار UX مستقل. يبقى اختبار Route مع Neon وcredentials الفعلية مفتوحًا.

### سجل دفعة 2026-08-16 — حفظ السنن والنوافل
أصبحت حالات السنن الأربع (`duha`, `witr`, `rawatib`, `sadaqah`) جزءًا من `religious.dhikr.sunnahChecks` بدل state محلي مؤقت داخل الواجهة. أضيف تطبيع متوافق مع البيانات القديمة في الحالة الموحدة، ومزامنة بعيدة، وRoute القسم الديني، مع الحفاظ على fallback المحلي عند غياب credentials. كما أضيف `toggleSunnah` المتفائل، وتستوعب الواجهة القيم المخصصة ضمن عقد الحالة دون تغيير الهوية البصرية أو RTL.

اختُبرت دورة تفعيل «صلاة الضحى» ثم إعادة تحميل `/religious`؛ بقيت الحالة مفعّلة بعد reload، ولم تظهر أخطاء تطبيقية في console. نجحت TypeScript وESLint بلا تحذيرات و`git diff --check` و`next build`. التفاصيل محفوظة في `verification/religious-sunnah-browser.md` و`verification/interaction-smoke-tests.md`.

يبقى الاختبار الفعلي مع قاعدة Neon وcredentials مفتوحًا ضمن البنود المرتبطة بالبيئة، كما تبقى مراجعة بعض العلاقات الإنتاجية وملفات migrations المؤجلة مفتوحة.

### سجل دفعة 2026-08-16 — تخصيص هدف الورد اليومي
أضيف هدف ورد يومي قابل للتخصيص داخل `religious.quranProgress.targetMinutes`، مع قائمة عربية بقيم 10 و20 و30 و45 و60 دقيقة في مساحة القسم الديني. أضيف setter موحّد يطبّع الهدف ضمن نطاق آمن ويقلّص `completedMinutes` عند الحاجة، مع استمرار الحفظ المحلي والمزامنة البعيدة. كما وُحّدت طبقات الحالة و`backend-sync` وRoute القسم الديني على نفس التطبيع، مع الحفاظ على القيم القديمة ضمن نطاق الواجهة.
اختُبرت دورة اختيار 30 دقيقة من `/religious`: انعكس التقدم من 8/20 إلى 8/30 بنسبة 27%، ثم بقيت القيمة بعد إعادة تحميل الصفحة عبر localStorage fallback. فحص وحدة التحكم أظهر رسائل React DevTools وHMR فقط دون أخطاء تطبيقية. نجحت TypeScript وESLint و`git diff --check` و`next build`، والتفاصيل محفوظة في `verification/religious-wird-target-browser.md` و`verification/interaction-smoke-tests.md`.
يبقى الاختبار الفعلي مع Neon وcredentials مفتوحًا ضمن البنود البيئية، كما تبقى مراجعة العلاقات الإنتاجية وملفات migrations المؤجلة مفتوحة.

### سجل دفعة 2026-08-16 — تحرير المهام inline
أضيف محرر مباشر داخل بطاقة المهمة في `/tasks` لتحرير العنوان والأولوية والموعد والتصنيف عبر `updateTask` من الحالة الموحدة. يحافظ النموذج على RTL والـsemantic tokens، ويظهر عند hover/focus دون تغيير تخطيط مساحة المهام، مع استمرار المزامنة البعيدة وlocalStorage fallback.
اختُبرت دورة كاملة على `task-1`: فُتح النموذج، عُدّلت الحقول الأربع، حُفظت التغييرات وانعكست مباشرة في البطاقة، ثم أُعيدت القيم الأصلية وحُفظت الاستعادة. لم تظهر أخطاء JavaScript أو مزامنة في console؛ ظهرت رسائل React DevTools وHMR فقط. نجحت TypeScript وESLint و`git diff --check` و`next build`، والتفاصيل محفوظة في `verification/tasks-edit-browser.md` و`verification/interaction-smoke-tests.md`.
يبقى الاختبار الفعلي مع Neon وcredentials مفتوحًا ضمن البنود البيئية، ولا توجد بيانات اختبارية متبقية في الحالة المحلية.

### سجل دفعة 2026-08-16 — خطة حفظ القرآن
أضيفت خريطة `memorizationSurahStatus` إلى `ReligiousState.quran` بثلاث حالات عربية تنظيمية: `learning` («بحفظها»)، `reviewing` («تحتاج مراجعة»)، و`memorized` («حفظتها»). أضيف `setMemorizationSurahStatus` إلى الحالة الموحدة مع تطبيع رقم السورة، وحُفظت الخريطة ضمن JSONB الحالي `quranProgress` في API القسم الديني، مع تطبيعها في `backend-sync` وlocalStorage hydration. لا توجد migration جديدة ولا تُخزّن نصوص الآيات.

أضيفت قائمة حالات داخل بطاقة «خطة الحفظ» في `/religious` باستخدام الكروت والـsemantic tokens القائمة، مع عداد للسور المحفوظة. اختُبرت دورة تغيير حالة سورة البقرة إلى «حفظتها»، فارتفع العداد من 2/7 إلى 3/7، ثم بقيت القيمة بعد إعادة التحميل عبر localStorage fallback. فحص وحدة التحكم أظهر React DevTools وHMR فقط دون أخطاء تطبيقية. نجحت TypeScript وESLint و`git diff --check` و`next build`، والتفاصيل في `verification/quran-memorization-browser.md` و`verification/interaction-smoke-tests.md`.

يبقى الاختبار الفعلي مع Neon وcredentials مفتوحًا ضمن البنود البيئية، كما تبقى قائمة السور الحالية منتقاة من السور الأكثر استخدامًا وليست فهرس المصحف الكامل.

### سجل دفعة 2026-08-16 — أعمدة زمنية للمهام
أضيفت أربعة أعمدة زمنية داخل مساحة `/tasks`: «النهاردة»، «بكرة»، «الأسبوع ده»، و«بعدين»، مع تصنيف مشتق من `dueLabel` الحالي دون تغيير عقد البيانات. تدعم كل بطاقة النقل بالسحب والإفلات وبأزرار وصول مباشرة، وتستخدم مرجعًا فوريًا للعنصر المسحوب حتى لا تفشل أحداث الإسقاط السريعة. تمر عملية النقل عبر `updateTask` الحالي، فتستفيد من التحديث المتفائل والمزامنة البعيدة وlocalStorage fallback.
اختُبرت الصفحة بصريًا، ثم نُقلت «حجز موعد الدكتور» إلى «بكرة» فعاد عداد «النهاردة» من 4 إلى 3 وارتفع عداد «بكرة» من 1 إلى 2، وأُعيدت المهمة إلى «النهاردة» فعادت العدادات الأصلية. نجحت TypeScript وESLint و`git diff --check` و`next build`، والتفاصيل في `verification/tasks-time-buckets-browser.md` و`verification/interaction-smoke-tests.md`. لا توجد migration جديدة.

## 2026-08-16 — المهام المتكررة وقائمة أقل من دقيقتين
- **المسار:** `/tasks`
- **الدورة:** أضيفت خيارات «بدون»، «يومي»، و«أسبوعي» عند إنشاء المهمة، مع دعم تعديل التكرار داخل نموذج تحرير المهمة. أُنشئت مهمة اختبار يومية فظهرت في عمود «النهاردة» مع شارة «يومية»، كما ظهرت المهمة الأسبوعية الحالية في «الأسبوع ده» مع شارة «أسبوعية».
- **الإضافة الجانبية:** قسم «أقل من دقيقتين» يعرض المهام المفتوحة منخفضة الأولوية في موعد النهاردة مع زر إكمال مباشر.
- **التحقق:** TypeScript وESLint و`git diff --check` و`next build` ناجحة، والفحص المرئي على localhost ناجح.
- **النطاق:** إعادة استخدام حقل `Task.recurring` الموجود في الحالة وAPI؛ لا migration جديدة. التفاصيل في `verification/tasks-recurrence-quick-list-browser.md`.

## 2026-08-16 — الآيات المفضلة والتدبر
- **النطاق:** إضافة مراجع آيات مفضلة مع خانة تدبر شخصية داخل `quranProgress`، وتطبيعها محليًا وبعيدًا عبر JSONB الحالي دون migration جديدة.
- **الواجهة:** زر حفظ/إزالة لكل آية مع بطاقة «آياتي المفضلة» وخانة تدبر قابلة للتحرير بالحفظ عند مغادرة الحقل، مع الالتزام بالكروت الحالية وRTL.
- **التحقق:** TypeScript وESLint و`git diff --check` و`next build` ناجحة، ثم حُفظت الآية الأولى من الفاتحة وأضيف تدبر، وبقي كلاهما بعد إعادة التحميل على localhost. فحص console أظهر React DevTools وHMR فقط دون أخطاء تطبيقية.
- **التفاصيل:** `verification/quran-favorites-browser.md`.
- **البند المرتبط:** الآيات المفضلة — مكتمل؛ خانة التدبر — مكتملة.

## 2026-08-16 — مجموعات السبورة وتحويل الورقة
- **المسار:** `/board`
- **الدورة:** أضيفت مجموعات قابلة للحفظ لكل سبورة، ومرشح مناطق التجميع، وتعيين مباشر لكل Sticky Note، مع ترحيل آمن للبيانات القديمة. أضيف تحويل الورقة إلى مهمة أو ملاحظة مع أرشفة المصدر، وتم تمرير `sourceNoteId` عند إنشاء المهمة.
- **التحقق:** نجحت TypeScript وESLint و`git diff --check` و`next build`. في المتصفح ظهرت ثلاث أوراق ومناطق التجميع؛ نُقلت «فكرة للمنتج» إلى «التقاط» فتغيرت العدادات، ثم حُولت مؤقتًا إلى مهمة فاختفت الورقة وظهرت المهمة، وبعدها حُذفت بيانات الاختبار وأعيدت الورقة والمجموعة الأصلية، وتأكدت الإعادة بعد إعادة التحميل.
- **النطاق:** استخدام JSON المحلي الحالي وواجهات المتجر والمزامنة القائمة؛ لا migration جديدة. التفاصيل في `verification/board-groups-browser.md`.
- **ملاحظة:** ظهر تحذير React عام عن hydration mismatch في جلسة التطوير دون خطأ JavaScript من منطق السبورة؛ يحتاج عزلًا مستقلًا إذا تكرر خارج هذه الجلسة.
- **البنود المرتبطة:** مناطق تجميع السبورة — مكتملة؛ تحويل ورقة السبورة إلى مهمة/ملاحظة — مكتمل.

## 2026-08-16 — عداد الصلاة وحالات الشبكة
- **المسار:** `/religious`
- **الدورة:** أضيف عداد تنازلي للصلاة القادمة يعتمد على المواقيت المحفوظة، مع انتقال تلقائي إلى فجر اليوم التالي بعد انتهاء آخر صلاة. يدعم المكوّن تحديثًا كل ثانية دون تغيير حالات الصلاة أو سجلها.
- **الاعتمادية:** تم التحقق من Route المواقيت محليًا عبر AlAdhan؛ أعاد HTTP 200 وJSON مطبّعًا لمدينة القاهرة والتاريخين الميلادي والهجري ومواقيت الصلوات الخمس. عند محاكاة فشل الشبكة بقيت آخر مواقيت ناجحة ظاهرة، وتحولت الحالة إلى رسالة خطأ قابلة للفهم دون انهيار الصفحة.
- **التحقق:** نجحت TypeScript وESLint و`git diff --check` و`next build`، ثم اختُبر تناقص العداد والانتقال إلى اليوم التالي في المتصفح، وفُحص console دون أخطاء JavaScript أو hydration جديدة مرتبطة بالدفعة.
- **النطاق:** لا migration جديدة ولا secrets؛ تُحفظ إعدادات المستخدم وآخر المواقيت فقط، ولا تُخزّن النصوص الدينية الخارجية كمحتوى موثوق. التفاصيل في `verification/prayer-countdown-browser.md`.
- **البند المرتبط:** مواقيت الصلاة من API مع عداد تنازلي — مكتمل.


## 2026-08-17 — تحرير الملاحظات inline وإصلاح hydration

- **المسار:** `/notes`
- **الدورة:** أُضيف تحرير inline لعنوان الملاحظة ومحتواها وتصنيفها، مع حفظ متفائل وإلغاء يعيد البطاقة إلى وضع العرض، بالاعتماد على `updateNote` المتصل بالـRoute المحمي والمزامنة المحلية.
- **إصلاح الاستقرار:** عولج اختلاف React hydration الناتج عن قراءة localStorage أثناء SSR؛ أصبحت تهيئة المتجر ثابتة على defaults أثناء العرض الأول، ثم يُحمّل localStorage داخل `useEffect` مع منع الكتابة قبل اكتمال hydration.
- **التحقق:** TypeScript وESLint و`git diff --check` و`next build` ناجحة. اختبار المتصفح أثبت الحفظ، الإلغاء، إعادة التحميل، RTL، واختفاء hydration Issue.
- **التفاصيل:** `verification/notes-inline-edit-browser.md`.
- **الحالة:** مكتمل محليًا، وجاهز للـcommit والدفع بعد مراجعة diff النهائية.


## 2026-08-17 — عداد الصلاة في Dashboard

أضيفت بطاقة عداد الصلاة القادمة إلى `/` باستخدام utility مشتركة في `lib/prayer-countdown.ts` بدل تكرار منطق الحساب بين Dashboard و`/religious`. تبدأ ساعة Dashboard بعد hydration وتُحدّث كل ثانية، وتعرض اسم الصلاة وموعدها ووسم «غدًا» عند الانتقال لليوم التالي، مع fallback آمن يعرض الصلاة pending قبل جاهزية الساعة. اختُبرت الصفحة بصريًا وقرئت قيمة العداد من DOM؛ انتقلت من `7س 36د 11ث` إلى `7س 36د 10ث` بعد 1.5 ثانية. نجحت TypeScript وESLint و`git diff --check` و`next build`. التفاصيل في `verification/dashboard-prayer-countdown-browser.md`.


## 2026-08-17 — فلاتر المهام حسب الأولوية والتصنيف
- المسار: `/tasks`
- الدورة: أُضيفت فلاتر إضافية للأولوية والتصنيف بجانب فلاتر الحالة والبحث، مع زر لمسح الفلاتر وإعادة توزيع المهام على الأعمدة الزمنية.
- التحقق: نجح TypeScript وESLint و`git diff --check` و`next build`، واختُبرت فلاتر الأولوية والتصنيف وتقاطعها ومسحها في المتصفح.
- التفاصيل: `verification/tasks-filters-browser.md`


## 2026-08-16 — وسوم الملاحظات المتعددة

- **المسار:** `/notes`
- **الدورة:** توسّع مكوّن الملاحظات لدعم اختيار أكثر من وسم للملاحظة، وعرض الوسوم كشرائح منفصلة، والبحث والفلترة بالوسم، مع تخزين متوافق رجعيًا داخل حقل `tag` الحالي كسلسلة مفصولة بفواصل دون migration جديدة.
- **التحقق:** نجح TypeScript وESLint و`git diff --check` و`next build`، كما نجح اختبار المتصفح لإنشاء ملاحظة بثلاثة وسوم، والفلترة بوسم «شغل»، والبحث المتزامن، مع سجل console خالٍ من أخطاء التطبيق أو hydration.
- **التفاصيل:** `verification/notes-tags-browser.md`
- **ملاحظة backend:** بقي عقد API الحالي متوافقًا؛ المزامنة البعيدة الفعلية تحتاج جلسة credentials وبيانات backend اختبارية، بينما يعمل localStorage fallback والحفظ المتفائل في الواجهة.

## 2026-08-17 — Quick Capture وتثبيت الملاحظات
- **المسار:** `/notes`.
- **الدورة:** أضيف حفظ سريع للملاحظة عبر `Enter` داخل حقل العنوان مع تجاهل Enter أثناء IME، وأصبح ترتيب البطاقات يضع الملاحظات المثبتة أولًا.
- **التحقق:** أُنشئت ملاحظة عربية عبر Enter، ثم ثُبتت وانتقلت إلى مقدمة القائمة. نجحت TypeScript وESLint و`git diff --check` و`next build`.
- **التفاصيل:** `verification/notes-quick-capture-browser.md`.

## 2026-08-17 — فلترة ألوان السبورة
أُضيف مرشح ألوان لأوراق السبورة إلى جانب مرشح مناطق التجميع، مع عدادات لكل لون وإعادة ضبط تلقائية عند تبديل السبورة. اختُبر اختيار «أزرق» فعرض الورقة الزرقاء فقط، ثم أعادت «كل الألوان» الأوراق الثلاث. اختُبر تقاطع «التقاط» مع «أزرق» فظهرت حالة عدم وجود نتائج بشكل صحيح. بقيت أدوات Zoom وPan والتحويل إلى مهمة أو ملاحظة متاحة، واختُبر التكبير إلى 110% ثم «إعادة ضبط» إلى 100%. سجل Console احتوى على React DevTools وHMR فقط دون أخطاء JavaScript أو hydration. التفاصيل في `verification/board-color-filter-browser.md`. نجحت بوابات TypeScript وESLint و`git diff --check` و`next build`.

## 2026-08-17 — الاستعادة الجماعية في الأرشيف
- المسار: `/archive`
- الدورة: أضيف تحديد فردي وتحديد كل النتائج الظاهرة مع عداد للعناصر المحددة، ثم استعادة جماعية تستخدم نفس `restoreArchivedItem` الفردي دون تغيير عقد البيانات.
- التحقق: نجحت TypeScript وESLint و`git diff --check` و`next build`. اختُبر تحديد النتيجة، تفعيل زر «استعادة المحدد (1)»، تنفيذ الاستعادة، تحديث العداد إلى صفر، وظهور رسالة النجاح.
- التفاصيل: `verification/archive-bulk-restore-browser.md`.
- الحالة: ناجح، مع بقاء البحث وفلاتر الأقسام والاستعادة الفردية متاحة.


## 2026-08-17 — حجما ورقة السبورة
- **المسار:** `/board`.
- **الدورة:** أضيفت قيمة `size` للورقة بخيارين `small` و`large`، مع اختيار الحجم عند الإنشاء، وتبديل الحجم مباشرة من بطاقة الورقة، وتطبيق عرض `w-40` للصغير و`w-72` للكبير. الأوراق القديمة التي لا تحتوي القيمة تستخدم `small` تلقائيًا، مع استمرار الحفظ في `personal-command-center-board-v2`.
- **التحقق:** أُنشئت ورقة «ورقة اختبار كبيرة» باختيار «كبير»، وظهرت بعرض واسع. ثم تم تبديل حجمها من بطاقة الورقة، وفحص localStorage أكد `size: large`، وإعادة تحميل `/board` أبقت القيمة. ظهرت خيارات الحجم العربية في نموذج الإنشاء وعلى البطاقات، دون كسر RTL أو أدوات Zoom/Pan أو السحب.
- **التفاصيل:** `verification/board-note-size-browser.md`.
- **الحالة:** ناجح، مع نجاح TypeScript والفحص البصري والتفاعلي قبل بوابات ESLint والبناء.

## 2026-08-17 — Badge ملون لأولوية المهام
- **المسار:** `/tasks`.
- **الدورة:** حُسّنت شارة الأولوية داخل بطاقة المهمة لتظهر على جميع المقاسات، مع نقطة لون وحدود وخلفيات Semantic مميزة للأولوية العالية والمتوسطة والمنخفضة، وإضافة `aria-label` عربي واضح.
- **التحقق:** فُتحت صفحة المهام، واختُبر فلتر «أولوية عالية» فظهرت شارتان «عالية» بالكلاس الدلالي `bg-warning/15`، ثم فلتر «أولوية متوسطة» فظهرت ثلاث شارات بالكلاس `bg-accent`، ثم فلتر «أولوية منخفضة» فظهرت شارتان بالكلاس `bg-muted`. لم تختفِ الشارات على العرض الحالي، وبقيت الأعمدة والفلاتر ونموذج الإضافة سليمة.
- **التفاصيل:** `verification/tasks-priority-badge-browser.md`.
- **الحالة:** ناجح، مع نجاح TypeScript والفحص البصري وفحص DOM قبل بوابات ESLint والبناء.

## 2026-08-17 — قرار Weekly Review قابل للتنفيذ
- أضيف إلى بطاقة «قرار الأسبوع القادم» في `/review` زر يحول الهدف المكتوب مباشرة إلى مهمة بالفئة «مراجعة أسبوعية»، الأولوية المتوسطة، وموعد «الأسبوع ده».
- يحفظ التحويل المراجعة كمسودة تلقائيًا قبل إنشاء المهمة، ويمنع التكرار عبر مطابقة العنوان والفئة؛ بعد الإنشاء يظهر الزر «أضيفت إلى المهام» disabled.
- تم اختبار إدخال «مراجعة خطة المنتج» والتحويل، ثم إعادة التحميل؛ بقي الهدف محفوظًا، ووجد فحص localStorage مهمة واحدة فقط بالخصائص المتوقعة.
- التوثيق: `verification/weekly-review-actionable-goal-browser.md` و`verification/interaction-smoke-tests.md`.
- الحالة: اختبار المتصفح ناجح؛ بوابات TypeScript وESLint و`git diff --check` ناجحة، وتبقى إعادة تشغيل `next build` قبل commit.

## 2026-08-17 — حفظ آخر آية مقروءة في المصحف
- أضيف تفاعل «احفظ الموضع» بجوار كل آية في `/religious` مع تمييز الآية المحفوظة وإظهار موضع السورة والآية داخل بطاقة التلاوة.
- أُعيد استخدام عقد `QuranPosition` و`saveQuranPosition` الحاليين، مع الحفاظ على `positionSeconds` و`reciterId` عند وجودهما ودون إضافة migration أو تخزين نص القرآن.
- اختبار المتصفح: حُفظت الآية الثانية من سورة الفاتحة، فتحولت الشارة إلى «آخر موضع» وظهر `aria-pressed="true"`، ثم بقيت الحالة بعد إعادة تحميل الصفحة عبر localStorage.
- نُظفت بيانات الاختبار المحلية بعد التحقق. التفاصيل في `verification/quran-last-read-ayah-browser.md` و`verification/interaction-smoke-tests.md`.
- النتيجة الحالية: اختبار المتصفح وTypeScript وESLint و`git diff --check` ناجحة؛ يلزم تشغيل `next build` قبل commit.

## 2026-08-17 — زر «أكمل من حيث توقفت» للمصحف
- **النطاق:** أضيف زر استئناف في بطاقة التلاوة يعيد اختيار السورة المحفوظة ويشغّل تلاوتها، مع إبقاء تمييز الآية الأخيرة وإمكانية التمرير إليها بعد تحميل النص.
- **التحقق:** غُيّرت السورة مؤقتًا ثم أُعيدت إلى سورة الفاتحة بالقيمة `1` عند الضغط على الاستئناف، وبقيت الآية الثانية محددة كآخر موضع. فحص DOM أكد وجود الزر وحالة `aria-pressed="true"`، ثم أُوقفت التلاوة بعد الاختبار.
- **الاستقرار:** لم يتغير عقد `QuranPosition` أو التخزين المحلي أو مصدر النص الخارجي، ولم تُضف migration أو secrets. نجحت TypeScript وESLint و`git diff --check` و`next build` (`BUILD_EXIT=0`).
- **التفاصيل:** `verification/quran-last-read-ayah-browser.md` و`verification/interaction-smoke-tests.md`.
- **الحالة:** جاهزة للـcommit والدفع.

### سجل دفعة 2026-08-17 — أنواع الورد اليومي والتقدم الصفحي
أصبح الورد اليومي قابلًا للتبديل بين الدقائق والصفحات ونصف الجزء والجزء الكامل. أضيفت حقول `wirdMode`, `targetPages` و`completedPages` مع تطبيع متوافق مع الحالات القديمة، وحُدّث المتجر وطبقة المزامنة وRoute القسم الديني لحفظها محليًا وخلفيًا. تحسب الواجهة النسبة من الهدف المختار وتعرض الوحدة والجزء المتبقي، مع إبقاء وضع الدقائق وسلوكه السابق متوافقين.

اختُبرت أوضاع الصفحات ونصف الجزء والجزء الكامل من `/religious`، ثم أُعيد الوضع إلى الدقائق للتحقق من التوافق الرجعي. ظهر الانتقال من `1 من 5 صفحة` إلى الحسابات الخاصة بكل وحدة، ثم بقي `wirdMode: "minutes"` والهدف `10 دقائق` والتقدم `8 من 10 دقيقة` بعد إعادة التحميل عبر localStorage fallback. التفاصيل محفوظة في `verification/daily-wird-mode-browser.md` و`verification/interaction-smoke-tests.md`.

يبقى الاختبار الفعلي مع Neon وcredentials مفتوحًا ضمن البنود البيئية، كما تبقى مراجعة migrations المؤجلة مفتوحة.
### سجل دفعة 2026-08-17 — جدول مراجعة خطة حفظ القرآن
أضيف إلى بطاقة «خطة الحفظ» جدول مراجعة مقترح مشتق من حالة كل سورة: السور الجاري حفظها تُقترح مراجعتها اليوم، والسور التي تحتاج مراجعة غدًا، والسور المحفوظة بعد 7 أيام للتثبيت. يعرض الجدول اسم السورة وحالتها وموعد المراجعة والخطة المختصرة، ويتحدث فورًا عند تغيير الحالة مع بقاء التخزين المحلي والمزامنة الحالية دون migration جديدة.
اختُبر `/religious` بصريًا وتفاعليًا؛ ظهر الجدول RTL داخل بطاقة التصميم الحالية، وتغيرت سورة البقرة من «حفظتها» إلى «تحتاج مراجعة» فانعكس الصف إلى «غدًا / مراجعة قريبة» وانخفض عداد المحفوظ من 3 إلى 2، ثم أعيدت الحالة إلى «حفظتها» بعد الاختبار. التفاصيل في `verification/quran-review-schedule-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات TypeScript وESLint و`git diff --check` و`next build` قبل اختبار المتصفح. لا توجد secrets أو migrations جديدة.
### سجل دفعة 2026-08-17 — فلترة المهام حسب الحالة
أضيف فلتر حالة مستقل إلى مساحة `/tasks` يغطي الحالات الثلاث الفعلية في عقد المهمة: «لم تبدأ» (`todo`)، «جارية» (`in-progress`)، و«مكتملة» (`done`). يعمل الفلتر مع تبويبات اليوم/المفتوحة/المكتملة والبحث وفلترة الأولوية والتصنيف، وتظهر «مسح الفلاتر» عند تفعيل أي فلتر إضافي.
اختُبرت الحالات الثلاث بصريًا وتفاعليًا: ظهرت مهمة واحدة في الحالة الجارية، وأربع مهام لم تبدأ، ومهمتان مكتملتان، ثم عاد العرض الكامل بعد اختيار «كل الحالات» دون تعديل على بيانات المهام. التفاصيل في `verification/tasks-status-filter-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات TypeScript وESLint و`git diff --check` و`next build`، ولم تُضف migration أو secrets.


## سجل دفعة — ترتيب خطة اليوم حسب الوقت والأولوية — 2026-08-17

أضيف ترتيب صريح لعناصر `/daily-plan` حسب وقت العنصر، ثم أولوية المهمة عند تساوي الوقت، مع رسالة عربية تشرح المنطق وتؤكد عدم تعديل المواعيد تلقائيًا. تم الحفاظ على إجراءات الإكمال والتأجيل والتخطي والإرجاع والتعديل والنقل وروابط السياق. أُغلقت بنود Timeline وخطة اليوم والمهام والملاحظات والسبورة التي كانت منفذة فعليًا ومثبتة بسجلات التحقق، مع إبقاء التخصيص المتقدم في Onboarding وتصفح أجزاء القرآن وجلب النص الكامل ضمن البنود المفتوحة.

نتيجة الجودة: نجح `tsc --noEmit` وESLint و`git diff --check` و`next build`. نجح الاختبار البصري والتفاعلي على `http://localhost:3004/daily-plan` للتاريخ `2026-08-16`، وسُجل في `verification/daily-plan-order-browser.md` و`verification/interaction-smoke-tests.md`.

## سجل دفعة — تصفح القرآن بالأجزاء — 2026-08-17

وُسّع Route المصحف ليجلب الجزء عند الطلب من AlQuran Cloud، مع الحفاظ على عقد جلب السورة الحالي. أضيف في `/religious` تبديل بين نمط السورة والجزء، ومحدد للأجزاء من 1 إلى 30، وعرض مرجع السورة عند انتقال آيات الجزء بين السور. بقيت المفضلة مرتبطة بمعرّف السورة ورقم الآية، وأُخفي حفظ الموضع السورِي في نمط الجزء حتى لا يُسجل مرجع غير صحيح؛ وعاد زر «أكمل من حيث توقفت» للعمل في نمط السورة.

اختُبر الجزء الأول بصريًا وتفاعليًا، فظهر `الجزء 1 · 148 آية` مع أسماء السور، وبقيت أدوات القارئ والتلاوة وحجم الخط والواجهة RTL سليمة بعد إعادة التحميل. التفاصيل في `verification/quran-juz-browser.md` و`verification/interaction-smoke-tests.md`.

نجحت بوابات `tsc --noEmit` وESLint و`git diff --check` و`next build`، ولم تُضف secrets أو migrations. أُغلق بند جلب النص الكامل لاحقًا بعد اعتماد الفهرس الكامل والجلب عند الطلب من AlQuran Cloud دون تخزين النص في قاعدة بيانات المستخدم؛ التفاصيل في سجل دفعة فهرس المصحف الكامل أدناه.

## سجل دفعة — Onboarding للعادات والورد اليومي — 2026-08-17

اكتمل الجزء العملي الأساسي من Onboarding بإضافة مرحلة «بدايتك» بعد مراحل الاسم والإيقاع والهدف. تتيح المرحلة اختيار حتى ثلاث عادات بداية من قائمة عربية، وتتيح اختيار أحد أنماط الورد الأربعة المدعومة: بالدقائق، بالصفحات، نصف جزء، أو جزء كامل. يشتق هدف الورد من النمط المختار، ثم ينشئ Onboarding العادات المحددة عبر `addHabit` ويحفظ نمط الورد وهدفه عبر `setWirdTarget` قبل عرض شاشة النجاح والملخص.

اختُبر `/onboarding` بصريًا وتفاعليًا باستخدام اسم تجريبي؛ ظهرت مؤشرات المراحل الأربع، اختيرت «قراءة يومية» ونمط «نصف جزء»، وتحدث الهدف إلى «نصف جزء · 10 صفحات». بعد الضغط على «ابدأ مساحتي» ظهرت شاشة «تم حفظ إعداداتك» مع ملخص المدينة وبداية اليوم والاتجاه والعادة والورد وروابط خطة اليوم ولوحة التحكم. التفاصيل في `verification/onboarding-habits-wird-browser.md` و`verification/interaction-smoke-tests.md`.

نجحت بوابات `tsc --noEmit` وESLint و`git diff --check` و`next build`. لم تُضف secrets أو migrations. تبقى أهداف الورد المخصصة لكل نمط والتخصيص الأعمق ضمن البنود المستقبلية.


## سجل دفعة — فهرس المصحف الكامل والنص العثماني — 2026-08-17

وُسّع Route المصحف ليجلب فهرس السور الكامل من AlQuran Cloud، مع استخدام إصدار `quran-uthmani` لجلب النص بالتشكيل عند الطلب عبر Route المحلي. رُبط الفهرس الكامل بواجهة `/religious` مع fallback محدود عند فشل الشبكة، مع الحفاظ على عدم تخزين نص القرآن محليًا أو في قاعدة بيانات المستخدم. بقيت أنماط السورة والجزء، والتلاوة، والمفضلة، وحفظ آخر موضع، وأحجام الخط متوافقة مع العقد السابق.

اختُبر فهرس السور بصريًا وتفاعليًا على `http://localhost:3004/religious`: ظهرت السور العربية من 1 إلى 114، واختيرت السورة 114 بنجاح، فتحول العنوان إلى «سُورَةُ النَّاسِ · 6 آية» وظهر النص العثماني بالتشكيل. التفاصيل في `verification/quran-full-catalog-browser.md` و`verification/interaction-smoke-tests.md`، مع حفظ ملاحظات المصدر في `verification/quran-source-notes.md`.

نجحت بوابات `tsc --noEmit` وESLint و`git diff --check` و`next build` قبل الدفعة، ولم تُضف secrets أو migrations. أُغلق بند جلب النص الكامل بالتشكيل، مع إبقاء أي تحسينات مستقبلية للتخزين المؤقت والأداء خارج هذا البند.

## سجل دفعة — Semantic Tokens لألوان أوراق السبورة — 2026-08-17
نُقلت ألوان أوراق السبورة في `components/board/board-workspace.tsx` من قيم ألوان مباشرة إلى توكنز دلالية معرفة في `app/globals.css`، مع توكنز مستقلة لألوان الورق الأصفر والأزرق والأخضر والوردي، ولون النص الأساسي والثانوي والخلفية الناعمة والحدود. لم تتغير لوحة الألوان الحالية أو أحجام الأوراق أو بنية الكروت أو سلوك الحفظ المحلي.
اختُبرت `/board` بصريًا وتفاعليًا: ظهرت الأوراق بالألوان المتوقعة، وعمل فلتر «أصفر 2» فأظهر الورقتين الصفراوين فقط، ثم عاد العرض إلى «كل الألوان» دون تعديل البيانات. بقيت أدوات التحويل والأرشفة والمجموعة والحجم مقروءة ومتوافقة مع RTL. التفاصيل في `verification/board-semantic-colors-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات `tsc --noEmit` وESLint و`git diff --check` و`next build`، ولم تُضف secrets أو migrations.

## سجل دفعة — Semantic Tokens لحالة تقويم الصلاة الجزئية — 2026-08-17
نُقلت حالة اليوم الجزئية في `components/religious/religious-workspace.tsx` من اللون المباشر إلى توكنز دلالية جديدة في `app/globals.css` (`bg-partial` و`partial-foreground`). بقيت حالات اليوم المكتمل وغير المسجل، وبنية التقويم، والهوية البصرية الحالية دون تغيير.
اختُبرت `/religious` بصريًا وتفاعليًا: سُجّلت صلاة الظهر مؤقتًا كـ«في وقتها» لإنشاء حالة جزئية، وأكد فحص DOM وComputed Styles ظهور خلايا التقويم بالصنف `bg-partial` واللون المحسوب `rgb(255, 243, 196)`. أُعيدت الصلاة إلى «لم تُسجّل» لتنظيف أثر الاختبار. التفاصيل في `verification/prayer-calendar-semantic-colors-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات `tsc --noEmit` وESLint و`git diff --check` و`next build`، ولم تُضف secrets أو migrations. تبقى مراجعة الألوان المباشرة الأخرى ضمن بند نظام التصميم المفتوح.

## سجل دفعة — استخراج StatCard المشترك من Dashboard — 2026-08-16
أُنشئ `components/ui/stat-card.tsx` كمكوّن مشترك لكروت الإحصاءات، مع دعم النغمات الدلالية الحالية `blue` و`green` و`purple` و`orange`، وحالات hover/focus، وروابط تنقل قابلة للوصول. استُبدل التعريف المحلي المكرر داخل `components/dashboard/dashboard-home.tsx` باستخدام المكوّن الجديد دون تغيير النصوص أو الحسابات أو الهوية البصرية.
اختُبرت الصفحة الرئيسية بصريًا وتفاعليًا على `http://localhost:3004/`: ظهرت الكروت الأربعة RTL كما هو متوقع، ونقل النقر على كارت «إنجاز المهام» إلى `/tasks` بنجاح، مع بقاء صفحة المهام وفلاترها سليمة. التفاصيل في `verification/stat-card-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات `tsc --noEmit` وESLint و`git diff --check` و`next build`، ولم تُضف secrets أو migrations.

## سجل دفعة — مرجع نظام التصميم وDialog المشترك — 2026-08-16
أُنشئت صفحة داخلية `app/design-system/page.tsx` لعرض مكونات النظام الحالية جنبًا إلى جنب، بما في ذلك StatCard وContentCard وButton variants والشارات وحقول الإدخال وEmptyState وLoadingState والتوكنز الدلالية. كما أُنشئ `components/ui/dialog.tsx` كمكوّن نافذة موحد يدعم الإغلاق من زر الإغلاق، والضغط خارج النافذة، ومفتاح Escape، مع RTL وطبقات التركيز الحالية.
اختُبرت الصفحة بصريًا وتفاعليًا على `http://localhost:3004/design-system`: ظهرت الأقسام العربية والمكونات المشتركة، فُتحت نافذة المعاينة وظهر overlay والعنوان والأزرار، ثم أُغلقت عبر `Escape` بنجاح دون أخطاء runtime أو hydration. التفاصيل في `verification/design-system-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات `tsc --noEmit` وESLint و`git diff --check` و`next build` قبل تسجيل الدفعة، ولم تُضف secrets أو migrations.
## سجل دفعة — ترحيل Quick Add إلى Dialog المشترك — 2026-08-16
استُبدل غلاف نافذة Quick Add المكرر في `components/layout/top-nav.tsx` باستخدام `components/ui/dialog.tsx` المشترك. احتفظت النافذة بتبويباتها وحقولها وسلوك مسح المسودة عند الإغلاق، بينما أصبح التعامل مع الـoverlay وزر الإغلاق وEscape موحدًا مع باقي النظام، دون تغيير في التخطيط العربي أو منطق الحفظ.
اختُبرت الصفحة الرئيسية بصريًا وتفاعليًا على `http://localhost:3004/`: فُتحت «إضافة سريعة»، وتحققت عناصر النافذة، ثم أُغلقت عبر `Escape` بنجاح دون أخطاء runtime أو hydration، ولم تُحفظ بيانات تجريبية. التفاصيل في `verification/quick-add-dialog-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات `tsc --noEmit` وESLint و`git diff --check` و`next build`، ولم تُضف secrets أو migrations. أصبح ترحيل Quick Add إلى Dialog المشترك منجزًا، وتبقى مراجعة الألوان المباشرة الأخرى وتحويل النوافذ الفعلية المتبقية ضمن البنود المفتوحة.
## سجل دفعة — Cairo وMetadata العربية — 2026-08-16
رُبط Cairo المحمّل عبر `next/font/google` بمتغير `--font-cairo` مع `display: swap`، ثم رُبط توكن `font-sans` بهذا المتغير بدل الاعتماد على اسم خط حرفي. احتُفظ بكلاس `bg-background` على وسم `<html>`، ووُحّدت metadata العربية مع manifest بإضافة اسم التطبيق والكلمات المفتاحية وإزالة قيمة `generator` الخاصة بأداة البناء.
اختُبرت الصفحة الرئيسية بصريًا وتفاعليًا على `http://localhost:3004/`: بقيت Dashboard RTL والكروت والتنقل العلوي دون تغيير، وأكد فحص DOM العنوان والوصف و`lang="ar"` و`dir="rtl"`، كما أكدت CSS المحسوبة استخدام Cairo وخلفية `bg-background`. التفاصيل في `verification/foundation-font-metadata-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات `tsc --noEmit` وESLint و`git diff --check` و`next build`، ولم تُضف secrets أو migrations. أُنجزت البنود المتعلقة بالخط والـmetadata وكلاس الخلفية والألوان المباشرة، وتبقى مراجعة `--radius` والمسافات والنوافذ الفعلية الأخرى ضمن البنود المفتوحة.
## سجل دفعة — تنظيف ألوان Design System — 2026-08-16
استُبدل استخدام `text-white/60` و`text-white/70` في رأس صفحة `/design-system` بتوكن `surface-dark-foreground` مع الحفاظ على درجات الشفافية نفسها. أُعيد مسح كود التطبيق عن أنماط `bg-white` و`text-white` و`border-white` والألوان المباشرة الشائعة، ولم تظهر نتائج.
اختُبرت صفحة مرجع نظام التصميم بصريًا، وفُتحت نافذة Dialog التجريبية ثم أُغلقت عبر `Escape` بنجاح. التفاصيل في `verification/design-system-semantic-color-cleanup-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات TypeScript وESLint و`git diff --check` و`next build` قبل تسجيل الدفعة، ولم تُضف secrets أو migrations.
## سجل دفعة — ترحيل نافذة التذكيرات إلى Dialog — 2026-08-16
رُحّلت نافذة «تذكير جديد» في `components/reminders/reminders-workspace.tsx` إلى `components/ui/dialog.tsx` المشترك. بقيت الحقول العربية والنوع والموعد والتكرار، وربط زر الحفظ بالنموذج عبر `form`، مع الحفاظ على مسح التكرار عند الإلغاء وسلوك الحفظ المحلي/البعيد.
اختُبرت `/reminders` بصريًا وتفاعليًا: ظهرت النافذة والحقول، ثم أُغلقت عبر `Escape` دون حفظ بيانات تجريبية أو تغيير العدادات. التفاصيل في `verification/reminders-dialog-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات TypeScript وESLint و`git diff --check` و`next build` قبل تسجيل الدفعة، ولم تُضف secrets أو migrations.
## سجل دفعة — ترحيل البحث الشامل إلى Dialog — 2026-08-16
رُحّلت `GlobalSearchDialog` إلى `components/ui/dialog.tsx`، وأضيف خيار `hideHeader` لدعم رأس البحث الداخلي مع بقاء العنوان متاحًا لشجرة الوصول. احتُفظ بمنطق البحث العربي، نتائج الأقسام، الروابط، زر الإغلاق ومسح الاستعلام.
اختُبرت الصفحة الرئيسية بصريًا وتفاعليًا: فُتح البحث، أُدخلت «مهمة» وظهرت نتائج من المهام وخطة اليوم، ثم أُغلق عبر `Escape` دون تنقل أو تعديل بيانات. التفاصيل في `verification/global-search-dialog-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات TypeScript وESLint و`git diff --check` و`next build` قبل تسجيل الدفعة، ولم تُضف secrets أو migrations.
## سجل دفعة — مكونات الحقول والشارات المشتركة — 2026-08-16
أضيفت مكونات `Input` و`Textarea` و`Select` و`Badge` إلى `components/ui` بمتغيرات التصميم الدلالية وحالات تركيز وإبطال موحدة، ثم استُخدمت فعليًا في صفحة `/design-system` بدل عناصر الحقول والشارات المكررة.
اختُبرت الصفحة بصريًا وتفاعليًا: أُدخل «نص تجريبي» في Input، وتغيّر Select إلى «هذا الأسبوع»، وظهرت الشارات الدلالية، ثم فُتح Dialog المرجعي وأُغلق عبر `Escape` دون أخطاء أو تغيير بيانات. التفاصيل في `verification/shared-fields-badge-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات TypeScript وESLint و`git diff --check` و`next build` قبل تسجيل الدفعة، ولم تُضف secrets أو migrations.
## سجل دفعة — توكنات ألوان المراجعة الأسبوعية — 2026-08-16
استُبدلت الكلاسات السداسية المباشرة في `ReviewMetric` بتوكنات `insight-warm` و`insight-violet` الدلالية، مع الحفاظ على القيم البصرية الحالية وإضافة تعريفاتها إلى `app/globals.css` وربطها بـTailwind.
اختُبرت `/review` بصريًا داخل RTL، وظهرت مؤشرات المراجعة بالأخضر والبرتقالي والبنفسجي والأزرق دون تغيير في المحتوى أو المنطق. راجعت console ولم تظهر أخطاء runtime أو hydration. التفاصيل في `verification/weekly-review-semantic-colors-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات TypeScript وESLint و`git diff --check` و`next build` قبل تسجيل الدفعة، ولم تُضف secrets أو migrations.
## سجل دفعة — اعتماد الحقول المشتركة في المساحات — 2026-08-16
اعتمدت `WeeklyReviewWorkspace` على `Textarea` المشترك في حقول المراجعة الثلاثة، واعتمد `ReligiousWorkspace` على `Input` و`Select` و`Textarea` في اسم قائمة التلاوة وتدبر الآيات وإعدادات المدينة وطريقة الحساب، مع الحفاظ على handlers والقيم والخصائص الحالية.
اختُبرت `/religious` و`/review` بصريًا وتفاعليًا داخل RTL. اختيرت قيمة «مخصص» الحالية في إعدادات المواقيت دون تغيير مقصود، وظهرت الحقول والقيم والأزرار دون فقدان. راجعت console ولم تظهر أخطاء runtime أو hydration. التفاصيل في `verification/shared-fields-adoption-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات TypeScript وESLint و`git diff --check` و`next build` قبل تسجيل الدفعة، ولم تُضف secrets أو migrations.
## سجل دفعة — ألوان مخطط المال الدلالية — 2026-08-16
أضيفت توكنات `chart-blue` و`chart-teal` و`chart-amber` و`chart-red` و`chart-violet` و`chart-pink` و`chart-emerald` و`chart-slate` إلى `app/globals.css`، واستُبدلت بها القيم السداسية المباشرة في donut وشرائط تصنيفات المصروفات داخل `MoneyWorkspace`.
اختُبرت `/money` بصريًا داخل RTL، وبقيت النسب والبيانات والمقارنة الشهرية وألوان المخطط متسقة. راجعت console ولم تظهر أخطاء runtime أو hydration. التفاصيل في `verification/money-chart-semantic-colors-browser.md` و`verification/interaction-smoke-tests.md`.
نجحت بوابات TypeScript وESLint و`git diff --check` و`next build` قبل تسجيل الدفعة، ولم تُضف secrets أو migrations.
## سجل دفعة — نموذج المصادقة بالمكونات المشتركة — 2026-08-16
اعتمد `AuthForm` على `Input` المشترك في حقول الاسم والبريد وكلمة المرور، وعلى `Button` المشترك لزر الدخول وإنشاء الحساب، مع الحفاظ على منطق Better Auth والتحقق المحلي ورسائل الخطأ وخصائص RTL و`dir="ltr"` للبيانات الإنجليزية.
اختُبرت `/login` بصريًا، ثم بُدّل النموذج إلى وضع إنشاء الحساب دون إرسال بيانات أو تنفيذ مصادقة، وظهر حقل الاسم وتغير الزر كما هو متوقع. التفاصيل في `verification/auth-form-shared-controls-browser.md` و`verification/interaction-smoke-tests.md`.
لم تُضف migrations أو secrets، وتُستكمل بوابات TypeScript وESLint و`git diff --check` و`next build` قبل تسجيل الدفعة.
## سجل دفعة — اعتماد Input في البحث الشامل — 2026-08-16
اعتمد `GlobalSearchDialog` على `Input` المشترك لحقل البحث، مع إبقاء شريط البحث بلا حدود أو خلفية إضافية حتى لا يتغير التصميم الحالي داخل `Dialog`.
اختُبرت الصفحة الرئيسية بصريًا، وفُتح البحث وأُدخلت كلمة «مهمة» فظهرت النتائج في «المهام» و«خطة اليوم»، ثم أُغلق عبر Escape وعادت الصفحة دون تنقل أو استعلام عالق. التفاصيل في `verification/global-search-input-adoption-browser.md` و`verification/interaction-smoke-tests.md`.
لم تُضف migrations أو secrets، وتُستكمل بوابات TypeScript وESLint و`git diff --check` و`next build` قبل تسجيل الدفعة.
## سجل دفعة — اعتماد الحقول المشتركة في الحساب — 2026-08-16
اعتمد `AccountWorkspace` على `Input` المشترك في حقول الاسم والمدينة وبداية اليوم وفترة العمل، وعلى `Textarea` للهدف الرئيسي و`Button` لحفظ التفضيلات، مع إبقاء حقل رفع ملف النسخة الاحتياطية الخام مخفيًا لأنه يحتاج `input type="file"` للوصول إلى File API.
اختُبرت `/account` بصريًا وتفاعليًا؛ أُدخل نص مؤقت في حقل الاسم ثم أُعيدت القيمة الأصلية دون الضغط على الحفظ، وبقي RTL والهوية وسلوك النموذج سليمين. التفاصيل في `verification/account-shared-controls-browser.md` و`verification/interaction-smoke-tests.md`.
لم تُنفذ عمليات تصدير أو استعادة أو حذف، ولم تُضف migrations أو secrets، وتُستكمل بوابات TypeScript وESLint و`git diff --check` و`next build` قبل تسجيل الدفعة.
## سجل دفعة — اعتماد الحقول المشتركة في الملاحظات — 2026-08-16
اعتمد `NotesWorkspace` و`NoteCard` على `Input` و`Textarea` و`Select` و`Button` المشتركة في البحث وفلترة الوسوم والتقاط الملاحظة وتحريرها، مع إبقاء أزرار الوسوم التفاعلية المخصصة كما هي لأنها تمثل حالة متعددة الاختيار داخل البطاقة.
اختُبرت `/notes` بصريًا وتفاعليًا؛ أُدخل «مشروع» في البحث ثم اختير وسم «تطوير»، فظهرت النتيجة المطابقة وظهر خيار «مسح الفلتر» دون إنشاء أو تعديل أي ملاحظة. التفاصيل في `verification/notes-shared-controls-browser.md` و`verification/interaction-smoke-tests.md`.
لم تُضف migrations أو secrets، وتُستكمل بوابات TypeScript وESLint و`git diff --check` و`next build` قبل تسجيل الدفعة.

## سجل دفعة — Checkbox المشترك وترحيل الأرشيف — 2026-08-17

أُنشئ `components/ui/checkbox.tsx` كمكوّن مشترك لحالات التحديد مع توكنات `border-input` و`bg-background` و`accent-primary` وحالات التركيز والإبطال. رُحّل مربعا التحديد الجماعي والفردي في `components/archive/archive-workspace.tsx` إليه، مع الحفاظ على `checked` و`disabled` و`aria-label` ومنطق التحديد والاستعادة دون تغيير عقد البيانات.

وُثّق Checkbox في `/design-system` واختُبرت حالته التفاعلية. فُتحت `/archive` بصريًا، وأكد فحص DOM وجود المكوّن المشترك وتعطيل التحديد الجماعي عند خلو النتائج، دون تنفيذ استعادة أو تعديل بيانات. التفاصيل في `verification/shared-checkbox-archive-browser.md` و`verification/interaction-smoke-tests.md`.

تبقى بوابات TypeScript وESLint و`git diff --check` و`next build` قبل إنشاء commit ودفع الدفعة.


## سجل دفعة — اعتماد الحقول المشتركة في مساحة الترفيه — 2026-08-17
اعتمد `EntertainmentWorkspace` على `Input` في البحث وحقول الاسم والتصنيف والسنة والانطباع، وعلى `Select` في فلاتر النوع والتصنيف وحالة المشاهدة، وعلى `Textarea` للملاحظة، وعلى `Checkbox` لخيارَي الترشيح والتحميل. حُفظت الكلاسات الخاصة بالتخطيط والمظهر الحالي، وبقي منطق `FormData` والفلاتر وتحديث الحالة كما هو.
اختُبرت `/entertainment` بصريًا وتفاعليًا؛ أُدخلت كلمة «دراما» في البحث، واختير التصنيف نفسه، ثم فُتح نموذج الإضافة وفُعّل Checkbox الترشيح وأُلغي النموذج دون حفظ. لم تظهر أخطاء runtime أو hydration في console. التفاصيل في `verification/entertainment-shared-controls-browser.md` و`verification/interaction-smoke-tests.md`.
لم تُضف migrations أو secrets، وتُستكمل بوابات TypeScript وESLint و`git diff --check` و`next build` قبل إنشاء commit ودفع الدفعة.


## سجل دفعة — اعتماد الحقول المشتركة في مساحة الأهداف — 2026-08-17
اعتمد `GoalsWorkspace` و`GoalCard` على `Input` و`Select` و`Textarea` و`Button` المشتركة في نموذج إنشاء الهدف، وتعديل الوصف والموعد، وإضافة مهمة مرتبطة بالهدف، مع إبقاء أزرار البطاقات المتخصصة وسلوك `FormData` والحالة كما هي.
اختُبرت `/goals` بصريًا وتفاعليًا؛ فُتحت تفاصيل الهدف ووضع التعديل، وأُدخل وصف عربي مؤقت ثم أُعيدت القيمة الأصلية دون حفظ. ظهر نموذج الهدف الجديد وحقل المهمة دون أخطاء، ولم تُنشأ بيانات اختبارية. التفاصيل في `verification/goals-shared-controls-browser.md` و`verification/interaction-smoke-tests.md`.
لم تُضف migrations أو secrets، وتُستكمل بوابات TypeScript وESLint و`git diff --check` و`next build` قبل إنشاء commit ودفع الدفعة.

## سجل دفعة — اعتماد الحقول المشتركة في مساحة العادات — 2026-08-17
اعتمد `HabitsWorkspace` على `Input` لاسم العادة وهدفها، وعلى `Select` للمهمة والمشروع والهدف المرتبطين، وعلى `Button` للحفظ، مع الحفاظ على القيم والـhandlers والقيود وسلوك الإضافة الحالي.
اختُبرت `/habits` بصريًا وتفاعليًا؛ فُتح نموذج «عادة جديدة»، أُدخل اسم عربي مؤقت، واختير مشروع «روتين الحركة والنوم» ثم أُعيد إلى «بدون مشروع»، وأُفرغ الاسم وأُغلق النموذج دون حفظ. بقي RTL والتخطيط والبيانات الأصلية سليمة. التفاصيل في `verification/habits-shared-controls-browser.md` و`verification/interaction-smoke-tests.md`.
لم تُضف migrations أو secrets، وتُستكمل بوابات TypeScript وESLint و`git diff --check` و`next build` قبل إنشاء commit ودفع الدفعة.


## دفعة اعتماد الحقول المشتركة في القسم الديني — 2026-08-17

تم ترحيل القوائم والحقول الخام المتبقية في `components/religious/religious-workspace.tsx` إلى `Select` و`Input` المشتركين، مع الحفاظ على خيارات الصلاة والسبحة والمصحف والقارئ والدعاء، وعلى الـhandlers والكلاسات السلوكية ومنطق الحفظ المحلي. تحقق المتصفح من RTL، ومن تغيير هدف السبحة مؤقتًا ثم إعادته إلى `33`، ومن إدخال دعاء مؤقت ثم إفراغه دون حفظ. أكد فحص DOM وجود `data-slot` للمكونات المشتركة، كما لم يظهر خطأ Next ظاهرًا.

مرجع التحقق: `verification/religious-shared-controls-adoption-browser.md`، وسجل التفاعل: `verification/interaction-smoke-tests.md`.

الحالة: **منفذة وظيفيًا، بانتظار بوابات الجودة والـcommit والـpush**.


## دفعة اعتماد Button المشترك في مساحة الترفيه — 2026-08-16

- **النطاق:** ترحيل الأزرار الخام المتبقية في `components/entertainment/entertainment-workspace.tsx` إلى `components/ui/button.tsx` مع الحفاظ على الـvariants والأحجام والكلاسات المحلية ومنطق الحالة.
- **التحقق:** فحص بصري وتفاعلي على `/entertainment`؛ تفعيل وإلغاء فلتر «مرشّح لحد»؛ فتح وإلغاء نموذج «إضافة عمل» دون حفظ؛ مراجعة console دون أخطاء runtime أو hydration.
- **التوثيق:** `verification/entertainment-shared-buttons-browser.md` و`verification/interaction-smoke-tests.md`.
- **الحالة:** مكتمل وظيفيًا، بانتظار بوابات الجودة ثم commit وpush.
