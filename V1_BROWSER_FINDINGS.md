# نتائج الفحص المتصفحي — Projects/Goals

## Projects

- بعد إعادة تشغيل خادم production، ظهرت صفحة `/projects` بالواجهة الجديدة بدل placeholder.
- ظهرت أعمدة Kanban العربية: قادم، جاري، مكتمل.
- ظهرت البطاقات التجريبية الثلاثة مع الهدف المرتبط، الموعد، نسبة التقدم، وعدد المهام.
- ظهر نموذج إضافة مشروع مع اختيار الهدف المرتبط.
- التنقل العام حافظ على RTL والروابط العربية، وظهر رابط حسابي.
- أزرار نقل المشروع والأرشفة ظهرت في البطاقات.

## ملاحظة جودة

- الفحص المتصفحي التالي يجب أن يشمل `/goals` والتفاعل مع زر نقل مشروع، إضافة مشروع محليًا، واختبارات 401 لـ`/api/goals` و`/api/projects` بدون جلسة.

## Goals والتفاعل المحلي

ظهرت صفحة `/goals` ببطاقات الأهداف، مؤشرات التقدم، عدد المشاريع المرتبطة، وأزرار الإيقاف المؤقت والأرشفة. تم ملء نموذج هدف جديد ثم إرساله، فظهر الهدف فورًا في أعلى القائمة وتغيرت الإحصاءات من هدفين نشطين إلى ثلاثة، ما يؤكد عمل optimistic localStorage fallback في غياب الجلسة وقاعدة البيانات.
