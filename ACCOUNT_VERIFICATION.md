# تحقق دفعة Account / Profile

## النطاق

تغطي هذه الدفعة ربط تفضيلات المستخدم بالجلسة والـBackend عند توفر credentials، مع الحفاظ على تجربة localStorage عند غياب قاعدة البيانات أو جلسة Better Auth.

## ما تم تنفيذه

- إضافة جدول `user_profile` إلى Drizzle schema مع `user_id` وقيود الملكية.
- إضافة migration `0001_fuzzy_miss_america.sql` لإنشاء جدول التفضيلات.
- إضافة `GET/PATCH /api/profile` مع استخراج المستخدم من الجلسة، ورفض الطلبات غير المصرح بها.
- جعل تحديث Profile جزئيًا آمنًا عبر دمج القيم الحالية بدل استبدال التفضيلات غير المرسلة بالقيم الافتراضية.
- إضافة hydration وoptimistic persistence للتفضيلات في `lib/backend-sync.ts` و`lib/command-center-store.tsx`.
- إضافة صفحة `/account` ومكوّن `AccountWorkspace` لتعديل الاسم، المدينة، بداية اليوم، فترة العمل، والهدف الحالي.
- إضافة رابط **حسابي** إلى التنقل العام.

## الاختبارات المنفذة

| الاختبار | النتيجة | الملاحظات |
|---|---|---|
| `pnpm exec tsc --noEmit` | ناجح | لا توجد أخطاء TypeScript بعد حذف `tsconfig.tsbuildinfo` القديم. |
| `pnpm exec next build` | ناجح | تم تجميع المسارات وتوليد الصفحات بنجاح. ظهر تحذير Next.js 16 بأن convention الخاص بـ`middleware` أصبح deprecated لصالح `proxy`، وليس فشلًا في البناء. |
| فتح `/account` في المتصفح | ناجح | ظهرت الصفحة بالعربية وRTL، وظهرت بطاقات الملف والتفضيلات وقيم localStorage الافتراضية، مع رابط حسابي في TopNav. |
| تعديل الهدف ثم الضغط على حفظ | ناجح | ظهرت رسالة `تم الحفظ`، واستمر العرض المحلي بدون جلسة أو قاعدة بيانات. |
| `GET /api/profile` بدون جلسة | ناجح | أعاد `HTTP/1.1 401 Unauthorized` والرسالة العربية `يجب تسجيل الدخول أولاً.` |
| عمل Profile مع قاعدة بيانات فعلية | مؤجل | لا توجد `DATABASE_URL` أو `BETTER_AUTH_SECRET` فعلية في بيئة الاختبار الحالية؛ طبقة الواجهة تستخدم localStorage fallback كما هو مطلوب. |
| اختبار حفظ Profile بجلسة فعلية | مؤجل | يحتاج حساب Better Auth وNeon credentials؛ مسار PATCH مقيد بالجلسة وownership في الكود. |

## ملاحظات الجودة

- لم تتم إضافة أي secret إلى الريبو.
- لا يعتمد العرض المحلي على قاعدة البيانات، ولا يؤدي غياب credentials إلى صفحة بيضاء.
- كل استعلام Profile يستخدم `user_id` المستخرج من الجلسة، ولا يعتمد على `user_id` من body العميل.
- صفحة Account ملتزمة بالكروت المستديرة، Cairo، semantic tokens، وRTL الموجود في الهوية الحالية.

## الخطوة التالية

الانتقال إلى دفعة V1 الخاصة بـProjects وGoals، مع تصميم العلاقة `goal -> project -> task` قبل إضافة Kanban والتحديثات الخلفية الخاصة بها.
